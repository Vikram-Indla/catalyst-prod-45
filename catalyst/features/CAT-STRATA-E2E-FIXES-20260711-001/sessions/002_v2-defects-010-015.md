# Session 002 — STRATA E2E QA Variance Report v2 (defects 010–015)

**Date:** 2026-07-11
**Input:** `STRATA-E2E-QA-VARIANCE-REPORT-v2.md` (defects STRATA-E2E-010 … 015)
**Method:** /regression — parallel read-only investigators → master table → per-cluster fix,
one commit each, verified functionally (DOM + Supabase `execute_sql`), never by screenshot.

## Outcome per defect

| ID | Verdict | Commit | Notes |
|---|---|---|---|
| 010 | **Fixed (partial) — OPEN** | `ca90cdede` | Ownership at creation works. Hierarchy-link-at-creation is infeasible (server rejects draft linking) — kept OPEN pending backend decision + QA retest. |
| 011 | Not a defect (product) | — | Current perspective seed authoritative (Vikram: current seed correct). No change. |
| 012 | Not a defect | — | Add-member verified live: RPC 200, member count 1→2, row renders. Absent success toast is the documented platform convention (no-success-flag, Vikram 2026-06-16). |
| 013 | **Fixed (partial) — OPEN** | `4ccb5db46` | Selector now excludes existing members + disambiguates twin names. Cycle/tenant scoping impossible (no cycle_id, org_id 100% NULL) — kept OPEN pending data/schema decision + QA retest. |
| 014 | Not a defect (product) | — | REQ-019 kept — legacy Initiative stays selectable for legacy rows. No change. |
| 015 | Fixed | `ab248a5b6` | Download error now surfaces via ADS SectionMessage. Generation logic was already correct; original failure was environmental. |

## Commits (each = one file, tsc + ADS audit clean, live-verified)
- `ab248a5b6` — 015: import-template error surfacing (`StrataExecutionImportPage.tsx`)
- `4ccb5db46` — 012 (not-a-defect) + 013: portfolio Add-member selector (`vmoAuthoring.tsx`)
- `ca90cdede` — 010: KPI ownership at creation (`StrataKpiLibraryPage.tsx`)

## Live-verification evidence
- 012: added a Project Card to "Growth Portfolio FY2027 (proof)" → RPC `strata_add_portfolio_member`
  200, count 1→2, row present; removed to restore state (`execute_sql` count back to 1).
- 013: `ICP Project` twins rendered as `· Manual` / `· Jira: ICP`; after adding one, it dropped
  from the selector and the survivor rendered unambiguously.
- 010: created `ZZTEST-010-Owner-KPI` with an accountable owner → modal closed clean,
  `accountable_owner_id` persisted (Abdullah Alshammari); test rows deleted.

## Verification risk (NOT a passed test)
Vitest cannot run in this environment — rolldown/Node `styleText` crash
(`ERR_INVALID_ARG_VALUE … ['underline','gray']`) prevents the runner from starting. The
`initiative.seam.guard.test.ts` (REQ-019) assertions were verified by READING, not executing.
Any claim that unit tests pass is unproven until the runner is fixed.

## 2026-07-11 — concurrency risk noted
Launched R5 (cross-category) and R9 (typography) fix agents in parallel against the SAME poison worktree (not isolated via separate git worktrees) since they were scoped to touch different declaration types (color/border vs font-size) potentially in the same files. Risk: concurrent Read/Edit on a shared file could race. Mitigation: on completion, diff review before commit; if corruption suspected, re-run one slice serially against the other's committed result instead of trusting the merged working tree.
## D-2026-07-11-01
Caught own mistake: when the R8 sweep agent reported it had spawned 5 sub-agents and was "waiting" but showed status=completed, I mistakenly called Agent (fresh, no context) instead of SendMessage(to: <original agentId>) to resume it. Killed the stray agent immediately (task_id a3dbf9473cae51597) before it touched any files — TaskStop confirmed "killed", zero file changes from it. Correctly resumed the original agent (aa65faefd294c756e) via SendMessage with full reconciliation instructions. No file corruption; caught before any risk materialized.

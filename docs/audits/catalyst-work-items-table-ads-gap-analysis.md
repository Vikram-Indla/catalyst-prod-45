# Catalyst Left Panel Navigator — ADS Gap Analysis

> Preflight audit — 2026-05-21. Evidence: Rovo spec sections 1–21 + live codebase.
> Primary files: `IssueListPanel.tsx`, `allwork.css`
> No fixes applied.

---

## Gap Analysis Matrix

| # | Audit Area | Rovo / ADS Requirement | Catalyst Current Evidence | Gap | Severity | Fix Required | File(s) |
|---|---|---|---|---|---|---|---|
| 1 | **Summary truncation** | No truncation — full summary displayed, cards grow in height indefinitely | `.jlpCardSummary { -webkit-line-clamp: 2; -webkit-box-orient: vertical; overflow: hidden }` — hard 2-line clamp | **CRITICAL** — spec explicitly states "No truncation — full summary text is displayed". Cards currently cut off long summaries | P0 | Remove `-webkit-line-clamp: 2`, `overflow: hidden`, `-webkit-box-orient: vertical`. Let card grow. | `allwork.css:364` |
| 2 | **Group-by selector** | Left side of header: field-name label + ChevronDown dropdown, `DropdownMenu` with group-by options (Parent, Status, Assignee, Priority, Sprint, Epic, Label, Issue Type, Fix Version, None), `aria-haspopup="listbox"` | **COMPLETELY ABSENT** — header only has a sort-order dropdown; no grouping concept exists in IssueListPanel | Feature missing | P0 | Implement group-by selector using `@atlaskit/dropdown-menu` with `DropdownItemGroup` and `DropdownItem` for all grouping fields | `IssueListPanel.tsx` |
| 3 | **Group headers in card list** | When grouping active: non-interactive group header rows (12px/700/uppercase/`#626F86`, padding 12px 16px, background `#F7F8F9`) | **ABSENT** | Feature missing | P0 | Add group header rendering between card groups | `IssueListPanel.tsx`, `allwork.css` |
| 4 | **Collapsible groups** | Group headers toggle child visibility; chevron icon rotates; `role="group"`, `aria-label="Parent: [summary]"` | **ABSENT** | Feature missing | P0 | Implement collapsible group state | `IssueListPanel.tsx` |
| 5 | **Infinite scroll / load-more** | IntersectionObserver triggers at ~200px from bottom; 50-item pages appended; spinner at list bottom during fetch; debounce 200ms; concurrent-fetch guard | **ABSENT** — all items rendered at once, no pagination | Feature missing | P0 | Add IntersectionObserver-based infinite scroll; integrate with data fetching layer | `IssueListPanel.tsx` |
| 6 | **Result count logic** | `"{loadedCount} of {totalCount}"` — loadedCount = currently-loaded items; totalCount from API; both update on scroll | Both numbers use `sortedItems.length`: `{sortedItems.length} of <strong>{sortedItems.length >= 1000 ? '1000+' : sortedItems.length}</strong>`. `totalCount` prop accepted but never used in render. | Logic bug — loaded = total always | P0 | Wire `totalCount` prop into the "of N" display; track `loadedCount` as pages load | `IssueListPanel.tsx:247` |
| 7 | **Panel container ARIA** | `role="navigation"`, `aria-label="Issue list"` on panel wrapper | No ARIA role on panel container | Missing | P0 | Add `role="navigation"` + `aria-label="Issue list"` to panel wrapper | `IssueListPanel.tsx` |
| 8 | **Card ARIA label** | Comprehensive: `aria-label="BAU-3243: Industrial Scan CR (block the services) - ..., Change Request, assigned to Yousuf Dawood"` | No `aria-label` on card — only `role="option"` + `aria-selected` | Missing accessible name | P0 | Build descriptive `aria-label` from key + summary + type + assignee | `IssueListPanel.tsx` |
| 9 | **`aria-activedescendant`** | `<div role="listbox" aria-activedescendant="card-{activeKey}">` | No `aria-activedescendant` on listbox container | Missing | P0 | Add `aria-activedescendant` pointing to active card id | `IssueListPanel.tsx` |
| 10 | **Icon source: ChevronDown** | `ChevronDownIcon` from `@atlaskit/icon` | `ChevronDown` from `@/lib/atlaskit-icons` (Lucide re-export) | Wrong library | P1 | Replace with `import ChevronDownIcon from '@atlaskit/icon/glyph/chevron-down'` | `IssueListPanel.tsx:2` |
| 11 | **Icon source: refresh** | `RefreshIcon` or `RetryIcon` from `@atlaskit/icon` | `RotateCcw` from `@/lib/atlaskit-icons` (Lucide re-export) | Wrong library | P1 | Replace with `import RefreshIcon from '@atlaskit/icon/glyph/refresh'` | `IssueListPanel.tsx:2` |
| 12 | **Icon source: sort arrows** | `@atlaskit/icon` arrow variants | `ArrowUpNarrowWide` / `ArrowDownNarrowWide` from `@/lib/atlaskit-icons` (Lucide) | Wrong library | P1 | Replace with `@atlaskit/icon/glyph/arrow-up` and `@atlaskit/icon/glyph/arrow-down` | `IssueListPanel.tsx:2` |
| 13 | **Sort/filter config button** | Dedicated `≡↕` icon button that opens a sort-configuration popup; `@atlaskit/button IconButton appearance="subtle"` | Sort direction toggle button (arrow up/down toggle on click) — different semantics | Missing sort config popup | P1 | Replace direction-toggle with proper sort-config `IconButton`; build popup | `IssueListPanel.tsx` |
| 14 | **Refresh button animation** | Icon rotates 360° clockwise over 500ms on click | No animation on click | Missing | P2 | Add `transform: rotate(360deg)` animation on click | `IssueListPanel.tsx`, `allwork.css` |
| 15 | **ChevronDown rotation** | Rotates 180° (`transform: rotate(180deg)`) over 200ms when dropdown is open | No rotation implemented | Missing | P2 | Add `transform: rotate(180deg)` CSS when `sortMenuOpen` is true | `allwork.css` |
| 16 | **Sort button height** | 32px × 32px tap target for header buttons | `.jlpSortBtn { height: 28px }`, `.jlpToolBtn { width: 28px; height: 28px }` | 4px short | P2 | Change to 32px height on both | `allwork.css:244,258` |
| 17 | **Card padding** | `padding: 16px 16px 16px 16px` (or `16px 16px 16px 13px` with accent bar) | `padding: 12px 16px` | 4px top/bottom missing | P1 | Change to `padding: 16px` | `allwork.css:351` |
| 18 | **Card min-height** | ~56px for metadata-only cards | No `min-height` | Not set | P2 | Add `min-height: 56px` | `allwork.css` |
| 19 | **Issue type icon size** | 20px × 20px | `<JiraIssueTypeIcon size={16}>` | 4px smaller than spec | P1 | Change to `size={20}` | `IssueListPanel.tsx:197` |
| 20 | **Assignee avatar component** | `@atlaskit/avatar <Avatar size="small">` (28px × 28px) + `border: 2px solid #FFFFFF` (white ring) | `<CatalystOwnerAvatar size="md">` (custom, likely 32px) — no white ring | Wrong component; size drift; no white ring | P1 | Replace with `@atlaskit/avatar` or at minimum add white ring and correct size | `IssueListPanel.tsx:200` |
| 21 | **Avatar tooltip** | `@atlaskit/tooltip` with 300ms delay, full display name, ADS dark tooltip styling | `showTooltip` on `CatalystOwnerAvatar` — implementation unknown | I cannot confirm this from the current Catalyst codebase without reading `CatalystOwnerAvatar` internals | P1 | Verify `CatalystOwnerAvatar` uses `@atlaskit/tooltip` with 300ms delay | `CatalystOwnerAvatar.tsx` |
| 22 | **Active card key color** | Active card: key color = `#0C66E4` (`color.link`). Inactive: `#44546F` (`color.text.subtle`) | `.jlpCardKey { color: var(--aw-text-subtle) }` — same color regardless of active state | Active key color not applied | P1 | Add `.jlpCard.jlpCardSelected .jlpCardKey { color: #0C66E4 }` | `allwork.css` |
| 23 | **Active card summary font-weight** | Active card summary: font-weight 400 (same as inactive) | `.jlpCardSummaryActive { font-weight: 500 }` | Spec says 400, Catalyst uses 500 | P2 | Change to `font-weight: 400` | `allwork.css:373` |
| 24 | **Card hover state** | `background: #F1F2F4` (`color.background.neutral.hovered`) | `.jlpCard:hover { background: var(--aw-hover) }` where `--aw-hover: var(--ds-surface-sunken, #F4F5F7)` | Token wrong — `--ds-surface-sunken` ≠ `color.background.neutral.hovered` | P1 | Change to `var(--ds-background-neutral-hovered, #F1F2F4)` | `allwork.css:357` |
| 25 | **Card active+hover state** | `background: #CCE0FF` (`color.background.selected.hovered`) | Not implemented | Missing | P2 | Add `.jlpCard.jlpCardSelected:hover { background: var(--ds-background-selected-hovered, #CCE0FF) }` | `allwork.css` |
| 26 | **Card pressed state** | `background: #091E4224` (`color.background.neutral.subtle.pressed`) brief flash | Not implemented | Missing | P2 | Add `:active` rule | `allwork.css` |
| 27 | **Card focus ring** | `2px solid #388BFF` (`color.border.focused`) focus ring on keyboard focus | No focus ring implemented | Missing WCAG 2.4.11 focus appearance | P0 | Add `@atlaskit/focus-ring` or equivalent CSS on `.jlpCard:focus-visible` | `allwork.css`, `IssueListPanel.tsx` |
| 28 | **Scrollbar width** | 6–8px thin style | `::-webkit-scrollbar { width: 20px }` — very wide Windows-style bar | 2.5× too wide | P1 | Reduce to 6–8px | `allwork.css:320` |
| 29 | **Scrollbar track color** | Transparent | `background: #F1F2F4` with `border-left: 1px solid #EBECF0` | Not transparent | P2 | Set `background: transparent` | `allwork.css:322` |
| 30 | **Card list container background** | `#F7F8F9` — shows through 1px gap between cards as separator | No background on `.jlpCards` | Gap-separator pattern missing | P2 | Add `background: #F7F8F9` to `.jlpCards` (or to `.jlpBody`) | `allwork.css:348` |
| 31 | **Panel width** | ~360–420px fixed; resize handle optional | I cannot confirm this from the current Catalyst codebase — panel width is set by the `awShell` CSS grid (`--aw-left: 240px`) | Panel likely 240px — far below spec minimum of ~360px | P0 | Increase `--aw-left` to 360–400px; add resize handle optionally | `allwork.css:180` |
| 32 | **Header height** | 48px explicit | No explicit height on `.jlpHeader` | Not guaranteed | P2 | Add `height: 48px` or `min-height: 48px` | `allwork.css` |
| 33 | **Header background** | `#FFFFFF` (`elevation.surface`) | `var(--aw-bg)` which resolves to `#ffffff` via `--cp-bg-elevated` | Correct value but wrong token chain | P2 | Use `var(--ds-surface, #FFFFFF)` directly | `allwork.css:226` |
| 34 | **Panel border-right** | `1px solid #091E4224` (`color.border`) | `.awDivider { background: var(--aw-border); width: 1px }` — uses a separate divider div | Correct visual result, different DOM structure | P2 | Acceptable; document as intentional | `allwork.css:199` |
| 35 | **Keyboard: J / K** | J = next issue; K = previous issue (Jira global shortcuts) | **ABSENT** | Missing | P1 | Add J/K key handling to `handleKeyDown` | `IssueListPanel.tsx:152` |
| 36 | **Keyboard: Enter** | Select/activate focused card | **ABSENT** | Missing | P1 | Add Enter key handling to `handleKeyDown` | `IssueListPanel.tsx:152` |
| 37 | **Keyboard: Space** | Alternative to Enter — select focused card | **ABSENT** | Missing | P2 | Add Space key handling | `IssueListPanel.tsx:152` |
| 38 | **Keyboard: Home / End** | Home = first card; End = last card | **ABSENT** | Missing | P1 | Add Home/End key handling | `IssueListPanel.tsx:152` |
| 39 | **Keyboard: Escape** | Move focus from panel to detail pane | **ABSENT** | Missing | P1 | Add Escape handler; call `onEscape` callback | `IssueListPanel.tsx` |
| 40 | **URL sync for selected issue** | Selected issue key in URL (`?selectedIssue=BAU-3243`) for deep linking and browser back | **ABSENT** in IssueListPanel | Missing deep link | P1 | Read/write `selectedIssue` URL param via `useSearchParams` | `IssueListPanel.tsx` |
| 41 | **Auto-scroll to active card** | Arrow key navigation scrolls card into view (minimum scroll distance) | **ABSENT** | Missing | P1 | Add `cardRef.current?.scrollIntoView({ block: 'nearest' })` on selection change | `IssueListPanel.tsx` |
| 42 | **Load-more spinner** | `@atlaskit/spinner <Spinner size="medium">` centered, 16px vertical padding, appears during page fetch | **ABSENT** — skeleton only for initial load | Missing | P1 | Add Spinner at list bottom when loading next page | `IssueListPanel.tsx`, `allwork.css` |
| 43 | **Error state for load-more** | `"Failed to load more issues. Retry"` with retry link | **ABSENT** | Missing | P2 | Add error state at list bottom | `IssueListPanel.tsx` |
| 44 | **`@atlaskit/dropdown-menu` for group-by** | `DropdownMenu` + `DropdownItemGroup` + `DropdownItem` | Self-rolled sort dropdown with `<div>` + radio inputs | ADS violation | P1 | Replace sort dropdown with `@atlaskit/dropdown-menu` as minimum; add group-by as separate `DropdownMenu` | `IssueListPanel.tsx:178` |
| 45 | **`@atlaskit/button IconButton` for toolbar** | `<IconButton appearance="subtle">` for sort-config and refresh | Native `<button>` elements | ADS violation | P1 | Replace with `@atlaskit/button/new IconButton` | `IssueListPanel.tsx:162,166` |
| 46 | **Footer count `aria-live`** | `aria-live="polite"` on footer announcing count changes | **ABSENT** | Missing | P1 | Add `aria-live="polite"` to footer container | `IssueListPanel.tsx:242` |
| 47 | **Metadata-only card (no summary)** | When summary is empty/not shown, card collapses to metadata row only (~56px) | `<div.jlpCardSummary>{item.summary}</div>` always rendered — no conditional — but empty string would collapse to zero height | Summary always rendered; no explicit conditional for absent-summary pattern | P2 | Add `{item.summary && <div.jlpCardSummary>...}` conditional | `IssueListPanel.tsx:191` |
| 48 | **Card border-radius** | `0` — cards are full-width flush | `.jlpCard` has no `border-radius` — OK | ✓ Compliant | — | — | — |
| 49 | **Card cursor** | `pointer` | `.jlpCard { cursor: pointer }` (inferred from spec; present as `.awRow:hover` pattern) | I cannot confirm this from current CSS without explicit check | P2 | Verify `cursor: pointer` on `.jlpCard` | `allwork.css` |
| 50 | **Group header ARIA** | `role="group"`, `aria-label="Parent: [parent issue summary]"` | **ABSENT** — no group headers | Feature missing | P0 | Implement with group headers | `IssueListPanel.tsx` |
| 51 | **Result count: "1000+" threshold** | `1000+` shows when total exceeds threshold; exact count may load async | `sortedItems.length >= 1000 ? '1000+' : sortedItems.length` — BUT the loaded count and total are both `sortedItems.length` | Logic exists but wired to wrong value | P0 | Fix: `{loadedCount} of <strong>{totalCount >= 1000 ? '1000+' : totalCount}</strong>` | `IssueListPanel.tsx:247` |
| 52 | **Result count styling** | `"50"` + `" of "` = weight 400, `#44546F`; `"1000+"` = weight 700, `#0C66E4` bold blue | `{sortedItems.length} of <strong>...</strong>` — loaded count has no explicit weight/color styling; only `<strong>` wraps total | Loaded count and "of" text lack explicit `#44546F` color | P2 | Add explicit color to "N of" portion | `allwork.css:426` |
| 53 | **Card gap / list background** | List container `background: #F7F8F9`; cards are `#FFFFFF`; 1px gap between cards shows through | Cards use `border-bottom: 1px solid var(--aw-border)`; container background not set | Different separator mechanism | P2 | Switch to gap+background mechanism or document border approach as intentional | `allwork.css` |
| 54 | **First card auto-selection on load** | First card auto-selected on initial load OR previously-viewed issue re-selected | Handled by parent (`selectedIssueKey` prop); no auto-selection inside component | I cannot confirm parent behavior — noted for follow-up | P2 | Verify parent sets initial `selectedIssueKey` | Parent component |
| 55 | **`@atlaskit/tokens` usage** | All colors via `@atlaskit/tokens` `token()` function | Mix of `var(--ds-*)`, `var(--cp-*)`, raw hex | Partial | P1 | Replace raw hex and `var(--cp-*)` with `token()` calls or `var(--ds-*)` | `allwork.css` |
| 56 | **AllWorkSplitView deprecated** | N/A — should be removed | `AllWorkSplitView.tsx` marked `@deprecated` but still compiled and referenced from `src/pages/workhub/AllWork.tsx` | Stale code compiles, increases bundle | P1 | Remove `AllWorkSplitView` and its route; ensure `IssueListPanel` is the sole implementation | Multiple files |

---

## Summary Counts

| Severity | Count |
|---|---|
| P0 — Critical blocking gap | 9 |
| P1 — Significant parity/ADS violation | 26 |
| P2 — Minor deviation / polish | 21 |
| Compliant / intentional | 2 |

# 03 — Gap Audit (Gate 3)

> Current Catalyst vs target enterprise Filters. Every row evidence-backed (file:line / pg catalog). Severity: P0 (security/data-integrity/blocking) · P1 (real defect / missing capability) · P2 (debt / polish).

| Gap | Current state | Target state | Sev | Files involved | Backend impact | UX impact | Risk | Fix strategy |
|---|---|---|---|---|---|---|---|---|
| **G1** | Directory row click + name link both → `/filters/create?filterId=` (builder). `FilterDetailPage` route orphaned. `FiltersListPage.tsx:269-275,317,578` | Row click + name → `/:key/filters/:filterId` (detail). Only explicit Edit → builder. | P1 | `FiltersListPage.tsx` | none | Users land in edit mode unexpectedly; can't read without risk of dirtying | Low | Change `detailHref` to point at `:filterId`; add a separate `editHref` for the kebab Edit action |
| **G2** | 3 divergent `filterState→JQL` (`basicToJql`, `lib/filterStateToJql`, `AllWorkToolbar.filterStateToJql`); `jqlToFilterState` forked ×3 (lib + 2 local dupes `FilterPreviewPage.tsx:280`, `ProductFilterPreviewPage.tsx:305`) | Single `FilterQueryModel` adapter: one serialize, one parse, one execute path | P1 | `lib/filters/*`, `lib/jql/*`, `AllWorkToolbar.tsx`, both preview pages | none (pure TS) | Inconsistent JQL for identical UI input; parse fixes don't propagate | High | Phase C: delete 2 local dupes first → collapse forward serializers behind adapter → golden-output test per call site |
| **G3** | `useLinkedEntities` stub returns `[]` (`FilterPreviewPage.tsx:600`, `ProductFilterPreviewPage.tsx:286`). "Linked entities" UI always empty | Real query: boards (`boards.filter_id`) + derived views (`filter_derived_views.source_filter_id`) | P1 | both preview pages, new shared hook | read-only query | Dead feature — users can't see what depends on a filter before editing/deleting | Low | Replace stub with `useLinkedEntities(filterId)` querying both tables; reuse on detail page derived-views section |
| **G4** | RLS UPDATE = `user_id OR owner_id` only. `editors_config` exists but is unenforced — specific editors cannot write | UPDATE honors editors via `ph_saved_filter_can_edit(filter_id, uid)` helper (mirrors the existing project/product member helpers) | **P0** | new migration; `FilterSaveModal`, share modal | **RLS change + new SECURITY DEFINER fn** | "Add editor" is a lie today; editors get 403 on save | Med | Add helper fn (qualify params — 2026-06-10 lesson); rewrite UPDATE policy `user_id OR owner_id OR can_edit`; 2-user RLS isolation test |
| **G5** | Missing indexes: `owner_id`, `product_key`, GIN `starred_by_user_ids`, GIN `viewers_config` | Add 4 indexes (owner & product used heavily in RLS SELECT; starred GIN for "Starred" tab; viewers GIN for the SELECT policy's jsonb `?` op) | P2 | new migration | 4 `CREATE INDEX` (non-destructive) | Slow directory queries at scale (10 rows today, but Jira sync grows it) | Low | `CREATE INDEX CONCURRENTLY` where possible; verify with EXPLAIN |
| **G6** | Legacy `saved_filters` (0 rows) live only via `SearchPage.tsx` (insert/update/delete/select). Orphan hook `src/hooks/useSavedFilters.ts` (0 importers) | Delete orphan hook. Migrate `SearchPage` saved-searches off `saved_filters` OR leave as a separate "saved searches" concern (NOT Filters module) | P2 | `src/hooks/useSavedFilters.ts`, `SearchPage.tsx` | optional table drop later | none (table empty) | Low | Delete orphan hook now; flag SearchPage migration as separate decision — **ASK Vikram** before touching global Search |
| **G7** | Derived views (kanban/roadmap/dashboard) + WhatsApp gated FALSE, project-hub only | Decide rollout: enable flags for project hub; extend to product/incident/tasks/release | P1 | `featureFlags.ts`, `FilterKebabMenu`, hub wrappers | none | Capability invisible until flags flipped | Low | Phase G/H: flip flags per hub after parity verified; keep project-hub-first |
| **G8** | No Releases-hub filter route/nav. `FilterHubType` lacks `'release'` (template registry has it) | Add `/releases/filters` trio + sidebar nav + `FilterHubType += 'release'` + sentinel scoping | P1 | `FullAppRoutes.tsx`, `ReleasesHubSidebar`, `filterVisibility.ts`, new thin wrappers | none (reuses `ph_issues`) | Release managers have no saved filters | Low-Med | Mount canonical trio with `hubType='release'`, fixVersion-scoped results |
| **G9** | `generate-whatsapp-summary` has no deterministic fallback; AI failure → error toast, no summary | Client-side template fallback from `FilterSummaryContext` counts (no AI, can't hallucinate) | P1 | new `buildDeterministicSummary.ts`, WhatsApp UI | none | Feature dead when AI down / flag off / rate-limited | Low | Pure-function fallback + "AI unavailable, showing data summary" banner; unit-tested |
| **G10** | `FilterDetailPage` lacks editors/subscribers display, full action bar, activity feed, derived-view traceability (actions live only in `FilterKebabMenu`) | Detail page becomes the source of truth (master prompt §3.4) | P1 | `FilterDetailPage.tsx` | reuses existing hooks | Detail page is read-thin; users bounce to directory kebab for actions | Med | Add 3 sub-sections reusing existing hooks (`useStarFilter`, `useToggleFilterSubscription`, `useCopyFilter`, `useChangeFilterOwner`, version hooks) |
| **G11** | `task_saved_filters` hook is stub bodies, unused; `task_saved_filters` table exists separately | Confirm tasks filters fully run on `ph_saved_filters` (they do via `useFiltersForProject`); delete stub hook | P2 | `src/modules/tasks/hooks/useSavedFilters.ts` | optional table drop | none | Low | Delete dead stub hook; leave table (ASK before dropping) |
| **G12** | `user_can_see_filter` fn checks only `is_shared/owner/user_id` — looser than `ph_saved_filters_select`'s scoped logic. Used by `fdv_select` for org-visibility derived views | Tighten to mirror the scoped SELECT logic OR document as intentional (derived view is a separate object with its own org gate) | P2 | migration (optional) | RLS fn change | derived-view visibility slightly inconsistent with parent filter | Low | **ASK Vikram** — may be intentional; not a leak (additive gate). Document, don't auto-change |

## Severity rollup
- **P0 (1):** G4 — editor-write RLS gap (security/governance correctness).
- **P1 (7):** G1, G2, G3, G7, G8, G9, G10.
- **P2 (4):** G5, G6, G11, G12.

## Phase C (G2) — BLOCKED, do not de-fork yet (2026-06-19)

Attempted the "safe half" (delete the 2 forked `jqlToFilterState` regex parsers → use the lib parser). **A golden/characterization test against the 9 real saved-filter JQL strings caught a regression that blocks the swap:**

- The lib parser DROPS the `assignee` facet when the JQL value is a Jira account id (`assignee = "712020:..."`) — `translate()` emits the `assignee_account_id` column but `COLUMN_TO_FACET` only maps `assignee_display_name`. The regex fork keeps it (keys on the JQL field name). 2 of the 9 real filters use account-id assignees.
- The obvious fix (map `assignee_account_id → 'assignee'` in the lib) is NOT safe: it changes AllWork, which already uses the lib parser, and AllWork has a **pre-existing assignee id-vs-name inconsistency** — `itemPassesFilters` (`AllWorkToolbar.tsx:382`) matches `f.assignee` against `item.assignee.name` (display name), while `applyServerFilter` matches `assignee_account_id`. Feeding account ids into the client path → empty results.

**Decision:** reverted all lib edits; shipped ONLY a characterization test (`src/lib/jql/__tests__/jqlToFilterState.golden.test.ts`) pinning current behavior + documenting the gap. The de-fork needs a prior dedicated slice to resolve the AllWork assignee id/name inconsistency (likely: resolve account id → display name at parse time, or make `itemPassesFilters` match both). **Needs Vikram decision on which path.** Forward-serializer collapse (C3/C4) remains untouched.

## Cross-cutting notes
- **Canonical adoption is ALREADY good** — the trio is reused across 4 hubs. This audit does NOT call for re-architecting; it calls for finishing wiring (G3/G10), one security fix (G4), one consolidation (G2), one new hub (G8), one fallback (G9), and cleanup (G5/G6/G11).
- **No `auth.jwt()` anti-pattern, no MDT/StoryPoints leaks, no parallel table implementations.** The codebase already follows CLAUDE.md reuse/RLS rules.
- **Three "ASK Vikram" items** before touching: G6 (SearchPage/global-search), G11 (drop task_saved_filters table), G12 (tighten user_can_see_filter). These are out of the Filters-module blast radius and need explicit scope sign-off.

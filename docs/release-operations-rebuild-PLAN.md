# Release Operations Rebuild — PLAN & HANDOVER

**Branch:** `release-operations-rebuild-01`
**Started:** 2026-06-18
**Spec source:** `/Users/vikramindla/Downloads/docs/design-handoffs/release-operations/` (18 files) + `02-CLAUDE-CODE-Release-Operations-Implementation-Prompt.md`
**Visual source of truth (open in browser during build):** `01-artifact-source.html`

> Replace the `/release-hub` (`rh_*`) stack with the new **Release Operations** module using ONLY canonical Catalyst assets. Excludes Incident Management. No new design system/token/table-component/pill/modal-shell/sidebar/chart-lib. Production = green. No Ask-Caty pill in global nav.

---

## LOCKED DECISIONS (from Vikram, 2026-06-18)

1. **Replace strategy:** Probe → propose. Inventory before deletion. (DONE — Phase 0 complete.)
2. **Branch:** `release-operations-rebuild-01` (created).
3. **DB writes:** Propose SQL, Vikram applies. Do NOT call `apply_migration` without explicit per-migration confirmation. Additive only, RLS included.
4. **Cadence:** Surface-by-surface. Build one → show → commit → wait for go.
5. **Schema strategy:** **Extend `rh_*` in place.** Add missing columns + new `rh_*`-prefixed tables. Keep existing data/FKs. Tighten RLS.
6. **Roles:** **Map to existing 4 `app_role`s.** No enum change. admin=full · program_manager=release_manager+change_manager+settings · team_lead=approve signoffs/SOP · user=viewer. Per-row approver identity tracked regardless of role.

---

## PHASE 0 — DISCOVERY (DONE)

### Existing release stacks (3 — only #1 is the target)
| Stack | Routes | Sidebar | DB | Action |
|---|---|---|---|---|
| **Release Hub v2.1 (TARGET)** | `/release-hub/*` | `ReleaseHubSidebar` (badge RH) | `rh_*` | rebuild on top of |
| Releases Mgmt (test) | `/releases/*` → redirect to `/release-hub` | `ReleasesManagementSidebar` (RL) | `tm_*`, legacy `releases` | retiring; leave redirects |
| Product releases | product hub | — | `product_releases`/`product_sprints` | **DO NOT TOUCH (regression risk)** |

Also off-limits: legacy `releases`/`release_vehicles` (portfolio) + `tm_release_signoffs` (test mgmt).

### Existing `rh_*` tables (extend these)
`rh_releases`, `rh_changes`, `rh_change_signoffs`, `rh_change_work_items`, `rh_change_status_history`, `rh_freeze_windows`, `rh_production_events`, `rh_change_activity_log`, `rh_release_test_cycle_links`.
- `rh_changes.release_id` is **1:1 today** — handoff needs **many-to-many** → add `rh_change_release_links`.
- RLS today = wide-open `USING(true)` → tighten in this rebuild.

### MISSING (must add as `rh_*` tables/columns)
SOP templates+steps, `rh_release_brs` (release↔business_requests), `rh_release_sprints`, `rh_release_work_items`, `rh_change_release_links`, `rh_release_notes`, `rh_readiness_checks`, `rh_notify_subscribers`. Plus columns on `rh_releases`/`rh_changes` for the richer lifecycle (health, readiness_pct, target_env, release_type, manager/owner/qa/uat ids, change_type, deployment_category, risk, window_start/end, override fields, etc.).

### Canonical components (USE THESE — handoff names were wrong)
| Need | Canonical path | Notes |
|---|---|---|
| Table | `src/components/shared/JiraTable/` (`JiraTable`, `cells.tsx` make*Cell, `editors.tsx` make*EditCell) | registry: `src/canonical/field-registry.ts` (FIELD_ID); row expand via `expandedRowIds`/`onToggleRowExpanded`; no built-in mobile card fallback |
| Status pill | `src/components/catalyst-detail-views/shared/sections/CatalystStatusPill.tsx` (interactive) + `StatusLozenge.tsx` (read-only) + `statusPalette.ts` | `STATUS_BG`: success `#94C748`, inprogress `#8FB8F6`, default `#DDDEE1`, moved `#F3D664`, new `#B8ACF6`, removed `#FD9891`; text `#292A2E`; `toStatusCategory()` from `src/components/ads` |
| Type icon | `src/components/shared/JiraIssueTypeIcon.tsx` | |
| User/approver picker | `src/components/EditableAssignee/EditableAssignee.tsx` + `AssigneeOption` | reuse for approver fields |
| Date | `src/components/shared/CatalystDueDateField/` + `makeDateEditCell` | wraps `@atlaskit/datetime-picker` |
| Description | `src/components/catalyst-detail-views/shared/sections/Description/Description.tsx` (Tiptap, CANONICAL) | NOT legacy `CatalystDescriptionSection` |
| Activity | `src/components/catalyst-detail-views/shared/sections/CatalystActivitySection.tsx` + `catalyst-ds` `ActivityPanel` | reads `ph_comments`+`ph_activity_log` |
| Notifications | `useNotificationsNew` is READ/MARK only | creation is server-side → notify-on-status-change needs **DB trigger or edge function** writing `notifications` rows + Caty chat |
| Modal/Tabs | `@atlaskit/modal-dialog`, `@atlaskit/tabs` directly | |
| Page header | `GlobalPageHeader` (breadcrumb) / `HubPageHeader` (hub) | |
| Sidebar | `SidebarBase.tsx` (`SidebarConfig`/`SidebarSection`/`SidebarMenuItem`) | ReleaseHubSidebar feeds it |

### Reusable existing release-hub assets (don't rebuild from zero)
8 command-center hooks (`src/modules/command-center/hooks/*`: KPIs, ReleaseHealth, Activities (realtime), DefectTrends, QualityGates, TestProgress, TeamPerformance, Milestones); `src/services/release-hub.service.ts` (9 CRUD services); `src/hooks/useReleaseHub.ts` (query-key registry + mutations); existing modals/drawers/badges in `src/components/releasehub/`.

### Roles
`app_role` enum = `admin|program_manager|team_lead|user`. `user_roles` table. Canonical RLS admin check:
```sql
EXISTS (SELECT 1 FROM public.user_roles ur WHERE ur.user_id = auth.uid() AND ur.role = 'admin'::app_role)
```
SECURITY DEFINER helper pattern (avoid RLS recursion): see `chat_is_member` in `20260609000100_fix_chat_rls_membership_recursion.sql`.

### Link-target keys
products(id, code) · business_requests(id) · sprints/anchor_sprints(id, code) · ph_issues(issue_key text PK, project_key).

---

## BUILD ORDER (surface-by-surface; each = data hook + empty/loading/error + ADS audit + dark check + scoped commit)

- **Phase 1 — Migrations** (propose SQL, Vikram applies): extend `rh_releases`/`rh_changes` columns; new tables `rh_sop_templates`, `rh_sop_template_steps`, `rh_sop_steps`, `rh_release_brs`, `rh_release_sprints`, `rh_release_work_items`, `rh_change_release_links`, `rh_release_notes`, `rh_readiness_checks`, `rh_notify_subscribers`; indexes (per impl prompt §11); tighten RLS to user_roles pattern + SECURITY DEFINER helpers; generate TS types.
- **Phase 2 — Sidebar + routes:** relabel `ReleaseHubSidebar` → "Release Operations"; sections per handoff (Overview / [Backlog, Release Kanban, Production Events, Calendar] / [Change Records, SOP Templates, Sign-off Queue, Freeze Windows] / Settings footer). Remove Triage + Compare. Wire routes in `FullAppRoutes.tsx`. Keep legacy redirects.
- **Phase 3:** Overview (KPI cards drill-down + **Pending Approvals panel w/ approver face avatars**).
- **Phase 4:** Backlog (Releases JiraTable, all cols/filters/states) + Create Release modal (validation).
- **Phase 5:** Release detail (9-step tracker + tabs: Overview/Scope/Readiness/Changes/Sign-offs/Release Notes/Production Events/Audit + Notify list).
- **Phase 6:** Release Kanban (reuse `src/modules/kanban`).
- **Phase 7:** Change Records list + Create/Map Change modal (Approvers + Notify fields).
- **Phase 8:** Change detail (tracker + tabs incl. **SOP execution table** w/ step lifecycle + evidence + expand).
- **Phase 9:** SOP Templates list + apply-to-change.
- **Phase 10:** Production Events list + detail (auto-gen on prod release completion).
- **Phase 11:** Calendar (release/change/freeze/prod lanes + conflict tint + click→drawer).
- **Phase 12:** Sign-off Queue + approval window.
- **Phase 13:** Freeze Windows list + create + conflict detection.
- **Phase 14:** Settings (release/change types, envs, approval policies, readiness rules, numbering, SOP step types, notification policy).
- **Phase 15:** Lifecycles + scheduling guards (release 9-stage, change 9-stage, SOP step) — enforced in service + RLS/API.
- **Phase 16:** Notify-on-status-change (DB trigger/edge fn → notifications + Caty chat) + change-number→work-item-history label + work-item Release&Change Traceability panel (Project/Product work-item view).
- **Phase 17:** AI advisory surfaces (CATY: risk summary, missing-SOP, notes draft) — advisory only, never auto-approve/execute.
- **Phase 18:** Tests (lifecycle transitions, scheduling guards, permission checks, e2e) + parity report vs `01-artifact-source.html` + acceptance checklist `14`.

## GUARDRAILS (every phase)
ADS `var(--ds-*)` tokens only; 3-colour status guardrail (grey/blue/green), blue only for +Create/links/active-nav, purple only AI; `JiraTable` for all lists; `CatalystStatusPill`/`StatusLozenge` for status; light mode no-regress + dark works; NO Incident Management; stage explicit file paths only (never `git add -A`); `git status` before each commit; don't touch `product_releases`/legacy `releases`.

---

## PROGRESS LOG
- 2026-06-18: Phase 0 discovery DONE. Branch created. Plan written.
- 2026-06-18: Phase 1 migration APPLIED (`20260618120000_release_operations_schema.sql`). 3 SECURITY DEFINER RLS helpers (rh_user_has_role/rh_is_manager/rh_is_approver); ALTER rh_releases(+11)/rh_changes(+11)/rh_freeze_windows(+5)/rh_production_events(+14); 10 new tables (rh_sop_templates, rh_sop_template_steps, rh_sop_steps, rh_release_brs, rh_release_sprints, rh_release_work_items, rh_change_release_links, rh_release_notes, rh_readiness_checks, rh_notify_subscribers); 24 indexes; updated_at triggers; RLS tightened on rh_releases/rh_changes/rh_change_signoffs/rh_freeze_windows/rh_production_events (read-all + manager-write; signoff/SOP approver+owner carve-outs). types.ts regenerated. Security advisors: no new ERROR; helpers clean (search_path set). Verified via execute_sql (10 tables, helpers, policies present).
  - OPEN (Vikram decisions, carried from proposal): (1) release-level sign-offs have no table — rh_change_signoffs is change-only; add item_type/item_id or new rh_signoffs later. (2) rh_release_issues vs new rh_release_work_items coexist — reconcile in hooks. (3) status stays TEXT, lifecycle enforced in service (Phase 15). (4) child/log tables left permissive (not retightened). (5) low-pri: REVOKE anon EXECUTE/SELECT on rh helpers/tables (module-wide).
- 2026-06-18: Phase 2 DONE. Sidebar relabelled → "Release Operations" (badge RO). Sections per handoff §6: Overview / [Releases, Production Events, Calendar] / [Change Records, SOP Templates, Sign-off Queue, Freeze Windows] / Settings footer. Removed Triage + Compare (routes → redirect to /overview). Renamed /release-hub/command-center → /release-hub/overview (legacy redirect kept). New routes: /overview, /calendar, /sop-templates, /settings. Calendar/SOP/Settings → `ReleaseOpsComingSoonPage` placeholder (ads/EmptyState) until Phases 9/11/14. Repointed all 6 hub-entry hrefs (SidebarBase HUB_ITEMS, HubSwitcher, hubs.ts, HomeSidebar, MobileNavigationMenu) + test → /overview. CatalystShell hub default → /overview. tsc: 0 new errors in touched files. ADS audit: PASS. Note: handoff §52 has NO Backlog/Kanban nav items — Releases list = backlog; Kanban = view toggle (Phase 6).
- 2026-06-18: Phase 3 DONE. Overview page (`/release-hub/overview`, CommandCenterPage.tsx) rebuilt ADS-clean (was --cp-* tokens + raw-UUID approver). Title → "Release Operations Overview". 4 KPI drill-down cards (Active Releases / Open Changes / Pending Approvals / Production Events → respective lists). **Pending Approvals panel** (priority §3b): `ads/Avatar` face avatar + approver name + role chip + change number/title + "N waiting" + Review→ action. New enriched hook `usePendingApprovals` (useReleaseHub) = pending rh_change_signoffs + batch profile join (full_name/avatar_url); null approver → "Unassigned" (no lie). Supporting panels: Release Status + Recent Production Events. StatusLozenge for pills. ADS audit PASS; tsc clean. Loading+empty states present.
  - GAP (flagged): error states not rendered per panel — query error currently falls through to empty ("No X"), mildly misleading. Add error+retry per §08 in a polish pass.
- 2026-06-18: Phase 4a DONE. Releases list (`/release-hub/releases`, AllReleasesPage.tsx) rebuilt on canonical `JiraTable` + ADS tokens (was hand-rolled `<table>` + cards + --cp-* = P0 canonical-table violation). Columns: Release (name+version+jira_key), Status (StatusLozenge), Health (pill), Env, Type, Target (planned_release_date∥target_date), Changes count, Updated (relative). New hook `useReleasesList` queries rh_releases directly (richer cols the summary view lacks) + change count via legacy rh_changes.release_id. Search + status filter tabs + loading/empty/error states (reused releasehub EmptyState/ErrorState). Row click → release detail. ADS audit PASS; tsc clean.
  - DEFERRED to 4b: (1) ADS-clean Create Release modal rebuild with richer fields (release_type, target_env, product, managers, planned dates) + validation + extended releaseService.create — currently reuses existing CreateReleaseModal (legacy fields, --cp tokens). (2) Faceted column filters (product/health/env/type) + sort wiring (columns marked sortable but uncontrolled; data pre-sorted updated_at desc). (3) Board view toggle → Phase 6.
- 2026-06-18: Phase 4b DONE. CreateReleaseModal rebuilt on @atlaskit/modal-dialog + @atlaskit/select + @atlaskit/datetime-picker + @atlaskit/textfield + @atlaskit/button/new (was hand-rolled overlay + shadcn Popover/Calendar + --cp-*). Fields: Name*, Release type* (regular/minor/major/hotfix/emergency), Target env* (qa/beta/staging/production), Product (from products), Version, Planned start, Planned release*, Release manager + Product owner (from profiles). Validation gates name/type/env/planned-release; inline errors on submit. target_date (NOT NULL) mirrored from planned_release; new releases → status 'draft'. Extended releaseService.create payload type with the new optional fields. ADS audit PASS; tsc clean.
  - NOTE: deferred QA lead / UAT lead pickers (data-contract lists them) — add when detail-view sign-off wiring needs them. 'draft' status renders grey via StatusLozenge default (To-Do category) — OK.
- 2026-06-18: Phase 5a DONE. New `ReleaseDetailPage` (/release-hub/:releaseId) replaces mock-data ReleaseDashboardV5Page (which used bg-slate-50 + mockDashboardData). Real shell: back link, header (name+version+jira_key, status lozenge / terminal badge, health pill), meta row (type/env/target/changes/readiness), **9-step lifecycle tracker** (draft→planned→in_readiness→ready_for_signoff→approved→scheduled→deploying→monitoring→completed; legacy status aliasing; terminal rolled_back/cancelled badge), and **8-tab @atlaskit/tabs scaffold**. Overview tab populated (status/managers/planned dates/source/description) from useRelease(id) + inline change-count query. Loading (Spinner)/not-found/error states. ADS audit PASS; tsc clean. Route repointed; old ReleaseDashboardV5Page.tsx now orphaned (dead mock — cleanup candidate).
  - DEFERRED to 5b+: tab contents — Scope (rh_release_brs/sprints/work_items), Readiness (rh_readiness_checks), Changes (rh_change_release_links), Sign-offs (rh_change_signoffs + approval window), Release Notes (rh_release_notes), Production Events (rh_production_events), Audit (rh_release_activity_log). Plus **Notify list** CRUD (rh_notify_subscribers) on header. Release manager/product owner show "Assigned" not the name (no profile join yet).
- 2026-06-18: Phase 5b DONE. Release detail tabs wired + Notify list. New `ReleaseDetailTabs.tsx` (ScopeTab/ChangesTab/SignoffsTab/NotifyList) + 6 hooks in useReleaseHub (useReleaseScope, useReleaseChanges, useReleaseSignoffs, useNotifySubscribers, useAdd/RemoveNotifySubscriber). Scope tab = BRs/Sprints/Work items sub-sections (read, empty states). Changes tab = linked changes (legacy release_id ∪ M:N rh_change_release_links). Sign-offs tab = signoffs for release's changes + approver avatar/name. **Notify list** (header, below meta): subscriber chips (avatar+name+remove) + Add via @atlaskit/select; full CRUD on rh_notify_subscribers. All read paths have loading+empty states; tables empty now so mostly empty states. ADS audit PASS; tsc clean. Mock dashboard + features/release-dashboard deleted (commit 5e4beb68f).
  - DEFERRED to 5c: Readiness / Release Notes / Production Events / Audit tab contents. Header still shows release_manager/product_owner as "Assigned" (no name join). Notify-on-status-change side effect = Phase 16; sign-off approval window = Phase 12.
- 2026-06-18: Phase 5c DONE — Release detail COMPLETE (all 8 tabs real). Added ReadinessTab (rh_readiness_checks + pass/fail/pending/na pills), ReleaseNotesTab (rh_release_notes latest, AI-drafted badge, read-only), ProductionEventsTab (rh_production_events by release_id + result badge), AuditTab (rh_release_activity_log + actor avatar + relative time). 4 new hooks (useReadinessChecks, useReleaseNotes, useReleaseProductionEvents, useReleaseAudit). Removed PlaceholderTab. All read + loading + empty states. ADS audit PASS; tsc clean.
  - DEFERRED (cross-phase, not detail-blocking): release_manager/product_owner name join on header (still "Assigned"); Release Notes generate/edit (AI = Phase 17); sign-off approval window (Phase 12); notify-on-status-change side effect (Phase 16); production event auto-gen on completion (Phase 10).
- 2026-06-18: Phase 6 DONE. Release Kanban via the CANONICAL project-module board (`KanbanBoardShell` + `BoardAdapter`), NOT ideation (`src/modules/kanban` was a wrong first pick — Vikram corrected; ideation board is hardwired to score/epic/platform/StatusId). New `releaseBoardAdapter.tsx` mirrors `incidentHubBoardAdapter`: ReleaseListRow → CanonicalBoardIssue (name→summary, jira_key, health→primary lozenge, target_env→secondary lozenge, target-date+change-count→meta, Rocket resolveIcon); 9 lifecycle-stage columns with legacy-status aliases; drag→`onStatusChange`→useUpdateReleaseStatus. AllReleasesPage: Table/Board view toggle (List/LayoutGrid) in header; board view mounts `<KanbanBoardShell adapter hideTitleHeader />` (its own toolbar owns search); table view keeps status tabs + search. ADS audit PASS; tsc clean.
  - NOTE: drag moves a release to ANY stage (no lifecycle guard yet — Phase 15 enforces valid transitions + scheduling/freeze gates). Terminal rolled_back/cancelled have no column (cards hidden on board) — acceptable. Board filters/group-by/assignees passed empty (releases have no assignee facet).
- 2026-06-18: Phase 7a DONE. Change Records list (`/release-hub/changes`, AllChangesPage.tsx) rebuilt on JiraTable + ADS tokens (mirrors Releases list; was hand-rolled table + --cp). New `useChangesList` hook (rh_changes direct w/ richer cols + release-name join). Columns: Change (chg_number+title), Status (StatusLozenge), Risk (pill), Type, Env, Category, Window (window_start∥deployment_date), Release, Updated. Search + status filter tabs (new/in_uat/in_beta/in_production) + states. Rows NOT clickable yet (change detail = Phase 8). New change → existing CreateChgModal. ADS audit PASS; tsc clean.
  - DEFERRED to 7b: ADS-clean Create/Map Change modal rebuild with richer fields (change_type, target_env, deployment_category, risk, window dates) + **Approvers** (sign-off rows) + **Notify** fields + extended changeService.create + Map-to-release flow.
- 2026-06-18: Phase 7b DONE. CreateChgModal rebuilt on @atlaskit modal/select/datetime-picker/textfield/textarea/button (was hand-rolled overlay + shadcn + --cp). Fields: Title*, Change type* (standard/normal/emergency/hotfix), Risk* (low/medium/high/critical), Target env*, Deployment category, Window start/end, Map to release, Description. **Approvers** = repeatable user+role rows → each inserts a pending rh_change_signoffs row (signoff_role+stage+assigned_to) post-create. **Notify** = multi-select users → rh_notify_subscribers rows (item_type='change'). chg_number auto-generated sequential CAT-CHG-<nnnn> (count of source='catalyst' + 1). Extended changeService.create payload type. Validation gates title/type/env/risk. ADS audit PASS; tsc clean. Phase 7 COMPLETE.
  - NOTE: chg_number sequencing is count-based (small concurrency-collision risk on the UNIQUE constraint — acceptable for now; move to a DB sequence/trigger if it bites). Approver/notify inserts are best-effort after change create (not transactional).
- 2026-06-18: Phase 8a DONE. New `ChangeDetailPage` (/release-hub/changes/:changeId) — header (chg_number + title + status/terminal badge + risk pill), meta (type/env/category/window), Notify list (item_type='change'), **9-step change lifecycle tracker** (draft→assessing→ready_for_approval→approved→scheduled→implementing→validating→implemented→closed; legacy new/in_uat/in_beta/in_production aliased; terminal failed/rolled_back/cancelled badge), 5-tab @atlaskit/tabs (Overview / SOP / Work items / Sign-offs / Activity). Overview/Work items/Sign-offs/Activity read nested useChange(id) payload (rh_change_work_items/signoffs/status_history). Route added; AllChangesPage rows now clickable → change detail. ADS audit PASS; tsc clean.
  - DEFERRED to 8b: **SOP execution table** (rh_sop_steps — expandable step rows, step lifecycle pending/in_progress/done/blocked/skipped/failed, evidence_url, branch/commit cells, mark-done actions) — the SOP tab is a placeholder. Sign-offs tab is read-only (approve/reject window = Phase 12).
- 2026-06-18: Phase 8b DONE — Change detail COMPLETE. New `SopExecutionTab` (rh_sop_steps): expandable step rows — step_no + title + type/owner, per-step status Select (pending/in_progress/done/blocked/skipped/failed → useUpdateSopStep sets started_at/completed_at), evidence link, status pill, "N of M done" progress; expand reveals description/environment/branch/commits/script/command/expected+actual result/blocker. New hooks useSopSteps + useUpdateSopStep. Wired into Change detail SOP tab. ADS audit PASS; tsc clean. Phase 8 COMPLETE.
  - NOTE: SOP steps populated by applying an SOP template (Phase 9) or manual add (later); empty state points to Phase 9. Step status change has no lifecycle guard yet.
- 2026-06-18: Phase 9 DONE. SOP Templates + apply-to-change (closes the SOP loop with Phase 8b). New `SopTemplatesPage` (/release-hub/sop-templates, replaces placeholder) — JiraTable of rh_sop_templates (name+desc, category, env, step count, updated) + New template. New `CreateSopTemplateModal` (@atlaskit) — name/category/env/description + repeatable steps (title/type/@atlaskit Toggle mandatory) → inserts rh_sop_templates + rh_sop_template_steps. Apply: `ApplyTemplateBar` in Change detail SOP tab (template Select + Apply) → `useApplySopTemplate` copies rh_sop_template_steps → rh_sop_steps (status pending, step_no offset by existing). New hooks: useSopTemplates, useCreateSopTemplate, useApplySopTemplate. ADS audit PASS; tsc clean. End-to-end: create template w/ steps → open change → apply → execute steps in SOP table.
  - DEFERRED: template detail/edit page (edit steps after create) — templates are create-only for now. Apply has no dedup (applying same template twice appends duplicate steps).
- 2026-06-18: Phase 10 DONE. ProductionEventsPage rebuilt on JiraTable + ADS (was hand-rolled --cp). New `useProductionEventsList` hook (rh_production_events richer cols + snapshots). Columns: Event (title + release/change key), Type, Env, Result (badge), Deployed, By. Row click → @atlaskit detail modal (meta + snapshot counts: work items / BRs / commits / SOP evidence / approvers). **Auto-gen**: `useUpdateReleaseStatus` inserts a production event when a release with target_env='production' reaches 'completed' (idempotent by release_id; result=SUCCESS, event_type='release'). ADS audit PASS; tsc clean.
  - INCIDENT (recovered, no loss): mid-Phase-10 the harness switched the working-tree checkout from release-operations-rebuild-01 to `main` (likely the cleanup-chip worktree). All commits intact on the branch (tip 5176a8128); `git checkout release-operations-rebuild-01` restored everything. Pushed branch to origin as a safety checkpoint. LESSON: parallel worktree/chip sessions can flip the main checkout — verify `git rev-parse --abbrev-ref HEAD` if files look reverted; push periodically.
  - DEFERRED: auto-gen does not yet populate the jsonb snapshots (render as '—'); snapshot capture is a follow-up. Manual production-event creation not exposed (auto-only per spec).
- 2026-06-18: Phase 11 DONE. New `ReleaseCalendarPage` (/release-hub/calendar, replaces placeholder) — month grid (prev/next nav, weekday header, ≥96px day cells, today tint, out-of-month dim), 4 lanes via `useReleaseCalendar` hook: release (blue, planned_release∥target_date → /release-hub/:id), change (teal, window_start∥deployment_date → change detail), freeze (red, multi-day span start_date..end_date), production (green, deployed_at). **Conflict tint** (danger bg) on days where a release/change falls inside a freeze window. Event chips click → navigate to detail. Legend. ADS audit PASS; tsc clean.
  - NOTE: change drag/reschedule on calendar not built (click-to-open only, per net-new-surface scope); >4 events/day collapse to "+N more" (no day-expand popover yet).
- 2026-06-18: Phase 12 DONE. SignOffQueuePage rebuilt on ADS (was --cp) — aggregates all pending approvals via usePendingApprovals (approver face avatar + change + role + wait time). Review → `ApprovalWindow` @atlaskit modal: comment textarea, Approve (comment optional → useApproveSignoff), Reject (comment REQUIRED, validated → useRejectSignoff). Makes the Overview Pending Approvals panel + Phase-7b Approvers actionable end-to-end. ADS audit PASS; tsc clean.
  - DEFERRED: Change detail Sign-offs tab still read-only (could mount the same ApprovalWindow — follow-up). "Gates production scheduling" (can't schedule a prod change/release until required approvers approve) is a lifecycle guard → Phase 15. RLS already restricts signoff UPDATE to approver roles + assigned_to.
- 2026-06-18: Phase 13 DONE. FreezeWindowsPage rebuilt on JiraTable + ADS (was --cp + shadcn Dialog). New `useFreezeWindowsList` hook — rh_freeze_windows + **conflict detection**: counts active releases/changes scheduled inside each window's date range matching its env (env='all'/null matches any). Columns: Freeze window (name+reason), Env, Window (start–end), Status, Conflicts (red AlertTriangle badge when >0), delete action. New `CreateFreezeWindowModal` (@atlaskit) — name/start*/end* (end≥start validated)/env scope/reason → useCreateFreezeWindow (extended payload type w/ target_env/applicability/status). Per-row delete via useDeleteFreezeWindow (confirm). ADS audit PASS; tsc clean.
  - NOTE: conflict count is advisory display only — it does NOT block scheduling yet (enforcement = Phase 15). product-scoped freeze (applicability='product' + product_id) not exposed in the create modal (defaults to 'all').
- 2026-06-18: Phase 14 DONE. New `ReleaseSettingsPage` (/release-hub/settings, replaces last placeholder) — **read-only reference** of the module's in-effect configuration: release types, change types, environments, deployment categories, risk levels, approval roles, SOP step types, 9-stage release + change lifecycles, change-numbering (CAT-CHG-nnnn), notification policy. Rendered as chip sections + an info banner. Deleted the now-orphaned `ReleaseOpsComingSoonPage` (all 3 placeholder routes — calendar/sop-templates/settings — now real). ADS audit PASS; tsc clean.
  - DECISION (logged): Settings is read-only because every type/env/policy is a hardcoded enum consumed by the create modals + trackers. Editable settings would need (a) config tables (migration → propose-SQL), AND (b) rewiring every consumer to source enums from the DB — a large separate effort that doesn't fit Phase 14. The reference page surfaces the contract honestly without a fake-editable UI. Revisit if Vikram wants editable config.
  - NEXT: Phase 15 — Lifecycles + scheduling guards (release 9-stage, change 9-stage, SOP step) enforced in service + RLS/API (valid transitions, freeze-conflict block, approval-gating, production-needs-change rule).

# 10 — Stubs & Drivers Audit (TestHub)

**Feature:** CAT-TESTHUB-PROD-20260703-001 · **Agent:** stubs/drivers hunter · **Date:** 2026-07-03
**Scope grepped:** `src/pages/testhub` (all subdirs), `src/components/testhub`, `src/components/test-plans`, `src/components/test-cycles`, `src/features/test-cycles`, `src/hooks/testhub`, `src/hooks/test-cycles`, `src/hooks/test-cases`, `src/hooks/test-management`, `src/components/catalyst-detail-views/test-{case,cycle}`, `src/components/releases/test-*`, `src/components/layout/TestHubSidebar.tsx`, `src/hooks/useTestHubTimeline.ts` — 175+ ts/tsx files.
**Patterns hunted:** TODO/FIXME/PLACEHOLDER, mock/stub, hardcoded arrays, `Math.random`, setTimeout-fake-loading, disabled buttons, `onClick={()=>{}}`, console.log-only handlers, static demo data, unwired props, commented-out queries, "wire later", UI-that-writes-nowhere, missing routes.

---

## Headline verdict

The **routed** TestHub (`/testhub/*`, 15 routes in `src/routes/FullAppRoutes.tsx:666-699`) is **substantially real** — Supabase reads AND writes verified on every core surface (evidence table at bottom). The fear of "built on stubs" is concentrated in:

1. **Three live P0 defects** on routed surfaces (fake AI endpoint, broken defect-row navigation, silent field-drop in Create Cycle).
2. **A large DEAD legacy generation** (`src/pages/releases/Test*`, `src/components/test-cycles/**`, `src/hooks/test-cycles/**`, `src/features/test-cycles/**`, `src/components/test-plans/**`, `src/components/releases/test-*`) that is full of mock data, `Math.random` row generators, console.log-only handlers, and a broken apply-mutation — but **none of it is reachable from any route today**. `/releases/*` redirects to `/release-hub/overview` (`FullAppRoutes.tsx:897`); zero routed page imports these families (verified by importer grep). It is a landmine inventory, not a live-lie inventory — until someone routes it.

---

## Severity-ranked findings table

Severity: **P0** = live route, user-visible wrong behavior · **P1** = live route, degraded/misleading · **P2** = dead code containing stubs (landmine if revived) · **P3** = cosmetic/benign.

| # | Sev | file:line | What is stubbed | User-visible symptom | Wiring needed |
|---|-----|-----------|-----------------|----------------------|---------------|
| 1 | **P0** | `src/hooks/test-management/useAIGeneration.ts:74` | Invokes edge function `ai-generate-test-cases` — **function does not exist**. `ls supabase/functions/` shows only `ai-generate-story-test-cases` (no `ai-generate` match in `supabase/config.toml`). | "Generate with AI" on `/testhub/repository` (`AIGenerateTestCasesDialog`, mounted `RepositoryPage.tsx:1006`) **always errors**. Persistence path after generation IS real (`RepositoryPage.tsx:599-624` → `useCreateTestCase`), so only the AI call is dead. | Create `supabase/functions/ai-generate-test-cases/` (or rename invoke to reuse `ai-generate-story-test-cases` with prompt-mode branch) + deploy. |
| 2 | **P0** | `src/modules/project-work-hub/adapters/defectsDataSource.ts:148-149` | `onOpenItem: (_key, id) => window.location.assign('/testhub/defects/${id}')` — **no `/testhub/defects/:id` route exists** (`grep "defects/:" FullAppRoutes.tsx` → 0 matches; only `/testhub/defects` at line 683). File's own header admits it: "falls back to no-op if route absent" (line 6-7) — but it doesn't no-op, it hard-navigates to an unmatched URL. | Clicking any defect row on `/testhub/defects` navigates to a dead URL (catch-all/404), full page reload, work lost. | Add `/testhub/defects/:id` detail route (CatalystDetailPanel w/ tm_defects view) OR open the canonical QA-Bug modal (per memory: defect creation = CreateStoryModal isDefect branch); until then make row-click a real no-op. |
| 3 | **P0** | `src/pages/testhub/cycles/CyclesPage.tsx:257,261,263,266 (state) → 287-301 (submit)` | Create Cycle modal renders **Release select (line 367), Owner select (376), Tags editor (401), Assignees checkbox tab (508)** but `handleCreateCycle` sends only `project_id, name, description, sprint_id, planned_start_date, planned_end_date` + case ids. `releaseId`, `ownerId`, `tags`, `assigneeIds` are **collected and silently dropped** — classic "UI renders but writes nowhere". | User fills Release/Owner/Tags, ticks Assignees, clicks Create → cycle saves without any of it; no error, no hint. Assignee tab is pure theater. | Extend `useCreateCycle` input + `tm_test_cycles` columns (owner/release) and write `tm_cycle_scope.assigned_to` for chosen assignees; or delete the fields until wired. |
| 4 | **P1** | `src/pages/testhub/reports/lab/ReportCanvas.tsx:853` | `if (!Renderer) return <ReportEmptyState message="Renderer not implemented" hint="…coming in next slice" />` | Defensive only today: all **26/26** `REPORT_REGISTRY` entries are `status: 'wired'` (`report-registry.ts`, uniq count = 26 wired). Symptom appears only if a slug is registered without a body. | None now; keep gate. |
| 5 | **P2** | `src/hooks/test-cycles/useAssignmentTable.ts:15-74, 203-206` | `generateMockAssignments()` — **85 fake rows via `Math.random`** (fake testers "Ahmed S./Sara M./Omar K./Fatima R.", fake TC-1001+ keys, random statuses/priorities/defect counts) returned **from the query's catch block** — any Supabase error is masked with fabricated data. Real path also hardcodes `testType: 'functional'` (line 194 — overriding the computed value from lines 169-173, dead logic), `executionTimeMinutes: null`, `executedAt: null`, `defectCount: 0` (196-199). | UNREACHABLE today: sole consumer chain `AssignmentTableView` → `CycleTableView` → `CycleCommandCenter.tsx` which is **not routed** (importer grep: only `CycleTabNavigation`, `cycleBoardAdapter`, generated usage-map). If revived: DB outage silently shows a fully-populated fake table. | On revival: delete mock fallback (throw → SectionMessage per silent-query-error sweep pattern); wire testType/execution fields to `tm_test_runs`. |
| 6 | **P2** | `src/hooks/test-cycles/useSmartAssignment.ts:93-97, 30-35, 74-79, 112` + `src/lib/assignment-algorithm.ts:174` + `src/hooks/test-cycles/useApplyAssignment.ts:27,35-38` | Smart Assignment: fabricated inputs — `estimatedDuration: 15`, `module: 'General'`, `dailyCapacity: 5`, `skills: {}`, `availability: []`, cycle context hardcoded to "starts today, ends +10 days, remainingDays: 10" regardless of real cycle dates; cosmetic 200ms `setTimeout` "calculating". **Plus a real wiring bug:** `distributeTests` sets `assignment.testCaseId = test.id` (= `tm_cycle_scope.id`), but `useApplyAssignment` looks rows up via `.eq('test_case_id', assignment.testCaseId)` → `maybeSingle()` finds nothing → every row `console.warn`-skipped → **Apply writes 0 rows** while `SmartAssignmentModal.tsx:57-58` fires `onApply?.(assignments.length)` reporting full success. | UNREACHABLE (only mount: `SmartAssignmentModal` ← unrouted releases pages). If revived: "AI-balanced" assignment that persists nothing yet toasts success — worst-case fake integration. | Fix id semantics (pass scope.id and update by `id`), derive capacity/duration/dates from real tables, drop the fake delay. |
| 7 | **P2** | `src/components/test-plans/dialogs/tabs/TeamTab.tsx:20-26, 124` | `mockTeamMembers` — 5 hardcoded fake people with ids `'1'..'5'`, fake capacity/specialty; plus a **hardcoded static "AI Team Recommendation"** paragraph ("consider adding Omar Hassan…") styled as if AI-generated. Selected mock ids flow into `owner_id`/`team_members` and are **submitted to `tm_test_plans`** (`CreateEditTestPlanDialog.tsx:100-166` → `useTestPlansG26.ts` real inserts) → invalid FK/garbage rows. | UNREACHABLE: dialog mounted only by `src/pages/releases/TestPlansPage.tsx` (unrouted). If revived: fake staff list, fake AI advice, DB writes with bogus ids. | Replace with `profiles` query (pattern exists in `CyclesPage.tsx:274-281`); gate/remove the AI card. |
| 8 | **P2** | `src/components/test-plans/dialogs/tabs/ScopeTab.tsx:2 (self-labelled "Placeholder"), 39 (search input, no onChange/no data), 43-45 (static "Requirements … will appear here")` + `dialogs/hooks/useCreateEditPlanForm.ts:152-157` | Requirements selector is an empty shell: nothing lists, nothing can be added, so `in_scope_ids` is permanently `[]`; coverage ring uses **fake formula** `existingTests = floor(inScope * 0.7)` (comment: "Mock calculation"). | UNREACHABLE (same unrouted dialog). If revived: scope tab that can never scope; invented coverage %. | Wire to `ph_issues`/requirements per release + real `tm_requirement_links` counts (live pattern exists in `src/hooks/test-cases/useRequirementLinks.ts`). |
| 9 | **P2** | `src/components/releases/test-cycles/CreateCycleModal.tsx:47-56` | `mockTestCases` tc-1…tc-8 hardcoded array as the test-case picker; `onCreateCycle` prop just bubbles data up (no DB write in modal). | DEAD: superseded by `CreateCycleModalEnhanced` ("with real data", used by unrouted `TestCyclesPage.tsx:557`) and by the real local modal in routed `CyclesPage.tsx:253`. Only export is via barrel `index.ts`. | Delete file. |
| 10 | **P2** | `src/components/releases/cycle-command-center/CycleCalendarView.tsx:39,45,49` | Calendar handlers are **console.log-only**: `console.log('Event clicked:'…)`, `('Reschedule event:'…)`, `('View details:'…)`. | UNREACHABLE (CycleCommandCenter unrouted). If revived: clicking calendar events does nothing visible. | Wire to cycle detail nav + `RescheduleModal` (component exists at `src/components/test-cycles/calendar/RescheduleModal.tsx`). |
| 11 | **P2** | `src/components/releases/test-case-detail/QuickActionsBar.tsx:47-109` | All quick actions are **toast-only theater**: "Starting test execution…", "Test case duplicated as X-copy" (no insert), "Exporting test case as PDF…" (no export), "Opening defect creation form…", "Opening item linker…", "Opening version history…" — zero mutations, zero navigation. | DEAD: no importers outside its own folder (grep confirmed; `ProjectBriefingView` has a different QuickActionsBar). | Delete folder or rebuild against routed CaseDrawer. |
| 12 | **P2** | `src/components/releases/test-case-detail/TestCaseLinksAttachments.tsx:69, 214` | "Attachments — placeholder for future implementation" / renders "Attachment uploads coming soon". | DEAD (no importers). Note the **routed** ExecutionPage has REAL attachment upload to storage (`ExecutionPage.tsx:505-526`, bucket path `test-runs/<run.id>/…`). | Delete; routed path already supersedes. |
| 13 | **P2** | `src/pages/testhub/defects/DefectsPage.tsx` (468 lines, 0 supabase reads/writes — table via `useDefects` hooks) | Orphaned older Defects page; route `/testhub/defects` uses `src/pages/testhub/DefectsPage.tsx` (canonical BacklogPage wrapper) instead. No importer routes to `defects/DefectsPage.tsx`. | None (dead). Confuses future readers/greps. | Delete (Move-not-copy rule: old generation should have been removed). |
| 14 | **P2** | `src/pages/testhub/FilterDetailPage.tsx` | 10-line page not referenced by any route (`/testhub/filters/:filterId` → `FilterPreviewPage`, `FullAppRoutes.tsx:698-699`). | None (dead). | Delete or route. |
| 15 | **P3** | `src/components/test-plans/dialogs/tabs/BasicInfoTab.tsx:54` | `setTimeout(…, 3000)` — auto-dismiss of "Dates auto-filled from release" message. | Benign UX timer, not fake loading. | None. |
| 16 | **P3** | `src/pages/testhub/cycles/ExecutionPage.tsx:508` | `Math.random()` used only for collision-safe attachment filenames. | Benign. | None. |
| 17 | **P3** | `src/components/test-plans/dialogs/components/CoverageRing.tsx:30` / `src/components/test-cycles/assignment-table/TableToolbar.tsx:54` | setTimeout = animation delay / debounce respectively. | Benign (and dead code anyway). | None. |

---

## Dead legacy generation — full inventory (P2 umbrella)

Verified unreachable: `/releases/*` → `<Navigate to="/release-hub/overview" />` (`FullAppRoutes.tsx:897`); **no** lazy import of `pages/releases/Test*|CycleCommandCenter` anywhere in `src/routes/*`; importer grep of the families returns only `CycleCommandCenter.tsx` + generated usage-map.

| Family | Contents | Stub density |
|---|---|---|
| `src/pages/releases/TestPlansPage.tsx`, `TestCyclesPage.tsx`, `CycleCommandCenter.tsx` | page shells | mounts everything below |
| `src/components/test-plans/**` (dialogs, tabs, CoverageRing, TeamMemberCard) | Create/Edit Test Plan wizard | mock team (#7), placeholder scope (#8), fake coverage math |
| `src/components/test-cycles/**` (calendar ×11, assignment-table ×11, notifications ×4, AddTestCasesToCycleDialog ×9) | cycle command center widgets | console.log handlers (#10); table itself real-query-with-mock-fallback (#5) |
| `src/hooks/test-cycles/**` (25 hooks) | data layer for above | `useAssignmentTable` (#5), `useSmartAssignment`+`useApplyAssignment` broken pair (#6); rest mostly real queries (`useCalendarData` explicitly "ZERO MOCK DATA") |
| `src/features/test-cycles/**` (5 components, 4 hooks) | config panel/timeline/milestones | no mock markers found (clean but unrouted) |
| `src/components/releases/test-cycles/**`, `smart-assignment/**`, `add-tests/**`, `cycle-command-center/**`, `test-case-detail/**` (11 files) | old cycle/case detail UX | mockTestCases (#9), toast-theater (#11), "coming soon" (#12) |

**Decision needed in Plan Lock:** delete these families (recommended — Move-not-copy / dead-code memory precedent) or explicitly re-baseline them as the raw material for the production revamp. Leaving them half-alive is how a future session routes a mock.

---

## Counter-evidence — what is PROVEN real on routed surfaces

| Surface (route) | Evidence |
|---|---|
| `/testhub/repository` + CaseDrawer + StepEditor | `useTestCases.ts` (41.6K) real CRUD; `CaseDrawer.tsx:109,124,386` `updateCase/createCase/createVersion.mutate`; AI-generated cases persisted via `useCreateTestCase` (`RepositoryPage.tsx:604-621`) |
| `/testhub/cycles`, `/testhub/:projectKey/cycles/:cycleKey`(`/execute`) | `CycleDetailPage.tsx` 23 reads/9 writes via `useTestCycles` (start/complete/scope add/remove, bulk status `:362-378` all have onClick); `ExecutionPage.tsx` 9/7 incl. `tm_cycle_scope.current_status` updates (`:95,532`) and real storage uploads (`:505-526`) |
| Status auto-cascade | run save → scope status in ExecutionPage; `passed_count` maintained by DB migrations (5 files, e.g. `supabase/migrations/20260121124446_*.sql`) |
| `/testhub/sets`, `/testhub/sets/:id` | 14/11 and 16/6 read/write supabase calls |
| `/testhub/dependencies` | real `tm_test_cycle_dependencies` CRUD (`TestHubDependenciesPage.tsx:37-79`, create/delete wired) |
| `/testhub/dashboard`, `/board`, `/my-work`, `/defects`, `/filters` | canonical wrappers (ProjectDashboardPage mode='test', KanbanPage mode='test', BacklogPage + testCases/defects adapters) — no local stubs |
| `/testhub/reports/*` | 26/26 registry entries `status:'wired'`; `reportData.ts` header documents the demo generator was **deleted** and replaced by `useRealTestReportData`; `report-insights` edge function **exists** (`supabase/functions/report-insights/`); CSV export real (`ReportExportMenu.tsx:48-52` Papa.unparse + saveAs) |
| `/testhub/timeline`, `/testhub/traceability` | timeline 5 reads/2 writes; traceability read-only matrix (no dead buttons — zero onClick grep hits) |
| Admin | `/admin/test/{priorities,types,statuses,run-statuses,permissions}` routed (`FullAppRoutes.tsx:150-154`) |
| Hooks sweep | `src/hooks/test-management/**`, `src/hooks/testhub/**`, `src/hooks/test-cases/**`: **zero** matches for mock/TODO/FIXME/hardcoded/Math.random |

## UNKNOWNs
- Whether `tm_test_plans.owner_id` is a UUID FK (mock ids `'1'-'5'` would then fail insert rather than store garbage) — schema not probed in this read-only pass.
- Whether a top-level catch-all route renders 404 vs blank for finding #2 — behavior on unmatched URL not runtime-verified.
- Runtime verification (screenshots/DOM) of findings #1-#3 — static-analysis confirmed only; recommend a browser probe in the validation phase.

# G01 — Test Design & Authoring & Repository Organization Gaps

Feature: CAT-TESTHUB-PROD-20260703-001 · Gap-mining agent (TestRail persona) · 2026-07-03
Yardstick: `discovery/14_competitor_feature_baseline.md` (Domains 1, 2, 4, 13, 15 authoring-relevant rows). Evidence verified in src/ at commit 7213f84ab unless cited to a discovery doc. Capability refs like "1.4" point to the baseline doc.

| ID | Title | Severity | Current state (file:line or discovery-doc evidence) | Target (vendor benchmark) | Proposed fix (concrete, Catalyst-canonical) | Effort S/M/L | Phase P1/P2/P3 |
|---|---|---|---|---|---|---|---|
| TD-001 | Sets row-click navigates to a route that does not exist | P0 | `TestSetsPage.tsx:435` navigates `/testhub/${projectKey}/sets/${set.id}` but only `/testhub/sets/:id` is mounted (`FullAppRoutes.tsx:679-680`; discovery 01 §1.3.1) | All vendors: every entity URL-addressable (2.8) | Register slug-based route + typed builder in `src/lib/routes.ts` per slug contract; fix caller; add `useTestSetBySlug` | S | P1 |
| TD-002 | Case version snapshots can silently fail — history lost without any error | P0 | `useAutoVersioning.ts` has 0 throws; silent `{data}` at :50, insert error captured but never thrown (discovery 02 §1) | ZS/QT: versioning is audit-grade (5.1-5.3) | Throw on error, convert to react-query mutation, surface `flag.error` from `@/components/shared/JiraTable/flags` | S | P1 |
| TD-003 | Barrel exports no-op stubs `useCreateTestCase`/`useDeleteTestCase`/`useCloneTestPlan` that lie success | P0 | `hooks/test-management/index.ts:34-44` (discovery 02 §3.1); already bit TestPlansPage clone | N/A — correctness | Delete stubs; re-export the real hooks from `useTestCases.ts:323/709`; grep-fix all barrel importers | S | P1 |
| TD-004 | Steps are card-per-step, not a 3-column grid (Action / Data / Expected as row columns) | P1 | `StepEditor.tsx:81-100` — action+expected in 2-col grid, test_data demoted to a separate input below | TR/XR 1.1: structured 3-column step rows, each field independently editable | Rebuild StepEditor as a 3-column grid (JiraTable-style rows or ADS grid) with per-cell commit; reuse in CaseDrawer + detail view | M | P1 |
| TD-005 | No drag-and-drop step reorder — only ↑/↓ buttons | P1 | `StepEditor.tsx:29-41,74-75` | TR 1.1: reorderable by drag | `@atlaskit/pragmatic-drag-and-drop` row handles on the step grid (same lib as canonical surfaces) | S | P1 |
| TD-006 | Step fields are plain `<textarea>` — no rich text despite `action_html`/`expected_result_html` DB columns | P1 | `StepEditor.tsx:84-88`; cols in `types.ts` tm_test_steps (unused) | TR/ZS 1.4: rich text in every step field, consistent renderer in editor+runner+export | Per-cell `CatalystRichTextEditor` (`src/components/shared/rich-text/`) writing `*_html`, plain-text mirror kept for search | M | P1 |
| TD-007 | Case description/preconditions are plain textareas; `description_html`/`preconditions_html` columns dormant | P1 | `CaseDrawer.tsx:285-303`; cols in `types.ts` tm_test_cases | TR/ZS 1.4 | `CatalystRichTextEditor` for objective + preconditions in create/edit and detail view | M | P1 |
| TD-008 | No paste-screenshot / inline attachment per step | P1 | No attachment UI anywhere in StepEditor/CaseDrawer; `tm_attachments` is polymorphic (entity_type/entity_id, `types.ts:41806`) so schema is ready | TR 1.5: paste-from-clipboard into any step, carried into runner and reports | Paste/drop handler per step cell → upload to existing `tm-attachments` bucket with entity_type='test_step'; thumbnail strip under the row | M | P2 |
| TD-009 | No case-level attachments in authoring or detail view | P1 | `tm_attachments` + bucket already used for defects (`useDefects.ts:822`); zero case UI | QT 1.12: case attachments with preview | Attachments section in CatalystViewTestCase (reuse defect attachment upload/list pattern) with entity_type='test_case' | M | P1 |
| TD-010 | Shared/reusable steps missing entirely — schema exists, UI was deleted | P1 | `tm_test_steps.shared_step_id` self-FK + `is_shared` (`types.ts:47628-47633`); `CreateSharedStepModal`/`DeleteSharedStepModal` exist only as stale refs in `usage-map.generated.ts:3310,3479` — files gone | TR/AIO 4.3: shared step groups, edit-once-update-everywhere | Shared-steps library page under /testhub/repository (JiraTable) + "Insert shared steps" picker in step grid; consumers render expanded; usage-count column | L | P2 |
| TD-011 | Preconditions are a text blob, not first-class reusable entities | P2 | `CaseDrawer.tsx:295-303` free text only | XR 4.1-4.2: Precondition with own key, attached to many tests, impact view | Defer behind TD-010; if built: `tm_preconditions` table + picker + "used by N tests" panel | L | P3 |
| TD-012 | Labels/tags invisible across authoring: no field in drawer, no column, detail view hardcodes `labels: null` with a false comment | P1 | `tm_labels`/`tm_case_labels` + `useTestCaseTags` are CLEAN (discovery 02); admin manages labels (`useAdminConfig.ts:211`); `CatalystViewTestCase.tsx:96-102` comment claims "tm_test_cases has no labels" — schema has `labels: string[]` | All vendors 1.6/2.3: labels filterable, bulk-editable | Wire `useTestCaseTags` into CatalystSidebarDetails EditableLabels; add JiraTable `makeLabelsEditCell` column; labels field in create flow; delete the false comment | M | P1 |
| TD-013 | No estimate field anywhere in authoring | P1 | `estimated_time` col on tm_test_cases; `useTestCases.ts:180,251` already accepts `estimated_duration_minutes`; no UI input | TR 1.6/6.6: estimates roll up to cycle effort forecasting | Estimate field (minutes) in case form + detail sidebar; repository column; sum shown per folder/cycle scope | S | P1 |
| TD-014 | No owner/assignee field in case create/edit | P1 | `assigned_to` col exists; `CaseFilters.assigned_to` supported (`types/test-management.ts`); CaseDrawer has no field | All vendors 1.6 | ADS user picker in CaseDrawer/detail sidebar (reuse `EditableAssignee` from CatalystSidebarDetails) | S | P1 |
| TD-015 | No test case templates | P2 | `is_template` boolean on tm_test_cases unused; new case always starts blank | TR 1.7: admin templates enforce authoring standards | "Save as template" + template picker on create (filter `is_template=true`); manage under /admin/test | M | P2 |
| TD-016 | Single-case clone built but unreachable — zero UI callers | P1 | `useCloneTestCase` (`useTestCases.ts:742-811`) unimported by any page (grep); `cloned_from_id` provenance never shown | TR/ZS 1.8: clone with steps/fields preserved | Add Clone to JiraTable `contextMenuActions`/row menu in RepositoryPage; show "cloned from" chip in detail view | S | P1 |
| TD-017 | No cross-project copy of cases | P2 | `useBulkCopyTestCases` copies to a target folder within the same project only (`RepositoryPage.tsx:676-682`) | TR/ZS 1.8: cross-project copy preserving steps/fields | Extend copy modal with project select; remap priority/type by name; keep `cloned_from_id` | M | P3 |
| TD-018 | Test type never switches the editor; Gherkin/BDD authoring absent | P2 | `test_format`, `gherkin_feature`, `gherkin_scenario` cols dormant; editor is always the manual step grid | XR 1.2-1.3: type-driven editor (steps grid vs Gherkin vs free text), scenario outlines | Type-switched editor shell: manual=step grid, BDD=Gherkin editor (monospace + syntax highlight), generic=rich text; persist `test_format` | L | P3 |
| TD-019 | Case lifecycle unenforced: any status transition allowed, DRAFT cases can enter cycles | P2 | Status is a free `<select>` (`CaseDrawer.tsx:246-255`); no gate in cycle-scope add path | ZS/PT 1.11: status machine with gating (only Ready/Approved into cycles) | Transition rules in `useUpdateTestCase`; warn/block DRAFT/DEPRECATED in add-to-cycle flows; audit transition history | M | P2 |
| TD-020 | Native `<select>` dropdowns in case form — banned hand-rolled UI | P1 | `CaseDrawer.tsx:246-282` (status/priority/type) | Catalyst canon | Replace with `@atlaskit/select` | S | P1 |
| TD-021 | CaseDrawer is a hand-rolled fixed side panel duplicating the detail-view role | P1 | `CaseDrawer.tsx:170-188` (position:fixed 640px, manual Escape); discovery 05 V13 | Catalyst canon: CatalystViewBase/CatalystDetailRouter own detail | Keep a create-only modal (`@atlaskit/modal-dialog`); fold ALL editing into CatalystViewTestCase; delete drawer edit path | M | P1 |
| TD-022 | Steps are read-only in the canonical detail view — editing requires the other surface | P1 | `CatalystViewTestCase.tsx:296-310` renders steps as static list | TR: single authoring surface, inline editable | Mount the (rebuilt) step grid inside CatalystViewTestCase Steps tab with `useTestSteps` mutations (already CLEAN) | M | P1 |
| TD-023 | Escape discards unsaved authoring work with no dirty-check; draft autosave hook exists unused | P1 | `CaseDrawer.tsx:37-46` closes on Escape unconditionally; `upsertDraft` mutation exists (`useTestCases.ts` ~:681-704) with zero callers | TR autosave (8.10 analog for authoring) | Dirty-state confirm on close + wire `upsertDraft` on debounce | S | P1 |
| TD-024 | No keyboard-first rapid authoring (grid bulk add) | P2 | Only path is drawer-per-case; no tab-through, no multi-row add | AIO/TR 1.10: author 20 cases without leaving keyboard | JiraTable `stickyCreateFooter`/inline-create row on RepositoryPage (pattern proven in BacklogPage) creating title-only DRAFT cases | M | P2 |
| TD-025 | Step-level fields hidden: `is_optional`, `notes`, `estimated_time_seconds` never exposed | P2 | Cols in `types.ts` tm_test_steps; `StepInput` is action/expected/test_data only (`StepEditor.tsx:4-8`) | TR: per-step metadata reportable | Add optional-step toggle + notes + per-step estimate to the rebuilt step grid | S | P2 |
| TD-026 | No custom step columns (extensible step schema) | P3 | No schema/UI | TR 1.13 step custom fields | Defer; ride on TD-049 custom-fields engine with scope='step' | L | P3 |
| TD-027 | Postconditions column exists in DB but has no UI field anywhere | P3 | `postconditions_html` in `types.ts` tm_test_cases; 0 grep hits in src/pages | TR/XR: postconditions standard | Postconditions rich-text field alongside preconditions (TD-007) | S | P2 |
| TD-028 | Folder drag-and-drop move/reorder unwired — mutations exist, tree renders no drag handlers | P1 | `useMoveFolder`/`useReorderFolder` CLEAN in `useFolders.ts` (discovery 02); `FolderTreeView` (`RepositoryPage.tsx:478-505`) has zero drag props (grep draggable/onDrop = 0) | TR/ZS 2.1: drag between folders preserving order | pragmatic-drag-and-drop on FolderNode rows wired to existing mutations | M | P1 |
| TD-029 | Cannot move cases to a folder: bulk menu is Copy/Archive only; no drag-case-onto-folder | P1 | `useBulkMoveTestCases` exists (`useTestCases.ts` ~:820-855, toast at :855) unwired; bulk menu `RepositoryPage.tsx:780-785` | TR 2.1/2.7 | Add "Move to folder" bulk action (reuse copy modal's folder picker) + drop-target folders | S | P1 |
| TD-030 | Bulk edit limited to copy/archive — no bulk status/priority/owner/labels change | P1 | `useBulkUpdateTestCases` exists (`useTestCases.ts:1065-1105`) with zero UI callers | TR 2.7: transactional bulk ops for 500 cases | Bulk-edit dropdown items (status/priority/type/owner/labels) wired to existing hook, with preview count + flags | M | P1 |
| TD-031 | Bespoke "select mode" banner instead of canonical bulk bar | P2 | `RepositoryPage.tsx:770-787` custom banner | Catalyst canon: JiraTable `BulkFooterBar` export (discovery 05 §1.1) | Swap to `selectable` + `BulkFooterBar` | S | P2 |
| TD-032 | Folders are structure-only: no run-folder / create-cycle-from-folder / export-folder operations | P2 | Folder kebab = Rename / New subfolder / Delete only (`RepositoryPage.tsx:386-412`) | TR 2.2: folder as actionable scope | Add kebab items: "Create cycle from folder" (feeds tm_cycle_scope), "Export folder CSV" (TD-045 engine) | M | P2 |
| TD-033 | Folder color/icon/description columns unused; create modal takes name only | P3 | `tm_folders.color/icon/description` in `types.ts`; `handleCreateFolder` passes name+parent only (`RepositoryPage.tsx:660-666`) | ZS/TR tree ergonomics | Optional icon/color in folder modal; render in tree | S | P3 |
| TD-034 | Folder tree fully eager — no lazy loading/virtualization for 10k+ case repos | P3 | `useFoldersWithCounts` loads all rows; tree built in memory (`RepositoryPage.tsx:487-488`) | TR/ZS 2.1: 50k-case trees, lazy nodes | Virtualize tree rows; paginate case list (TD-041) first — likely sufficient for v1 | M | P3 |
| TD-035 | Tree communicates structure only — no per-folder health/last-run stats | P3 | Counts only via `useFoldersWithCounts` | AIO/PT 2.12: tree as health heatmap | Optional pass-rate dot per folder from latest-run rollup RPC | M | P3 |
| TD-036 | Search covers title+case_key only — step text, objective, preconditions unsearchable | P1 | `useTestCases.ts:76` `.or(title.ilike…,case_key.ilike…)` | TR/QT 2.5: full-text incl. step text, <1s at 10k cases | Postgres tsvector over case+steps (generated column + GIN) exposed via RPC; keep ilike fallback | M | P2 |
| TD-037 | No toolbar filters (status/priority/type/label/assignee) on Repository despite hook support | P1 | `CaseFilters` supports all (`types/test-management.ts`); page wires only folder+search (`RepositoryPage.tsx:626-629`) | All vendors 1.6/2.5 | Canonical `BasicFilterBar` (`src/components/filters/BasicFilterBar.tsx`) or JiraTable per-column `renderFilterMenu` | S | P1 |
| TD-038 | Saved filters for test cases unverified — /testhub/filters mounts the project-hub filter engine | P2 | `FiltersListPage.tsx` wraps project-hub pages `hubType="test"` (discovery 01 §3.1) | XR 2.6: saved, shareable queries over tests | PLACEHOLDER: does the canonical filter engine query tm_test_cases or only ph_issues in test mode? Probe, then either wire a tm adapter or descope | M | P2 |
| TD-039 | Archived cases unreachable: archive works but there is no archived view or restore | P1 | `useBulkArchiveTestCases` wired (`RepositoryPage.tsx:668-674`); `CaseFilters.showArchived` exists; no UI toggle, no unarchive action | TR/QT 2.9: archive default, retrievable, excluded from metrics | "Show archived" toggle + Unarchive bulk action (`archived=false` update via TD-030 hook) | S | P1 |
| TD-040 | Per-row hard delete is the visible destructive action; run-history tombstone behavior unverified | P2 | Trash icon per row (`RepositoryPage.tsx:744-762`) → `useDeleteTestCase`; archive hidden in bulk menu | TR/QT 2.9/5.9: archive-first; deletes never corrupt run history | Demote delete to detail-view kebab behind confirm; PLACEHOLDER: verify FK behavior of tm_cycle_scope/tm_test_runs on case delete (tombstone vs cascade) | S | P2 |
| TD-041 | No pagination on repository list — full fetch regardless of size | P2 | `CaseFilters.page/per_page` exist, unused by page (`RepositoryPage.tsx:626-632`); JiraTable pagination props available | TR: 10k+ case grids | Wire `page`/`rowsPerPage`/`totalRowCount` through useTestCases (it already returns `{cases}` result object) | S | P2 |
| TD-042 | Repository grid is 4 data columns with no sort, column picker, or inline edit | P2 | Columns key/title/status/priority + delete (`RepositoryPage.tsx:684-764`); no `sortable`, `columnVisibility`, `onCellEdit` despite JiraTable support | TR: owner/labels/updated/last-run/automation columns, sortable | Add columns via prefab cells (`makeLabelsEditCell`, `makeAssigneeEditCell`, `makeDateCell`) + column picker + controlled sort | M | P1 |
| TD-043 | No manual case ordering within a folder | P3 | `board_position` col exists (board-only); list order fixed by query | TR 2.11: repository order inherits into cycles | Row drag handle (`renderRowDragHandle`) persisting a folder-scoped sort_order | M | P3 |
| TD-044 | No CSV/Excel test-case import (multi-row steps, mapping wizard, dry-run) | P1 | Zero import flow in module (grep); prior gap-analysis "Bulk import (CSV): missing" | TR 15.1: mature CSV import with validation preview + dedupe keys | Import wizard modal: file → column mapping → dry-run validation report → commit via `useCreateTestCase` batch; steps as numbered rows convention | L | P2 |
| TD-045 | No CSV export of repository/filter results | P1 | `ReportExportMenu` exists for reports only (`components/testhub/reports/`); repository has none | All vendors 15.2: export respects current filters/columns | "Export CSV" toolbar action serializing current filter result incl. steps (reuse report export-row utilities) | S | P2 |
| TD-046 | No tool-to-tool migrators (TestRail/Xray/Zephyr XML/JSON) | P3 | Nothing beyond (missing) CSV | XR/ZS 15.3 | Defer; TestRail XML adapter layered on TD-044 wizard | L | P3 |
| TD-047 | No Gherkin .feature import/export | P3 | `gherkin_*` cols dormant; no repo round-trip | XR 15.8 | Defer with TD-018; .feature parse/serialize on the BDD editor | L | P3 |
| TD-048 | Flag columns dormant: cannot flag a case (flaky/quarantine) or filter flagged | P2 | `is_flagged`/`flag_reason` on tm_test_cases; 0 UI grep hits for is_flagged in src/pages | QT/AIO 11.5: flakiness surfaced on the case, quarantine filter | Flag toggle + reason in detail sidebar; flagged filter chip + repository column; later feed computed flip-rate | M | P2 |
| TD-049 | `custom_fields` Json column with no field-definition system or rendering | P2 | Col on tm_test_cases; no admin UI (prior gap-analysis "Custom fields admin: missing"); handover §9 admin scope unbuilt | TR/PT 14.1-14.2: typed custom fields, filterable, per-project sets | `tm_custom_field_defs` table + /admin/test fields page + dynamic renderer in case form/detail; report exposure later | L | P3 |
| TD-050 | No case-to-case linking (related / duplicates / depends-on); parent hierarchy dormant | P2 | `parent_case_id` col unused; prior gap-analysis "Case linking: missing" | TR references, ZS relations | Links section in CatalystViewTestCase reusing canonical linked-work-items pattern; typed relation table | M | P2 |
| TD-051 | No duplicate/similar-case detection at authoring time | P3 | Nothing; AI gateway exists (discovery 12) | AIO 13.4: "92% similar to PROJ-T88" before save | Embedding similarity check in create flow via ai gateway; accept/merge UX | M | P3 |
| TD-052 | Silent `{data}` destructures feed the authoring dropdowns — priorities/types render empty on error, cases saved untyped | P1 | `CaseDrawer.tsx:48-72` (both queries), `RepositoryPage.tsx:634-646` | N/A — correctness | Throw on error per silent-sweep pattern; SectionMessage+Retry in drawer body | S | P1 |
| TD-053 | Case-list enrichment silently swallows errors — step/run counts silently 0; reads possibly-dead legacy `test_cycle_executions` | P1 | `useTestCases.ts` 11 silent destructures (:118,:135,:216 …:1154, discovery 02 §2); legacy table risk (discovery 02 §5) | N/A — correctness (zero-assumption law) | Throw or render "—" on enrichment failure; migrate execution counts to tm_test_runs | M | P1 |
| TD-054 | Color-law violations in authoring surfaces | P1 | `StepEditor.tsx:76` `var(--ds-text-danger, var(--ds-text-danger))` nonsense fallback; `CaseDrawer.tsx:182` rgba fallback (discovery 05 V12) | ADS token law | Token-only cleanup; ratchet baseline down | S | P1 |
| TD-055 | AI generate dialog (main authoring accelerator) built on banned shadcn stack | P1 | `AIGenerateTestCasesDialog.tsx:28-50` imports `@/components/ui/*` dialog/button/select/etc (discovery 05 V8) | Catalyst canon | Rebuild on `@atlaskit/modal-dialog` + ADS primitives, keep `useAIGeneration` wiring | M | P1 |
| TD-056 | AI bulk-create is non-atomic with an uninformative failure toast | P2 | `RepositoryPage.tsx:604-623` Promise.all → generic "Failed to create some AI-generated cases"; no per-case result or retry | TR AI 13.1: review-before-save, per-item outcome | Promise.allSettled + per-case result list in dialog (created / failed+retry) | S | P2 |
| TD-057 | Smart/dynamic sets dormant — schema present, no builder UI | P2 | `tm_test_sets.is_smart` + `smart_query` (prior gap-analysis:43); Sets pages hand-rolled (discovery 05 V2-V3) | PT 2.4 filter-trees: repository re-projected by saved query | Smart-set builder on the canonical filter bar output; resolve membership at read time | M | P3 |
| TD-058 | "Replace in all test sets" on new version has no impact preview | P3 | `CaseDrawer.tsx:358-396` bare checkbox | TR 4.5: "used in N cases/M runs" warning before breaking edit | Query tm_set_cases/tm_cycle_scope counts, show "affects N sets, M open cycles" in the modal | S | P3 |

# G07 — Reporting & Analytics Gap Table (PractiTest persona)

Feature: CAT-TESTHUB-PROD-20260703-001 · Gap-mining agent · 2026-07-03
Yardstick: `discovery/14_competitor_feature_baseline.md` Domain 11 (+ 9.6, 10.4, 12.9, 15.2/15.6). Inventory basis: `discovery/06_reports_inventory.md` (the 26 wired reports are REUSE-only, all fixes below are additive or bug-fix — no refactor of shipped bodies). Cross-checked against prior research `docs/feature-builder/features/test-hub/gap-analysis.md` (2026-06-26 — its "13 report stubs" P0 is now CLOSED by the 26-report hub; remaining overlaps deduped into rows below). Severity: P0=broken/lying today, P1=table-stakes missing, P2=competitive, P3=delighter.

| ID | Title | Severity | Current state (file:line or discovery-doc evidence) | Target (vendor benchmark) | Proposed fix (concrete, Catalyst-canonical) | Effort S/M/L | Phase P1/P2/P3 |
|---|---|---|---|---|---|---|---|
| RPT-001 | Silent query errors in 7 of 10 dedicated-body report hooks render fake zeros | P0 | 06 §2: useSprintTestingStatus 6 `{data}`-only/0 throws, useGovernance 3/0, useProductStatus 4/0, useProjectTestingStatus 4/0, useTeamPerformance 4/0, useTesterPerformance 3/0, useDefectsIncidents 1/0 — failed query = "0 stories / 0 coverage" presented as real data | All vendors: a report either shows data or an error, never a false zero (PT audit-emphasis baseline 5.6) | Apply the established silent-query-error sweep pattern (memory: `silent-query-error-sweep`) to the 7 hooks: `if (error) throw error` per query; ReportRenderer error boundary (ReportRenderer.tsx:22–50) already catches and shows SectionMessage+Retry — hook-level change only, zero UI refactor | S | P1 |
| RPT-002 | Default report does org-wide full-table scans with client-side JSONB sprint filtering | P0 | 06 §5.2: useSprintTestingStatus.ts:49–129 fetches ALL Stories, ALL QA Bugs, ALL Production Incidents org-wide and filters `ph_issues.sprint_release` in JS; same in usePointsBurndown | TR/QT: reports scoped server-side, sub-second on 10k+ entities (14 §2.5 scale bar) | Add a `tm_get_sprint_testing_scope(sprint_name, project_id)` SECURITY INVOKER RPC doing the JSONB containment in SQL (staging cyij first); hooks keep identical return shape so bodies are untouched | M | P1 |
| RPT-003 | Two parallel analytics stacks compute the same metrics with different formulas | P0 | 06 §3+§5.3: hub computes pass rate/tester performance client-side over raw tables while `tm_get_tester_performance`, `tm_compare_cycles` etc. RPCs (src/hooks/test-cycles/useCycleAnalytics.ts) compute the same server-side — tester performance exists in BOTH with different formulas → divergent numbers between surfaces | One number per metric org-wide (QT Insights single-source metric definitions) | Declare tm_* RPCs the SSoT for cycle-level metrics; new/changed reports consume RPCs; document formula per metric in FORMULA_EXPLANATIONS (reportDefinitions.ts:204–224); do NOT rewrite the 26 shipped bodies — converge only where numbers visibly disagree (tester-performance first) | M | P1 |
| RPT-004 | Live-routed `/release/incidents/reports` reads a 0-row table and shows empty reports as real | P0 | 06 §3: src/pages/release/IncidentReportsPage.tsx (609 lines) reads legacy `incidents` table (0 rows per IncidentReportPage header) — 4 report tabs of confident-looking empty data | No vendor ships a dead-data report surface | Redirect `/release/incidents/reports` → `/incident-hub/reports` (the live MTTR surface, src/modules/incidents/analytics/pages/IncidentReportPage.tsx) via FullAppRoutes; retire the page in a later cleanup slice (PLACEHOLDER: Vikram confirm redirect vs delete) | S | P1 |
| RPT-005 | `rh_releases.readiness_pct` is a hand-stored column presented as release readiness | P0 | 13 §4.1: useReleaseHub.ts:82–83 selects stored `readiness_pct`, rendered at CommandCenterPage.tsx:165; NOT derived from quality gates or test executions — competing "readiness" definitions (13 §7.3) | XR/QT: readiness derives from gate evaluation + execution data, never hand-entry | Decide one readiness definition: wire `tm_evaluate_quality_gates`/`tm_create_readiness_snapshot` output into the command center, or label the column "manual estimate" until then (PLACEHOLDER: depends on release-identity decision, see RPT-018) | M | P2 |
| RPT-006 | History-gated reports show near-empty charts with no "capture since" disclosure | P0 | 06 §5.4: defect-closure-trend, points-burndown, incident MTTR accrue from the 2026-07-03 trigger with no backfill; charts silently omit all pre-capture events — "0 closures in May" reads as fact | TR/QT time-series reports state their data window explicitly | Add a small `SectionMessage appearance="information"` line "History captured since 2026-07-03" to DefectClosureTrendBody/PointsBurndownBody/IncidentReportPage, driven by a `historySince` field on the registry entry — additive prop, no body refactor | S | P1 |
| RPT-007 | Sprint reports scope by `sprint_release` JSONB while tm_* artifacts use `sprint_id` FK — same sprint, two scopes | P1 | 13 §6: SprintTestingStatusBody.tsx:4 resolves scope from JSONB; tm_test_cycles/plans/cases/defects carry `sprint_id → ph_jira_sprints` (migration 20260627192541); sprint numbers can disagree with cycle-level numbers for the same sprint | One sprint identity feeding all reports (XR fixVersion-consistency equivalent) | Sprint reports additionally join tm_* rows via `sprint_id` and reconcile; long-term bridge documented in Plan Lock (PLACEHOLDER: which model is canonical for delivery-side issues — needs Vikram decision) | M | P2 |
| RPT-008 | No flaky-test report | P1 | 06 §4: run history exists per case (tm_test_runs) but no flakiness computation anywhere in src | QT Launch/AIO flakiness score per case, quarantine-candidate filter (14 §11.5) | New registry entry `flaky-tests` + RPC computing status flip-rate over last N runs per case from tm_test_runs; render via ReportStatusView (metric ribbon + JiraTable) with flip-rate column + Lozenge | M | P2 |
| RPT-009 | No automation coverage / manual-vs-automated report despite ready schema | P1 | 06 §4: `automation_connectors`, `automation_results`, `automation_test_mappings` exist (types.ts:1328–1520) with zero src consumers | AIO automation dashboards, XR blended manual+automated provenance (14 §12.9) | New registry entry `automation-coverage`: cases with mapping vs without, automated result pass-rate vs manual, per-folder split; blocked until automation ingestion lands (PLACEHOLDER: depends on G-automation domain sequencing) | M | P3 |
| RPT-010 | No scheduled report delivery (email/Slack subscriptions) | P1 | 06 §4: tm_saved_reports saved views exist (useSavedReports.ts) but no scheduling/delivery of any kind | TR scheduled reports, PT subscriptions with baked-in filters (14 §11.7) | `tm_report_subscriptions` table (view_id FK, cadence, channel) + pg_cron-triggered edge function rendering the saved view's rows to CSV + posting via existing notification path; Slack via configured webhook (PLACEHOLDER: is there an org email sender today?) | L | P3 |
| RPT-011 | PDF export is table-only — no charts, no branding, no report header block | P1 | ReportExportMenu.tsx:1–9 header: "Charts are NOT embedded in the PDF this phase — table only" | XR Xporter templated formal documents; TR PDF with charts (14 §11.6, §15.6) | Phase 2 of the existing export menu: rasterize the recharts SVG (ReportChart.tsx) via canvas → jspdf image + title/project/date/filters header block; keep lazy loaders in src/lib/exportLoaders | M | P2 |
| RPT-012 | No Excel (XLSX) export — CSV only | P1 | src/lib/exportLoaders loads papaparse/jspdf/file-saver only (ReportExportMenu.tsx:13–18); 14 §15.2 expects Excel | All vendors export Excel from any grid (14 §15.2) | Add lazy `xlsx` (SheetJS) loader to exportLoaders + "Export XLSX" DropdownItem in ReportExportMenu reusing the same ExportColumn/ExportRow contract | S | P2 |
| RPT-013 | Every hub report is single-project — no cross-project/org rollup | P1 | 06 §4: "Every hub report is single-project (project picker); no cross-project/org dashboard" | QT Insights / PT org-level QA health (14 §11.3) | New registry category "Portfolio" with 1–2 reports (org execution status, org defect summary) using an org-scoped RPC; render with ReportStatusView; permission-gate to leads via CRE | L | P3 |
| RPT-014 | No defect density per component/module/folder | P1 | 06 §4: RPC `tm_compare_cycles` returns `defect_density` per cycle (useCycleAnalytics.ts:157,190) — never surfaced; no per-module density anywhere | PT dashboards defect clustering/hotspots (14 §9.6) | New registry entry `defect-density`: tm_defects grouped by component/folder (via linked case's folder) + per-cycle density from the existing RPC; bar chart via ReportChart + JiraTable detail | M | P2 |
| RPT-015 | No escaped-defect / leakage (phase-containment) report | P1 | 06 §4: sprint-testing-status counts QA Bugs vs Production Incidents (useSprintTestingStatus.ts:126–129) but no leakage ratio or containment trend | Enterprise QA staple: found-in-test vs found-in-prod ratio per release/sprint | New registry entry `defect-leakage`: (Production Incidents)/(QA Bugs+Incidents) per sprint/release from ph_issues, trend line via ReportChart; reuses the sprint scope RPC from RPT-002 | M | P2 |
| RPT-016 | No test-case aging/staleness report | P1 | 06 §4: case-usage counts cycles, not recency; no "not executed in N days / not updated in N days" view | TR/PT stale-case alerts; 14 §2.12 tree health | New registry entry `case-staleness`: tm_test_cases joined to latest tm_test_runs.executed_at + updated_at buckets (30/90/180d); JiraTable with age Lozenges, CSV export free via existing menu | S | P2 |
| RPT-017 | No environment/platform coverage report despite schema support | P1 | 06 §4: `environment` columns exist (types.ts:9164, 9632) — no report consumes them | TR configurations / XR test environments segregate results per env in every report (14 §3.9) | New registry entry `environment-coverage`: pass/fail matrix env × cycle from tm_test_runs.environment; flag rows where environment is null (zero-assumption: render dash, not a default env) | M | P3 |
| RPT-018 | Release readiness scorecard fully built but unmounted, FK'd to a dead identity space | P1 | 13 §7: gates/waivers/templates/history/readiness snapshots (tm_release_quality_gates, tm_release_readiness, RPCs, useReleaseQualityGates.ts, quality-gates components) all functional, zero routes; everything FK'd to legacy `releases(id)` while live surfaces run ph_releases/rh_releases | XR/QT release-readiness go/no-go report — "the single report a release manager needs" (14 §11.10) | Mount a `release-readiness` registry entry consuming useReleaseQualityGates/useReleaseReadiness once release identity is resolved (PLACEHOLDER: re-point FKs vs mapping table vs migrate live surface — Vikram decision, blocks this row) | L | P2 |
| RPT-019 | Test-plan progress report exists as RPC only, never surfaced | P1 | 06 §4: `tm_get_plan_analytics` (useCycleAnalytics.ts:223–257) not in hub; no per-plan rollup report | XR Test Plan consolidated verdict, QT plan progress (14 §6.5) | New registry entry `plan-progress` calling the existing RPC; ReportStatusView with per-cycle child rows; dedup policy note in FORMULA_EXPLANATIONS | S | P2 |
| RPT-020 | Execution-velocity/throughput trend RPC unsurfaced in hub | P1 | 06 §3: `tm_get_cycle_execution_velocity` + VelocityChart live only in the orphaned Cycle Command Center cluster (13 §3: no route mounts pages/releases/**) | TR activity/comparison reports — executions per day, acceleration (14 §11.4) | New registry entry `execution-velocity` (Execution category) calling the existing RPC; line chart via ReportChart; do NOT resurrect the orphan pages | S | P2 |
| RPT-021 | 16 Wired reports have zero drill-through — charts/tables are dead ends | P1 | Dedicated bodies navigate (`onRowClick → /browse/:key`, e.g. GovernanceBody.tsx:117, DefectSummaryBody.tsx:191); WiredReportBody/ReportCanvas path has no navigate/onRowClick (grep: 4 matches, all in bodies/) | All vendors: report row → entity in one click (14 §16.7 deep links) | Add optional `onEntityOpen` wiring in ReportCanvas renderers: case rows → testHub repository detail, cycle rows → Routes.testHub cycle detail, defect rows → /browse/:key; additive props, existing visuals untouched | M | P2 |
| RPT-022 | Traceability matrix export is a flat list, not a req × case matrix grid | P1 | 06 §4: traceability-summary/detail are list/chain views with CSV of the table; no matrix-format export; PDF table-only | XR/PT/QT auditor-ready traceability matrix export (14 §10.4) | Add a matrix pivot exporter to the traceability reports: rows=requirements, cols=cases, cells=latest run status; XLSX (RPT-012 loader) as the natural format; UI stays the existing chain view | M | P2 |
| RPT-023 | No persisted metric time-series — trends recompute from live rows, deletes rewrite history | P1 | All trend reports scan created_at on current tables (06 §2 data sources); no snapshot table; 14 §11.4: "time-series persisted independently of current entity state (deletes don't rewrite history)" | TR/QT persisted historical trends | `tm_metric_snapshots` table + nightly pg_cron capture of core KPIs (pass rate, open defects, coverage %, executed count) per project; trend reports read snapshots where available, live rows otherwise | L | P3 |
| RPT-024 | No data-freshness indicator or manual refresh on any report | P2 | 5-min staleTime on every body (e.g. SprintTestingStatusBody.tsx:41) with no `dataUpdatedAt` display and no refresh button anywhere in ReportsHubPage/bodies (grep) | PT/QT dashboards show last-refreshed + refresh control | Add "Updated Xm ago" + refresh IconButton to the hub header (ReportsHubPage) driven by react-query `dataUpdatedAt`/`refetch`; one shared component, additive | S | P2 |
| RPT-025 | Hub bulk hook fetches 10 tables client-side for every Wired report view | P1 | 06 §1: useRealTestReportData.ts — 10 tables in one queryFn; all aggregation in browser; will not hold at 10k+ cases (14 §2.5 scale bar) | TR/QT server-side report queries | Add per-category aggregate RPCs (execution, cases, defects, multi-cycle) and switch useRealTestReportData internals to them behind the same ReportData shape — bodies and ReportCanvas untouched (reuse, not refactor) | L | P3 |
| RPT-026 | Cycle quality/defect-trend analytics (RPC-backed) reachable from no live route | P1 | 13 §3+06 §3: `tm_get_cycle_quality_trends`, `tm_get_cycle_defect_trends`, `tm_compare_cycles` + charts live only under orphaned pages/releases/** cluster | TR run/milestone analytics live on the cycle surface | Surface as hub registry entries (`cycle-quality-trend`, `cycle-defect-trend`) and/or a tab on the live TestHub CycleDetailPage; consume RPCs directly, do not remount orphan pages (13 §8 resurrection risk) | M | P2 |
| RPT-027 | No reopened-defect rate report | P2 | 06 §4: needs tm_defects status history; only closure capture (from 2026-07-03) exists | TR/PT defect quality metrics | Extend the D-004 status-capture trigger to record reopen transitions on tm_defects; add `reopened-rate` registry entry once ≥1 sprint of history accrues (history-gated, gets RPT-006 disclosure) | M | P3 |
| RPT-028 | No defect MTTR/MTTD report for test defects (only Production Incident MTTR exists) | P2 | 06 §3: Incident Hub MTTR covers ph_issues Production Incidents only; tm_defects have created/closure capture but no age-to-close report | PT defect age/resolution-time dashboards | New registry entry `defect-mttr`: median/percentile time-to-close from tm_defects closure history (post-2026-07-03), grouped by severity; RPT-006 disclosure applies | S | P3 |
| RPT-029 | No burndown forecast / "will the cycle finish by the date" | P2 | execution-burndown/burnup exist (06 §2 rows 3–4) but plot actuals only; no pace-based ETA anywhere | TR forecast, QT predictive completion (14 §11.9) | Add a computed forecast line (linear pace over trailing 7 days) to the existing burndown canvas renderer + ETA date in metric ribbon; formula documented in FORMULA_EXPLANATIONS | M | P3 |
| RPT-030 | No run-level comparison (same case across runs/cycles, diff view) | P2 | 06 §4: multi-cycle-detail pivots latest status per cycle; no run-level re-run diff; tm_test_runs history exists | TR result-history per test-in-run, XR execution history (14 §7.6) | New registry entry `run-comparison`: pick a case → JiraTable of its runs across cycles (status, executor, duration, defects); links to execution records | M | P3 |
| RPT-031 | Multi-cycle comparison lacks deltas and baseline selection | P2 | 06 §2 rows 13–16: 4 multi-cycle reports pivot latest status only; RPC `tm_compare_cycles` (richer: defect_density, health) unused by hub | TR milestone comparison with regression deltas | Add baseline-cycle picker + delta columns (pass-rate Δ, new-fail count) to the multi-cycle canvas renderers, sourcing `tm_compare_cycles`; additive columns only | M | P3 |
| RPT-032 | No net-open defect trend (created vs closed overlay) | P2 | defect-trend = created_at only (06 §2 row 11); defect-closure-trend = closures only (row 12); no combined open-count-over-time | PT/TR defect trend shows created/closed/open in one chart (14 §11.4) | New canvas renderer `defect-open-trend` combining both existing series + cumulative open line; history-gated disclosure per RPT-006 | S | P3 |
| RPT-033 | No defect aging-bucket view (open defects by age) | P2 | DefectSummaryBody lists open defects (06 §2 row 9) with no age dimension | PT defect age dashboards | Add age-bucket Lozenge column + bucket bar chart to DefectSummaryBody from tm_defects.created_at — additive column on the existing JiraTable | S | P3 |
| RPT-034 | Saved views cannot capture folder/owner/cycle filter state | P2 | SavedReportParameters = `{projectId?, cycleId?, range?}` only (useSavedReports.ts:13–17); WiredReportBody filters include folder/owner/cycle (WiredReportBody.tsx:55–75) — a saved view silently loses folder/owner | TR/PT saved reports persist full filter state (14 §11.7 "filters baked in") | Widen SavedReportParameters with folder/owner keys (JSONB column already free-form); apply on load in WiredReportBody; backward-compatible | S | P2 |
| RPT-035 | Deep-link URL carries only `?project=&range=` — shared links drop filters | P2 | 06 §1: deep-link `?project=&range=` (useSavedReports row); cycle/folder/owner not URL-serialized | 14 §16.7: "a URL pasted in standup opens the exact filtered view" | Serialize all WiredReportBody filters to search params (read+write); same keys as RPT-034 | S | P2 |
| RPT-036 | Shared saved views are org-global — no project/team scoping | P2 | useSavedReports.ts:4–7: RLS = owner's rows + is_shared rows (boolean only); a shared view from any project appears for everyone | PT role-scoped dashboard sharing (14 §11.2) | Add nullable `project_id` to tm_saved_reports + filter shared views to current project in the Save-view picker; RLS unchanged | S | P3 |
| RPT-037 | People reports (tester/team performance) visible to anyone with hub access | P2 | 06 §2 rows 21–22: tester-performance/team-performance registry entries have no permission gating evidence; 14 §11.12: per-tester productivity must be permission-gated to leads | TR/PT privacy controls on productivity views | Gate the People category via existing CRE permission check (09_cre doc) — hide category in ReportNavigator + guard route for non-leads (PLACEHOLDER: which CRE permission key maps to "QA lead") | S | P2 |
| RPT-038 | No composite QA dashboard — hub is strictly one-report-per-screen | P2 | ReportsHubPage renders a single `:reportSlug` at a time (06 §1); no multi-widget QA overview page | PT named dashboards with widget grid, QT Insights (14 §11.2) | New `/testhub/reports/overview` registry entry composing 4–6 existing metric ribbons/charts as cards (reuse bodies' hooks, dashboard_widget_config-style grid); NOT a new widget framework | M | P3 |
| RPT-039 | Project-hub dashboard has only 2 shallow test widgets | P2 | TestCasesOverviewWidget.tsx (2.7K) + TestCyclesProgressWidget.tsx (3.7K) vs 20+ delivery widgets in src/components/project-hub/dashboard/widgets/; no pass-rate, coverage, defect-density, or flaky widget | XR/ZS coverage widgets on non-test surfaces (14 §10.8) | Add 2–3 test KPI widgets (pass-rate trend, requirement coverage %, open defects by severity) reusing report hooks; register in dashboard_widget_config | M | P3 |
| RPT-040 | No planned-vs-actual execution effort report | P2 | 06 §4: `avg_execution_time` in RPCs only; no estimate field surfaced, no planned-vs-actual report | TR estimates + forecast (14 §6.6, §7.7) | Blocked on case-level estimate field existing (authoring domain); once present, `effort-variance` registry entry comparing estimates vs tm_test_runs durations (PLACEHOLDER: confirm estimate column lands in G-authoring) | M | P3 |
| RPT-041 | No dynamic/custom report builder (existing one is dead code) | P2 | 06 §3: src/components/reports/ReportBuilder.tsx + SavedReportsList + 7 siblings orphaned, never routed; no filter-based dynamic report engine | PT filter-tree dynamic reports, TR custom reports (14 §11.8) | v2 candidate: registry-driven "custom view" = saved filter + column set + grouping over the existing ReportData shape; do NOT revive the orphaned builder (hand-rolled, pre-dates JiraTable) | L | P3 |
| RPT-042 | No metrics API for BI consumption | P2 | All report math lives in browser hooks (06 §1); no metrics endpoints; BI teams would scrape | TR API / QT Insights API (14 §11.11) | Expose the aggregate RPCs from RPT-025/existing tm_* RPCs via a documented edge function (`report-metrics`) with service-token auth; ships naturally after RPT-025 | M | P3 |
| RPT-043 | No uncovered-requirements report scoped per release | P2 | Uncovered lists exist per project/sprint (ReportStatusView `uncoveredEmpty`, e.g. SprintTestingStatusBody.tsx:184) but nothing answers "is release 2.3 covered?" (14 §11.10) | XR requirement-coverage gap per release — the go/no-go report | `release-coverage-gap` registry entry: ph_issues in release scope (sprint_release JSONB / fix-version equivalent) left-joined to tm_requirement_links; blocked with RPT-018's identity decision for release scoping (PLACEHOLDER) | M | P3 |
| RPT-044 | Filter model inconsistent between Wired and dedicated bodies | P2 | WiredReportBody has date/cycle/folder/owner filters (WiredReportBody.tsx:8); dedicated bodies each own ad-hoc pickers (sprint select, project select) — no shared filter bar, different UX per report | TR/PT uniform report filter bar (14 §11.1 "each filterable") | Extract the WiredReportBody filter row into a shared `ReportFilterBar` component and adopt it in new bodies only; existing 10 dedicated bodies keep their pickers (reuse-not-refactor) until touched for other reasons | M | P3 |
| RPT-045 | Caty insight text is excluded from CSV/PDF exports | P3 | ReportExportMenu exports columns/rows only (ReportExportMenu.tsx:20–27 Props); ReportInsightCard content not passed | Vendor exports include summary/commentary blocks (14 §11.6) | Pass optional `insightText` prop from bodies to ReportExportMenu; append as a header paragraph in PDF and a comment row in CSV | S | P3 |
| RPT-046 | Formula/methodology explanations cover only Lab-derived reports | P3 | FORMULA_EXPLANATIONS map lives in reportDefinitions.ts:204–224 (20 lab slugs); the 10 dedicated bodies have no methodology disclosure | PT/TR document metric definitions in-product | Extend FORMULA_EXPLANATIONS with entries for the 10 dedicated slugs + render via the existing explanation affordance in the hub header | S | P3 |
| RPT-047 | No favorites/recents in the report navigator (26 reports, flat category list) | P3 | ReportNavigator lists 8 categories/26 entries (06 §1–2); no pinning, no recently-viewed | TR/PT dashboards pin frequent reports | Add localStorage-backed "Recent" section + star-to-pin (user_starred_items pattern exists per memory: starred-unification) at top of ReportNavigator | S | P3 |
| RPT-048 | No AI failure-clustering / cycle-close summary report | P3 | 14 §13.6: cycle-close report drafts itself, claims linked to runs; Catalyst has the AI gateway + report-insights edge fn (06 §1) but insight cards are per-report aggregates only | QT Launch analytics; emerging LLM summaries | New `cycle-close-summary` registry entry: aggregates failed runs + linked defects for a chosen cycle into the existing `report-insights` edge fn with a cycle-close prompt; aggregates-only payload preserved | M | P3 |
| RPT-049 | No natural-language query over reports | P3 | No NL entry point in hub; AI gateway exists (12_ai_gateway.md) | Rovo-direction NL → query, shown to user for trust (14 §13.8) | Delighter: NL box in hub header that maps to (report slug + filter params) via edge fn, navigating to the deep-link from RPT-035; compiled selection always shown before navigate | L | P3 |
| RPT-050 | Report/analytics dead code confuses future work: 3 dead Lab defs, 9 orphaned legacy components, orphaned evidence reports | P3 | 06 §2 (project-overview/metrics/activity defs with no renderer/registry) + §3 (src/components/reports/* 9 orphans; src/components/evidence/reports/ orphaned) | n/a — hygiene | Delete-or-decide slice: remove the 3 dead defs and the 9 unrouted legacy components (keep ExecutiveInsightStrip/ReportTable until RPT-004 lands); move-not-copy law applies | S | P3 |
| RPT-051 | No export/report-access audit trail | P3 | ReportExportMenu writes files client-side with no logging; 14 §5.8/§11.6 compliance bar expects export traceability | PT/QT compliance tiers log report exports | Insert a row into an `tm_report_export_log` table (report id, user, format, filter params) on export; admin-only viewer later | S | P3 |
| RPT-052 | Repository tree carries no health/coverage rollups (structure only) | P3 | 06 §4-adjacent + 14 §2.12: AIO folder stats / PT tree heatmaps; Catalyst folder tree shows counts at most (repository domain doc) | The tree itself communicates last-run health per folder | Folder-level pass-rate/last-run chips on the repository tree fed by a per-folder aggregate RPC; render as Lozenge, dash when never-run (zero-assumption) | M | P3 |
| RPT-053 | Incident MTTR report invisible from the TestHub reports navigator | P3 | 06 §3: MTTR lives at /incident-hub/reports (split out in Phase 2 Lane C); ReportNavigator has no cross-hub link — QA persona won't find it | PT single pane for quality reports | Add a non-report "external link" entry type to report-registry pointing at Routes incident-hub reports; navigator renders it with a shortcut icon | S | P3 |

## Severity counts
P0: 6 (RPT-001…006) · P1: 19 (RPT-007…023, 025, 026) · P2: 19 (RPT-024, 027…044) · P3: 9 (RPT-045…053) · Total: 53

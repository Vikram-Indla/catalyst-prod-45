# G03 — Gap Mining: Test Planning, Cycles, Environments, Builds, Milestones (Xray persona)

Feature: CAT-TESTHUB-PROD-20260703-001 · Gap-mining agent G03 · 2026-07-03
Inputs: discovery 01, 02, 03, 13, 14 (yardstick) + src/ spot-verification + prior research `docs/feature-builder/features/test-hub/gap-analysis.md` (deduped).
Severity: P0=broken/lying today · P1=table-stakes missing · P2=competitive · P3=delighter.

| ID | Title | Severity | Current state (file:line or discovery-doc evidence) | Target (vendor benchmark) | Proposed fix (concrete, Catalyst-canonical) | Effort S/M/L | Phase P1/P2/P3 |
|---|---|---|---|---|---|---|---|
| PLN-001 | Test Plans have no TestHub surface at all | P1 | `testHubRoutes` (routes.ts:114-135) has no plans builder; FullAppRoutes grep for TestPlansPage = 0 mounts under /testhub; only importer is orphaned `src/pages/releases/TestPlansPage.tsx` (13 §3) | XR Test Plan issue type as container above cycles (14 §6.1) | New `/testhub/plans` + `/testhub/:projectKey/plans/:planKey` routes; JiraTable list + ProjectPageHeader chassis; reuse `useTestPlansG26` hooks (renamed to test-management family); key-based routing via `plan_key` per slug contract | L | P1 |
| PLN-002 | "Clone plan" is a no-op stub that reports success | P0 | `useCloneTestPlan` barrel stub returns `{}` (test-management/index.ts:34-44); consumed by TestPlansPage.tsx:63,92 — UI toasts success, nothing happens (02 §3.1) | TR rerun milestone / XR plan clone (14 §6.8) | Implement real clone mutation (plan row + tm_plan_scope + tm_plan_team, zero results carried); delete the barrel stubs so importers hit the real hook | M | P1 |
| PLN-003 | Cycle list + KPI strip permanently empty by construction on releases TestCyclesPage | P0 | `useTestCycleList`/`useTestCycleListSummary` always return `[]`/`null` (test-management/index.ts:46-52, test-cycles/useTestCycleList.ts:6-8); consumed at pages/releases/TestCyclesPage.tsx:31-40,94,97 (02 §3.1) | Any competitor: cycle list reflects DB (14 §7.1) | Delete the stub hooks + the orphaned page (route already redirects, 13 §3); keep `/testhub/cycles` as the single cycle list; remove the 6 dead `cycleListKeys` invalidations (useBulkActions.ts:79/121, useInlineEdit.ts:91, useExecutionMutations.ts:105/135/165/196) | S | P1 |
| PLN-004 | Assignment table fabricates 85 fake rows on any query error | P0 | `generateMockAssignments()` returns 85 randomized rows with fake testers 'Ahmed S.', 'Sara M.' as the queryFn result on ANY caught error (useAssignmentTable.ts:16-74, 203-206) (02 §3.2) | TR/QT: real per-instance assignment grid (14 §7.3) | Delete mock generator; throw on error; SectionMessage + Retry per silent-query-error-sweep pattern; zero-assumption rendering law | S | P1 |
| PLN-005 | Assignment rows hardcode testType 'functional', discarding computed mapping | P0 | useAssignmentTable.ts:194 hardcodes `'functional'`; mapping logic at :169-173 computed then thrown away (02 §3) | TR: real case-type per row | Use the computed mapping; render dash when type unknown (zero-assumption) | S | P1 |
| PLN-006 | Calendar events all render 'Untitled'/'General'/'medium' with fake 30-min duration | P0 | useCalendarData.ts:46 select never fetches the `tm_test_cases.title/module/priority` fields mapped at :52-65; `estimatedDurationMinutes: 30` hardcoded (:64); 2 silent destructures (:20,:44) (02 §3.4) | QT plan calendar / TR milestones timeline (14 §6.10) | Fix the select to join tm_test_cases fields; duration from `tm_test_cases.estimated_time`; throw on error; delete 'Unknown Cycle' fallback | S | P1 |
| PLN-007 | No calendar/schedule surface mounted in TestHub at all | P2 | `test-cycles/calendar/` (12 files) imported only by orphaned releases CycleTableView (01 §4.3); no calendar route in testHubRoutes (grep routes.ts = /tasks/calendar only) | QT plan board/calendar view; TR milestone timeline (14 §6.10) | Add calendar view toggle on `/testhub/cycles` reusing the fixed useCalendarData + existing calendar components after canonical audit (ADS tokens, no hand-rolled UI) | M | P2 |
| PLN-008 | Cycle status enum is a 7-value split-brain FSM (planned vs draft, in_progress vs active) | P0 | `tm_cycle_status` = planned, in_progress, completed, archived, draft, active, paused (03 §3); two vocabularies coexist; `trg_validate_cycle_status_transition` guards transitions over the muddled set (03 §4) | ZS/AIO: single clean lifecycle Not started → In progress → Done (14 §7.5) | Migration: map draft→planned, active→in_progress; shrink enum to 5 states (planned, in_progress, paused, completed, archived); single transition map in one consolidated cycle mutations hook; StatusPill lozenge mapping | M | P1 |
| PLN-009 | Cycle status hand-maintained, never derived from instance results | P1 | Status set manually via useCycleMutations.ts:39-169 / useTestCycles.ts:624; counters exist (`trg_sync_cycle_scope_counters`, 03 §2.1) but never drive status | ZS/AIO: cycle state computed from instances; manual override audit-logged (14 §7.5) | DB trigger: derive in_progress when first run recorded, completed-eligible when not_run=0; UI prompts (not forces) transition; overrides logged to tm_cycle_execution_audit | M | P2 |
| PLN-010 | Cycle route resolver treats DB errors as "not found" | P0 | useTestCycleByKey.ts:21-22 `const { data } = …maybeSingle(); return data ?? null` — transient error indistinguishable from missing cycle → wrongful 404/redirect (02 §2 "highest user impact") | Table stakes: error ≠ empty | Destructure error and throw; CycleDetailPage renders SectionMessage + Retry on isError; spinner on isPending | S | P1 |
| PLN-011 | Three parallel cycle CRUD stacks + 9 query-key families with dead/miswired invalidations | P0 | useTestCycles vs useTestCyclesEnhanced (duplicate create/delete/clone) vs useCycleMutations; keys `['test-cycles']`, `['tm_test_cycles']`, `['cycle']` invalidated but never queried; cross-stack edits leave the other stack stale (02 §4, §7.2) | n/a (engineering hygiene enabling everything else) | Consolidate to one `useTestCycles` module with exported key factory; delete useTestCyclesEnhanced duplicates; sweep all invalidations to the factory | M | P1 |
| PLN-012 | "Reschedule test" silently reschedules the entire cycle | P0 | useTestReschedule.ts:17-46 updates `tm_test_cycles.planned_start/end`, not per-test rows; 1 silent destructure (:38) (02 §1) | TR: per-instance due dates (14 §7.1 instance fields) | Rename hook/action to rescheduleCycle; per-instance scheduling uses existing `tm_cycle_scope.due_date`; fix silent destructure | S | P1 |
| PLN-013 | Add-set-to-cycle writes to a table that has no migration and no type | P0 | SetDetailPage.tsx:300 (insert) / :411 (select) hit `tm_cycle_sets` via `as any`; no CREATE TABLE in any migration, absent from types.ts (03 §9) — likely runtime failure | TR run-from-set; ZS cycle from set | DB probe cyij for tm_cycle_sets; if absent, migration creating it (cycle_id, set_id, UNIQUE pair) or replace with expand-set-into-tm_cycle_scope at add time (preferred — one membership model) | S | P1 |
| PLN-014 | Plan↔cycle linking split-brain: untyped `plan_test_cycles` vs canonical `test_plan_id` FK | P0 | useTestPlansG26.ts:434/462/481 read/write `plan_test_cycles` (NOT in types, `as any`) while schema has `tm_test_cycles.test_plan_id → tm_test_plans` (03 §2.1, 02 §1) | XR: plan contains executions natively (14 §6.1) | Consolidate on `tm_test_cycles.test_plan_id`; migrate any plan_test_cycles rows; delete the untyped table access | M | P1 |
| PLN-015 | Plans→cycles→runs hierarchy not navigable anywhere | P1 | CycleDetailPage never shows parent plan (no plan hook among its imports, 01 §3.2); no plan detail page exists under /testhub; `get_plan_progress` RPC exists unused by any live page (02 §1 useTestPlansG26) | XR/QT: plan rolls up child cycles with consolidated verdict (14 §6.1, 6.5) | Plan detail page: overview + child-cycles JiraTable + progress rollup via get_plan_progress; breadcrumb + plan link on CycleDetailPage header | L | P1 |
| PLN-016 | No plan-level aggregation/dedup policy (test in 3 cycles counts 3×) | P2 | get_plan_progress internals unread; no latest/worst-status-per-case semantics anywhere in hooks (02 §6) | XR: deduplicated plan rollup by latest/worst per configurable policy (14 §6.5) | PLACEHOLDER: read `get_plan_progress` SQL — then implement latest-run-per-case dedup view `v_tm_plan_case_status` and surface policy toggle on plan detail | M | P2 |
| PLN-017 | No rerun-failed cycle creation | P1 | grep `rerun\|failed_only` across useTestCycles/useTestCyclesEnhanced/pages/testhub/cycles = 0 matches; clone exists (useTestCycles.ts:344) but copies full scope | TR "rerun failed" one click, preserving assignments (14 §7.2) | Extend clone mutation with `statusFilter: ['failed','blocked']` + keepAssignments flag; "Re-run failed" row action on CyclesPage + CycleDetailPage | M | P1 |
| PLN-018 | Two duplicate cycle-clone implementations with different behavior | P1 | `clone` (useTestCycles.ts:344, 1 silent destructure :359) vs `cloneEnhanced` (useTestCyclesEnhanced.ts:416), separate query keys (02 §7.2) | n/a | Single clone mutation (part of PLN-011 consolidation) with options {resetResults, keepAssignments, statusFilter}; fix silent scope read | S | P1 |
| PLN-019 | No create-cycle-from-folder/filter/set scope builder | P2 | Add-tests in CycleDetailPage is inline hand-picking (01 §3.2, 13 §5 "UNKNOWN exact mechanism — page is self-contained"); orphan TestRepositoryBrowser/AddTestsSlideOver only in releases cluster | TR/XR: cycle from folder, saved filter, previous failures (14 §7.2, 2.2) | Scope-builder step in cycle create/detail: sources = folder subtree (useFolderTree), test set (tm_set_cases), saved filter; bulk insert via `tm_bulk_add_cases_to_cycle` RPC (exists, 03 §8) | M | P2 |
| PLN-020 | Cycle create modal has zero environment field; env model is text/FK split-brain | P1 | grep 'environment' in CyclesPage.tsx = 0 matches; tm_test_cycles carries BOTH free-text `environment` and `environment_id → tm_environments` (03 §2.1) | TR configurations / XR test environments; central catalog, no free-text drift (14 §14.13) | Add environment Select (tm_environments via useAdminConfig, already CLEAN) to cycle create/edit; backfill text→FK migration; deprecate the text column | M | P1 |
| PLN-021 | No per-instance environment/configuration — env is cycle-level only | P2 | `tm_cycle_scope` columns: assigned_to, current_status, due_date, priority, sort_order — no environment (03 §2.1) | TR configurations, XR environments: same case appears once per config, results separable (14 §7.4, 3.9) | Add `environment_id` to tm_cycle_scope (nullable, defaults to cycle env); scope-builder option "multiply by environments"; UNIQUE constraint becomes (cycle_id, test_case_id, environment_id) | L | P2 |
| PLN-022 | No configuration-matrix entity (browser × OS × env) | P3 | `tm_environments` is flat name/url admin list (TESTHUB_BUILD_HANDOVER.md:60); no config-group concept in schema (03 §2) | TR configuration groups multiply assigned runs (14 §3.9) | `tm_config_groups` + `tm_configs` schema; matrix expansion at cycle-build; defer until PLN-021 lands | L | P3 |
| PLN-023 | No build/version tagging on execution — "which build broke it" unanswerable | P2 | tm_test_runs has environment_snapshot Json + no build column; `found_in_build` exists only on tm_defects (03 §2.2); grep build in ExecutionPage = 0 | TR "tested-on build" per instance; XR/QT build+SHA metadata on results (14 §7.7, 12.10) | Add `tested_build text` to tm_test_runs; cycle-level default build field; runner header input; pipe into defect `found_in_build` on inline defect create | M | P2 |
| PLN-024 | Runner never captures environment at execution time | P2 | `tm_test_runs.environment_snapshot Json` exists (03 §2.1) but ExecutionPage.tsx imports show no env capture (01 §3.2) — PLACEHOLDER: verify no write path exists | TR/XR: result records its environment (14 §7.4) | On recordResult, snapshot {environment_id, name, build} into environment_snapshot; display on run history | S | P2 |
| PLN-025 | Cycle release binding points at legacy `releases` table no live surface displays | P0 | `tm_test_cycles.release_id → releases(id)` (03 §2.1); live release UX runs on ph_releases/rh_releases; zero live release surface consumes tm_* data (13 §2, §4.1) | XR fixVersion binding feeds release readiness (14 §6.2) | PLACEHOLDER: Vikram decision — repoint FKs to ph_releases vs mapping table vs sync job (13 §7.1). Blocking prerequisite for any release-scoped planning/reporting | L | P1 |
| PLN-026 | Dual sprint-linkage models make sprint-scoped planning reports disagree with cycle records | P1 | tm_* `sprint_id → ph_jira_sprints` FK (20260627192541) vs reports resolving scope from `ph_issues.sprint_release` JSONB (SprintTestingStatusBody.tsx:4) (13 §6) | XR: one sprint scope definition across planning + reporting | Bridge view `v_sprint_test_scope` joining both models; reports and cycle filters consume the view; document canonical = FK | M | P2 |
| PLN-027 | Dual legacy release FKs on tm_test_cases (release_id + release_version_id) | P1 | Both survive (bootstrap 80778, 80786); release_version_id explicitly deprecated in comment only (useTestCaseRelease.ts:20-22) (13 §2, §7.7) | n/a (hygiene) | Migration dropping `release_version_id` after data check; ties into PLN-025 decision | S | P1 |
| PLN-028 | Bulk/round-robin assignment unusable from TestHub — component family orphaned | P1 | `assignment-table/` (12 files) imported only by orphaned releases CycleTableView (01 §4.3); CycleDetailPage has no bulk assignment (grep workload/capacity = 0); RPCs `tm_bulk_assign_tester`/`tm_assign_tester_to_cycle` exist unused (03 §8) | TR/QT: distribute 400 instances across 6 testers in seconds (14 §7.3) | Assignment tab on CycleDetailPage: JiraTable of scope + multi-select bulk assign (round-robin, by-folder) calling tm_bulk_assign_tester; reuse fixed useAssignmentTable (post PLN-004) | M | P1 |
| PLN-029 | Tester workload invisible during planning | P2 | `tm_get_cycle_team_workload` RPC + useCycleTeamWorkload (CLEAN, realtime) consumed only by orphan cycle-command-center (02 §1, 13 §5) | TR workload view per tester (14 §7.3) | Mount workload summary (per-assignee counts/bars via canonical progress components) in the CycleDetailPage assignment tab | S | P2 |
| PLN-030 | No capacity/effort forecasting (estimates never roll up) | P2 | `tm_test_cases.estimated_time` exists (03 §2.1); no Σ-estimate vs availability view anywhere; `tm_get_cycle_execution_velocity` RPC + hook exist unused by live pages (02 §1) | TR estimates + forecast completion date from execution pace (14 §6.6, 11.9) | Cycle forecast card: remaining = Σ estimates of not_run; pace from velocity RPC; projected finish vs planned_end with warning lozenge | M | P2 |
| PLN-031 | My Work shows authored cases, not the tester's execution queue | P1 | MyWorkPage = BacklogPage + `useTestCasesSource` adapter over tm_test_cases (01 §3.1); view `v_tm_my_work` exists in types (03 §2) unused | TR todos / QT sessions: "give me my next unexecuted instance" (14 §8.8) | Add "My executions" tab/adapter on MyWorkPage sourcing v_tm_my_work (assigned tm_cycle_scope rows, status, due_date, cycle link); Execute row action → runner | M | P1 |
| PLN-032 | Cycle completion doesn't lock results — closed cycles silently editable | P2 | grep lock in CycleDetailPage = 0; only `trg_lock_scope_version` (version pinning) exists (03 §4); no trigger/RLS blocks result writes on completed/archived cycles | QT/PT: closed means closed; reopen is permissioned + logged (14 §7.11) | Trigger rejecting tm_test_runs/tm_cycle_scope writes when parent cycle completed/archived; "Reopen" permissioned action writing tm_cycle_execution_audit; read-only banner in UI | M | P2 |
| PLN-033 | Per-instance run history invisible in cycle views | P2 | tm_test_runs.run_number retains history (03 §2.1) but CycleDetailPage scope table and ExecutionPage show current status only (01 §3.2 — no history UI listed) | XR execution history / TR result history per test-in-run; fail-then-pass fully visible (14 §7.6) | Run-history inline dialog per scope row (runs + step results + executor + duration), fed by existing tm_test_runs query in useCycleExecutionItems | M | P2 |
| PLN-034 | Milestones: schema exists, zero TestHub UI; plan milestones table possibly dropped | P2 | `tm_cycle_milestones` typed (03 §2); `tm_plan_milestones` created 20260211183753:59 but NOT in types (03 §9); MilestoneEditor lives in Releases-side features/test-cycles (01 §4.4); grep milestone in pages/testhub = handover doc only | TR milestones with dates, overdue highlighting, completion rollup (14 §6.3) | DB probe tm_plan_milestones; milestone section on plan+cycle detail (dates, done-state, overdue lozenge) reusing MilestoneEditor after canonical audit | M | P2 |
| PLN-035 | Overdue cycles never highlighted | P2 | tm_test_cycles.planned_end exists; no overdue computation/badge on CyclesPage (563 LOC, JiraTable, no date-derived signal per 01 §3.2) | TR milestones overdue highlighting (14 §6.3) | Derived "Overdue" warning Lozenge on CyclesPage rows + filter chip when planned_end < today and status ∉ {completed, archived} | S | P2 |
| PLN-036 | Entry/exit criteria are free text with no gating | P3 | tm_test_plans has entry_criteria/exit_criteria text columns (03 §2.3); no checklist state, nothing gates plan closure | QT/PT: criteria are checklist items whose state gates closure (14 §6.7) | Structured criteria Json [{text, done, doneBy, doneAt}]; plan close transition warns/blocks on unmet blocking criteria | M | P3 |
| PLN-037 | Plan approvals workflow built in DB, no UI | P2 | tm_plan_approvals + notify trigger `trg_notify_tm_plan_approvals` (03 §2.3, §4); approvals mutations exist in useTestPlansG26 (02 §1); no live page consumes them | QT/PT: approval recorded (who/when) before execution (14 §16.2) | Approvals section on new plan detail page: request/approve/reject with Avatar + timestamp; status lozenge on plans list | M | P2 |
| PLN-038 | Plan versioning captured by trigger, invisible to users | P3 | tm_plan_versions + `trigger_plan_version` (03 §2.3); no version history UI for plans anywhere | ZS/QT: versions citable, restorable (14 §5.2) | Version history tab on plan detail (read-only list + snapshot view); restore deferred | S | P3 |
| PLN-039 | Templates exist in schema (plans + runs) with no create-from-template UX | P2 | tm_test_plans.is_template + template_id self-FK, tm_run_templates + `tm_create_run_from_template` RPC (03 §2.3, §8); clone-from-template mutation in useTestPlansG26 unused by live UI | TR templates per project (14 §1.7); XR plan clone (6.8) | "Start from template" option in plan create dialog + "Save as template" action; cycle create gains template select calling the RPC | M | P2 |
| PLN-040 | No ad-hoc/unscripted entries in a cycle | P3 | tm_cycle_scope requires test_case_id, UNIQUE(cycle_id, test_case_id) (03 §2 keys) | PT/XR generic tests: quick exploratory item without a library case (14 §7.8) | "Quick add" creates a minimal generic tm_test_case (type=exploratory, folder=Ad-hoc) then adds to scope — no schema change | M | P3 |
| PLN-041 | Timeline is flat cycles only — no plan→cycle hierarchy, creation disabled | P3 | TestHubTimelinePage (100 LOC): create/child/menu/detail disabled, "flat cycles" (01 §3.1) | QT plan board/timeline of cycles (14 §6.10) | After PLN-014/015: plans as parent rows, cycles as children; enable detail panel (CatalystViewTestCycle already exists) | M | P3 |
| PLN-042 | Cycle dependencies tracked but never enforced in lifecycle | P3 | tm_test_cycle_dependencies + DependenciesPage live (01 §3.1); nothing checks dependencies on cycle start (no reference in useCycleMutations, 02 §1) | Beyond most competitors — Catalyst-native differentiator | Warning on transition to in_progress when a blocking dependency cycle is unfinished (read tm_test_cycle_dependencies in transition mutation) | S | P3 |
| PLN-043 | Three cycle-create implementations (one live hand-rolled, two orphaned) | P1 | CyclesPage inline modal with hand-rolled grid-form layouts (:357-384) vs orphan CreateCycleModal + CreateCycleModalEnhanced (13 §5) | n/a (single canonical modal) | One canonical CreateCycleModal component (atlaskit ModalDialog + Form) used by CyclesPage; delete orphan pair with the releases cluster cleanup | M | P2 |
| PLN-044 | Cycle scope list is a raw `<table>` — JiraTable rule violation on the core planning surface | P1 | CycleDetailPage.tsx:423 raw table (01 §3.2, R4); handover mandates JiraTable (TESTHUB_BUILD_HANDOVER.md:338) | n/a (repo law) | Replace with JiraTable<ScopeRow> (columns: key, title, assignee avatar, status pill, env, due date, run history action) | M | P1 |
| PLN-045 | Zero error states across Cycles/CycleDetail/Execution pages | P1 | CycleDetailPage 24 loading hits, zero error renders; CyclesPage 7; ExecutionPage 8 (01 §7); silent-sweep never covered testhub | Table stakes: failure ≠ empty | Apply the silent-query-error-sweep pattern (isError → SectionMessage + Retry, isPending → Spinner) to all three pages + the 5 planning-domain silent destructures (useTestCycles:359, useAddTestsToCycle:20/51, useSmartAssignment:49, useTestReschedule:38) | M | P1 |
| PLN-046 | Smart assignment computes suggestions on silently-empty workload data | P0 | useSmartAssignment.ts:49 silent `{data: workloadData}`, 0 throws — on error, suggestions computed over wrong (empty) workload (02 §1) | TR/QT: assignment suggestions grounded in real workload | Throw on error; disable suggestions UI with SectionMessage when workload query fails | S | P1 |
| PLN-047 | tm_scheduled_runs migration-only; no scheduling of cycles/runs exists | P3 | Created 20260121134233, NOT in types (03 §2.3, §9); no UI/hook references | QT Launch scheduled orchestration; minimum = scheduled cycle start reminders (14 §12.11) | PLACEHOLDER: probe cyij for tm_scheduled_runs — then decide drop vs build (scheduled cycle activation + notification via catalyst_notify) | M | P3 |
| PLN-048 | Role-based cycle assignment table typed but unconsumed | P2 | tm_cycle_assignments in types (03 §2); zero hooks read it (absent from 02 master table consumers); parallel to per-instance tm_cycle_scope.assigned_to | TR/QT role-based distribution | Decision: fold role-assignment into PLN-028 assignment tab (assign-by-role using tm_cycle_assignments) or drop the table; PLACEHOLDER: check row counts on cyij | S | P2 |
| PLN-049 | Set membership split-brain corrupts set→cycle planning counts | P1 | `tm_set_cases` (src ×4) vs `tm_test_set_cases` (src ×0 but owns count trigger `trigger_update_test_set_count`) — test_count maintained on the table nobody writes (03 §9) | TR: set counts trustworthy for cycle sizing | Consolidate on tm_set_cases; repoint count trigger; drop tm_test_set_cases after data merge | M | P1 |
| PLN-050 | Plan membership also duplicated (tm_test_plan_cases vs tm_plan_scope) | P2 | Both live in types (03 §2.3); useTestPlansG26 writes tm_plan_scope; tm_test_plan_cases has no src consumer found (02) | n/a (one membership model) | Consolidate on tm_plan_scope; migration merging any tm_test_plan_cases rows; drop the orphan | S | P2 |
| PLN-051 | No dynamic plan/cycle membership by query | P3 | tm_plan_scope is static rows only; tm_test_sets has dynamic_criteria Json + membership_type enum (03 §2.3) but dynamic resolution is unwired (prior gap-analysis: "UI for smart sets unclear") | XR add-by-JQL; PT filter-based test sets resolve at build time (14 §6.4) | Scope row type 'filter' storing saved_filters id; resolved into concrete tm_cycle_scope rows at cycle-build with preview count | L | P3 |
| PLN-052 | Cycle-level defect/result summary panel missing from live cycle detail | P2 | Counters rendered ad hoc (CycleDetailPage.tsx:94-95, 278); the standup-ready KPI/donut/summary components (CycleKPICards, StatusDonutChart, ReleaseTestSummaryPanel) all orphaned in releases cluster (13 §3, §5) | AIO/TR: live counts by status + defect totals without opening a report (14 §7.10) | KPI strip on CycleDetailPage header: executed %, pass %, blocked, open defects (from `tm_get_release_test_summary`-style cycle RPC `tm_get_cycle_execution_summary`, exists 03 §8) | S | P2 |
| PLN-053 | Sets navigation broken + UUID routes block set-driven planning | P0 | TestSetsPage.tsx:435 navigates `/testhub/${projectKey}/sets/${set.id}` — route doesn't exist (FullAppRoutes.tsx:679-680); `:id` UUID param violates slug contract (01 §1.3) | ZS/AIO: key-addressable entities (14 §2.8) | Route sets by `set_key` (`/testhub/:projectKey/sets/:setKey`), register builder in routes.ts, useSetByKey hook; same pattern reserved for plans (plan_key) | S | P1 |
| PLN-054 | No cycle-wide setup/preconditions surfaced to testers | P3 | tm_test_cycles has description + environment only (03 §2.1); runner shows nothing cycle-level before step 1 | PT test-set level fields / TR run description; setup stated once per cycle (14 §4.9) | Render cycle description + environment + build banner at top of ExecutionPage; optional `setup_notes` rich-text column | S | P3 |
| PLN-055 | Assignment events don't produce an actionable tester digest | P3 | `catalyst_notify_tm_cycle_scope` trigger exists (03 §4) — per-row notifications only; no "you have 14 instances" digest or queue burn-down (14 §16.9) | TR todos digest; AIO assignment notifications (14 §8.8, 16.9) | Daily digest edge fn aggregating open tm_cycle_scope per assignee → catalyst notification; deep-link to My-executions tab (PLN-031) | M | P3 |
| PLN-056 | Releases hook trio fragments the release picker used in planning | P2 | test-management/useReleases (react-query) vs testhub/useReleases (manual useState, zero cache integration — stale until remount) vs releases/* hooks (02 §7.3) | n/a | Single useReleases hook post PLN-025 identity decision; delete the manual-state outlier | S | P2 |
| PLN-057 | Cycle environment shown inconsistently: Releases-side joins FK, TestHub reads raw text | P2 | useCycleDetails.ts:62 joins tm_environments(id,name); TestHub CycleDetailPage renders the free-text column (grep environment in CyclesPage = 0; text col per 03 §2.1) | One environment truth (14 §14.13) | Covered by PLN-020 backfill; sweep all cycle reads to environment_id join after migration | S | P1 |
| PLN-058 | No risk-based scoping — nothing lets a lead cut scope defensibly | P3 | No risk field on tm_test_cases/tm_plan_scope (03 §2.1 column list); no prioritized-cut view | PT risk fields + filter trees (14 §6.11); Catalyst AI leapfrog candidate (14 yardstick note 3) | Risk score field (custom_fields Json initially) + "trim to top-N by risk" action in scope builder; AI-suggested ranking via existing gateway later | L | P3 |
| PLN-059 | Cycle-clone doesn't offer reset-results/keep-assignments choices | P2 | clone (useTestCycles.ts:344) copies scope wholesale; no options surface in CyclesPage row actions (01 §3.2) | TR/ZS: regression round N+1 setup trivially repeatable with options (14 §7.9) | Clone dialog with checkboxes {keep assignments, keep environments, carry only failed/blocked} on the consolidated clone mutation (with PLN-017/018) | S | P2 |
| PLN-060 | Sprint select exists on cycle create but nowhere else in the planning chain | P2 | CyclesPage.tsx:271-293 writes sprint_id; plans dialog BasicInfoTab has sprint select but is unmounted (13 §6); no sprint filter on CyclesPage list, no sprint column | XR: sprint-scoped test planning end-to-end | Sprint column + filter on CyclesPage JiraTable; sprint field on plan create once plans surface lands (PLN-001) | S | P2 |

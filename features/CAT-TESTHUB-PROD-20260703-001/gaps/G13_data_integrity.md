# G13 — Data Integrity & Wiring Correctness Gaps

Feature: CAT-TESTHUB-PROD-20260703-001 · Gap-mining agent G13 · 2026-07-03
Inputs: discovery 02/03/04/07/10 + 14 (yardstick, cited as `14 §n.m`). Severity: P0 = broken/lying today on a routed surface; P1 = live integrity risk / table-stakes correctness; P2 = dead-code landmine, split-brain hygiene; P3 = minor.

| ID | Title | Severity | Current state (file:line or discovery-doc evidence) | Target (vendor benchmark) | Proposed fix (concrete, Catalyst-canonical) | Effort S/M/L | Phase P1/P2/P3 |
|---|---|---|---|---|---|---|---|
| DAT-001 | Route resolver swallows errors → wrongful 404 on cycle URLs | P0 | useTestCycleByKey.ts:21-22 — `const {data} = await query.maybeSingle(); return data ?? null` (02 §2: "highest user impact"); also `supabase as any` cast at :14 | All vendors: key-based entity URLs load or error, never mis-404 (14 §2.8, §16.7) | Destructure error and throw; resolver distinguishes isError (SectionMessage+Retry) from genuine not-found (404); remove `as any` | S | P1 |
| DAT-002 | 40 silent `{data}`-only destructures across 16 hook files render every backend failure as "empty" | P0 | 02 §2 full catalog: useTestCases ×11, useDefects ×10, useTestCaseVersions, useTestCycles, useAddTestsToCycle ×2, useCycleTestCases, useExecutionMutations, useTestRepository ×2, useTestReschedule, useSmartAssignment, useCalendarData ×2, useAssignmentTable, useTestPlansG26, useDefectsG25 ×2, useAutoVersioning | Errors surface as errors — repo-wide contract already established (07 L11, silent-query-error-sweep) | Apply the sweep pattern to all 40: destructure `error` + throw in queryFn; consumers render isError→SectionMessage+Retry, isPending→spinner; binary acceptance row per hook | M | P1 |
| DAT-003 | 3 error-as-empty blocks return `[]` on query failure (execution/defect history) | P0 | useTestCaseExecutionHistory.ts:36-39 (`console.error; return []`), useDefectsG25.ts:184, :197 (02 §2) | Execution history must be trustworthy for regression judgment (14 §7.6, §8.11) | Replace `return []` with `throw error`; UI error state per DAT-002 contract | S | P1 |
| DAT-004 | Barrel no-op stubs `useCreateTestCase`/`useDeleteTestCase`/`useCloneTestPlan` shadow real hooks — any importer gets a mutation that "succeeds" doing nothing | P0 | test-management/index.ts:34-44 (02 §3.1); real implementations exist at useTestCases.ts:323/709; pages/releases/TestPlansPage.tsx:63,92 consumes the clone stub (dead family per 10 §dead-inventory, but barrel is importable by anyone) | Mutations that report success must persist (zero-assumption law) | Delete the stubs from index.ts; re-export the real hooks; grep all barrel importers and repoint; tsc gate | S | P1 |
| DAT-005 | `useTestCycleList`/`useTestCycleListSummary` stubs return `{data:[]}`/`{data:null}` always | P2 | test-management/index.ts:46-52 + test-cycles/useTestCycleList.ts:6-8 (02 §3.1/§3.3); sole page consumer pages/releases/TestCyclesPage.tsx is unrouted (10 §dead-inventory) | Cycle list is the atomic unit of a testing round (14 §7.1) | Delete stub hooks + the dead consumer page in the DAT-047 legacy purge; routed /testhub/cycles already uses real useTestCycles | S | P2 |
| DAT-006 | Dead invalidations: `cycleListKeys` factory invalidated by 3 mutation files but no query ever uses the key | P1 | useTestCycleList.ts key factory; invalidators useBulkActions.ts:79/121, useInlineEdit.ts:91, useExecutionMutations.ts:105/135/165/196 (02 §3.3, §1 row 54) | — | Delete factory with DAT-005; repoint the 6 invalidation sites to the canonical cycle key family (DAT-007) | S | P1 |
| DAT-007 | Query-key fragmentation: ≥9 key families + 1 typo'd key over the same cycle data; ['test-cycles'], ['tm_test_cycles'], ['cycle'] invalidated but never queried | P1 | 02 §4: dead invalidations at useCycleMutations.ts:30, useTestReschedule.ts:26/56, useAddTestsToCycle.ts:73/77, useApplyAssignment.ts:63 | — | Single `cycleKeys` factory module (pattern: cycleExecutionKeys); codemod all cycle hooks onto it; ESLint rule banning raw-string cycle keys | M | P1 |
| DAT-008 | Cross-stack stale caches: useTestCycles mutations don't invalidate `cycle-details`/`cycle-test-cases`; useCycleMutations covers the reverse set only | P1 | 02 §4 last bullet | Cycle state is computed and consistent everywhere (14 §7.5) | After DAT-007, all cycle mutations invalidate the canonical family root prefix | S | P1 |
| DAT-009 | Race-prone defect key generation: client-side MAX-scan of all defect_keys, bypasses atomic RPC | P0 | useDefects.ts:135-151 `generateDefectKey` (02 §7.4); tm_next_entity_key RPC already used by useTestCases.ts:307, useTestCycles.ts:76 | Stable unique per-project keys (14 §2.8) | Replace scan with `tm_next_entity_key(project_id,'DEF')`; two concurrent creates can no longer mint duplicate keys (UNIQUE(project_id,defect_key) then 409s instead of corrupting) | S | P1 |
| DAT-010 | Defect auto-link chain inserts 4 columns absent from generated types (`link_type, linked_id, entity_label, link_source` on tm_defect_links) — batch insert silently fails | P0 | useDefects.ts:353-361 vs types.ts:42887-42994 (04 D3); failure hidden by `{data}` destructure | Bidirectional defect↔test links that survive (14 §9.3) | Probe cyij live; either migrate the columns (staging-first, RLS-checked) or rewrite the insert to the real schema; regen types; remove silent destructure | M | P1 |
| DAT-011 | Defect-create requirement-derivation queries `tm_requirement_tests`/`tm_requirements` — absent from types, likely dropped on cyij | P0 | useDefects.ts:442-444 (04 D3); 03 §9: migration-created 20260403185357 but NOT in types | Requirement→failing-test→defect chain (14 §10.10) | Live probe; rewrite derivation against tm_requirement_links (the canonical table) and delete the dead relation reads | M | P1 |
| DAT-012 | Cycle→plan traversal uses nonexistent `plan_test_cycles` despite a real `tm_test_cycles.test_plan_id` FK | P1 | useDefects.ts:394 + useTestPlansG26.ts:434/462/481 `as any` (04 3c; 03 §9) | — | Rewrite joins on test_plan_id FK; DECIDE create-or-delete for plan_test_cycles as G26's plan↔cycle m2m — PLACEHOLDER: does cyij have the table out-of-ledger? | S | P1 |
| DAT-013 | `tm_cycle_sets` has no migration and no type — routed "add set to cycle" fails at runtime, error silently renders "0 cycles" | P0 | SetDetailPage.tsx:300 (insert), :411 (select), both `(supabase.from('tm_cycle_sets') as any)` (03 §9; 07 L9 confirms live silent failure) | Create cycle from set / set↔cycle linkage (14 §7.2) | CREATE TABLE migration on cyij first (id, cycle_id FK, test_set_id FK, UNIQUE pair, 4 tm_user_has_access RLS policies); regen types; remove casts; error-surface per DAT-002 | M | P1 |
| DAT-014 | Set-membership split-brain: src writes `tm_set_cases` (×4) but the count trigger lives on `tm_test_set_cases` (src ×0) → `test_count` never syncs | P1 | 03 §9 duplicates; 07 L5: test_count "=0 forever", PGRST200 on embed | Set/tree stats must be trustworthy (14 §2.12) | Canonicalize tm_set_cases; move `trigger_update_test_set_count` onto it (or derive live COUNT); drop tm_test_set_cases after data check | M | P1 |
| DAT-015 | Two defect data layers over the same tm_defects table (`tm-defect*` vs `g25-*` keys) with zero cross-invalidation | P1 | test-management/useDefects.ts vs useDefectsG25.ts (02 §7.1); G25 UI deleted but hook alive via lib/shared-quality (07 L20) | One source of truth per entity | Retire useDefectsG25 + lib/shared-quality/hooks/useDefects.ts; migrate remaining importers to the tm-defect stack; delete g25 keys | M | P2 |
| DAT-016 | Three parallel cycle CRUD stacks: useTestCycles vs useTestCyclesEnhanced (duplicate create/delete/clone) + useCycleMutations | P1 | 02 §7.2, §1 rows 36-37, 51 | — | Consolidate to one cycle hook module on the DAT-007 key factory; delete the Enhanced duplicate after importer repoint | M | P2 |
| DAT-017 | testhub/useReleases bypasses react-query entirely (useState/useEffect) — external invalidations of `['releases-list']` never refresh it; stale-until-remount | P1 | testhub/useReleases.ts:48-56, 147-204 (02 §1 row 64, §7.3) | — | Port to useQuery/useMutation on the releases key family; collapse the ×3 release stacks (test-management/useReleases, testhub/useReleases, releases/*) | M | P2 |
| DAT-018 | Version snapshots can silently fail — 0 throws in useAutoVersioning; insert error captured, never surfaced | P1 | useAutoVersioning.ts:50 (silent read), :77 (swallowed insert error) (02 §1 row 22) | Execution records bind permanently to the case version executed (14 §5.3) — silent snapshot loss breaks the audit chain | Throw on read+insert errors; toast failure; add version-created assertion to the save flow acceptance | S | P1 |
| DAT-019 | Execution-history/count enrichment reads legacy dead `test_cycle_executions` with errors swallowed → counts silently 0 | P1 | useTestCases.ts:118-136, 216-217; useTestCaseExecutionHistory.ts:24 (02 §5; 03 §10: test_* family "ALL DEAD 0 rows") | Per-case run history is table-stakes (14 §7.6) | Rewire history/counts to tm_test_runs joined via tm_cycle_scope; delete legacy-table reads; throw on error | M | P1 |
| DAT-020 | Defect history joins legacy `th_test_executions` via tm_defect_links.test_run_id — run-id vs execution-id semantic mismatch | P2 | useDefectsG25.ts:193 (02 §5; 03 §10) | — | Dies with DAT-015 retirement; if any consumer survives, join tm_test_runs by id | S | P2 |
| DAT-021 | Canonical execution spine hard-FKs into a legacy family: `tm_test_runs.test_data_row_id → test_data_rows` | P1 | types.ts Relationships (03 §10); consumers useTestData.ts, useDataRowResults.ts, useCreateRunWithDataRows.ts:246 | Datasets are first-class with iteration-level results (14 §3.2-3.4) | Migrate test_data_parameters/test_data_rows into tm_* (tm_dataset_params/tm_dataset_rows), backfill, repoint FK, retire legacy reads — staging cyij first | L | P2 |
| DAT-022 | useTestPlansG26 blankets every `.from()` with `as any` — typegen cannot catch any schema drift in the whole plans stack | P1 | useTestPlansG26.ts (02 §1 row 67: "every .from() is as any") | — | Regen types, remove all casts, fix surfaced errors (incl. DAT-012 decision); add lint/CI grep banning `from('tm_` + `as any` | M | P1 |
| DAT-023 | 12 migration-created tm_ tables absent from types (tm_requirements, tm_requirement_tests, tm_scheduled_runs, tm_plan_milestones, tm_saved_filters, tm_notifications, …) — likely dropped on cyij while code/docs assume them | P1 | 03 §9 comm-diff list; typegen is recent (tm_ai_usage_log restored 20260703280000 present) making drops likely real | — | One live probe per table on cyij; per-table DECIDE create-DDL vs delete-code; record in 09_DECISIONS — PLACEHOLDER: probe results | M | P1 |
| DAT-024 | Denormalized counters unproven/fragile: cycle counters drifted once (total_cases 4 vs real 3), tm_test_sets.test_count stuck 0, tm_folders.case_count + tm_test_plans counters unaudited | P1 | 07 L5 (drift incident, D12); 03 §2.1/§4 trigger inventory | Live counts, standup-ready (14 §7.10) | Counter-audit slice: per counter prove exactly one maintenance trigger OR replace read with live COUNT / v_tm_cycle_progress view; backfill drifted values | M | P1 |
| DAT-025 | FK family split: 5 tm_* tables FK `project_id→projects` instead of tm_projects (insert 23503 risk on every future write) | P1 | 07 L6: tm_audit_logs, tm_gate_templates, tm_run_templates, tm_scheduled_runs, tm_step_definitions; tm_test_sets already bit once (D10) | — | One-shot information_schema FK audit + repoint migration (staging-first); dedupe the duplicate "Senaei BAU" tm_projects rows first | S | P1 |
| DAT-026 | RLS enabled with zero policies = default-deny rendering as "empty" — bit tm_set_cases once; remaining 55+ tm_* tables unaudited | P1 | 07 L7 (42501 incident, D11); 03 §5: 110 policies vs 67 RLS-enabled tables — coverage unproven per-table | Role-based access that fails loudly, not blankly (14 §14.5) | Phase-0 sweep: pg_policies vs pg_class.relrowsecurity for all tm_*; every new table ships the canonical 4 tm_user_has_access policies in the same migration | S | P1 |
| DAT-027 | Legacy triggers referencing dead columns 400'd ALL writes twice; untouched tm_* tables still unprobed | P1 | 07 L4: auto_create_test_case_version (42703 on every case UPDATE), 2 audit triggers on tm_cycle_scope reading dead test_cases.test_key | — | Phase-0 trigger forensics: enumerate pg_trigger per tm_* table + authed INSERT/UPDATE/DELETE write-probe before any phase builds on that table | M | P1 |
| DAT-028 | Enum casing mismatches matched 0 rows / 400'd writes; ExecutionPage run-status uppercase fix was deferred and is UNVERIFIED | P1 | 07 L8: SetDetailPage 'PLANNED'/'IN_PROGRESS' bug (fixed), DEFECT_STATUSES invented values (fixed), ExecutionPage deferral status UNKNOWN | Statuses flow consistently through runner, reports, APIs (14 §8.5) | One enum-bridge module per entity (severityToDb pattern); re-probe ExecutionPage run-status casing; ban raw status string literals in queries | S | P1 |
| DAT-029 | Sprint linkage FK target moved twice and is unproven (iterations → ph_jira_sprints → sprints-native?) | P1 | 07 L21: old handover says iterations.id, types.ts says ph_jira_sprints, sprints-native shipped 2026-07-02; live target UNKNOWN | Plan/cycle↔sprint binding feeds release views (14 §6.2) | Phase-0 probe FK target of tm_test_cycles/tm_test_cases/tm_defects/tm_test_plans.sprint_id on cyij; align pickers + queries — PLACEHOLDER: probe result | S | P1 |
| DAT-030 | Cycle release FK points at legacy `releases` table, not the canonical sprint_release model | P1 | types.ts:46729-46748 FK→releases (04 3a); report canon uses sprint_release JSONB (memory testhub-report-revamp) | One release spine feeding readiness (14 §6.2) | Plan Lock decision: unify release spine (adapter or FK migration) before any cycle↔release UI work | M | P2 |
| DAT-031 | Story↔test link split-brain: `linked_story_key` column vs `tm_requirement_links.external_key` — a case linked via one mechanism is invisible to the other | P0 | 04 1b/D4: work-hub TestCasesSection reads linked_story_key; TestCoveragePanel reads tm_requirement_links.external_key | Single typed "covers" relation the coverage engine computes over (14 §10.1) | Canonicalize tm_requirement_links; backfill link rows from linked_story_key; retire the column + its readers (move-not-copy) | M | P1 |
| DAT-032 | Requirement links have no referential integrity: `requirement_id` bare uuid (no FK), `external_key` free text with no picker/validation — typos silently break coverage | P1 | tm_requirement_links types.ts:44922-45032; link entry UI free-text only (04 1a; CatalystViewTestCase.tsx:409-421) | XR links tests to real issues, not strings (14 §10.1) | ph_issues picker (search-as-you-type) writing validated issue_key + requirement_id; DB CHECK/trigger validating key existence; backfill audit of orphan keys | M | P1 |
| DAT-033 | Defect sprint dual-column split-brain: sprint_id FK exists but the canonical create surface writes only the free-text `sprint` name | P1 | CreateStoryModal.tsx:845-847,871 writes text, leaves sprint_id null (04 2e) | — | QA-Bug branch resolves sprint name → id and writes sprint_id; backfill existing rows; deprecate text column | S | P1 |
| DAT-034 | CycleDetailPage DefectPanel writes the cycle-scope row id into `tm_defects.project_id` and omits required `defect_key` — insert errors or corrupts project scoping on a routed page | P0 | CycleDetailPage.tsx:702-710, `project_id: item.id // scope as project proxy` (04 D2); Insert type requires defect_key (types.ts:43060) | Inline defect creation pre-filled and correct (14 §8.7) | Replace panel insert with canonical CreateStoryModal(QA Bug) prefilled with source_test_run_id/source_test_case_id + real project; key via tm_next_entity_key (DAT-009) | S | P1 |
| DAT-035 | TraceabilityPage fetches ALL projects' links then filters client-side, and resolves project as `projects[0]?.id` ignoring the route's projectKey | P0 | TraceabilityPage.tsx:62-79 (no .eq on query), :119-121 (04 D5) | Auditor-grade scoped traceability matrix (14 §10.4) | Server-side `.eq('project_id', …)` resolved from route projectKey; remove client-side org-wide scan | S | P1 |
| DAT-036 | "Generate with AI" on routed /testhub/repository invokes a nonexistent edge function `ai-generate-test-cases` — always errors | P0 | useAIGeneration.ts:74; `supabase/functions/` contains only ai-generate-story-test-cases (10 #1) | AI case generation grounded in story text (14 §13.1) | Deploy ai-generate-test-cases (or add prompt-mode branch to ai-generate-story-test-cases and repoint invoke); zero external imports rule for MCP-deployed fns | S | P1 |
| DAT-037 | Defect row click hard-navigates to unrouted `/testhub/defects/:id` — dead URL + full page reload, work lost | P0 | project-work-hub/adapters/defectsDataSource.ts:148-149; no defects/:id route in FullAppRoutes.tsx (10 #2) | Deep links to any entity (14 §16.7) | Add key-based defect detail route `/testhub/defects/:defectKey` (slug contract — no UUID params) + routes.ts builder + useDefectByKey hook; or open canonical modal | S | P1 |
| DAT-038 | Create Cycle modal silently drops Release/Owner/Tags/Assignees — fields render, values never submitted | P0 | CyclesPage.tsx:257-266 (state), 287-301 (submit omits them), 367/376/401/508 (UI) (10 #3) | Per-instance assignment + config at creation (14 §7.3/7.4) | Extend useCreateCycle input + tm_test_cycles owner/release columns; write tm_cycle_scope.assigned_to for ticked assignees; until wired, remove the fields (a lie is worse than silence) | M | P1 |
| DAT-039 | Mock-on-error assignment table: catch block returns 85 Math.random fake rows (fake testers 'Ahmed S.', 'Sara M.') — any DB failure renders a convincing fake table | P2 | useAssignmentTable.ts:16-74, 203-206 (02 §3.2); unrouted today (10 #5: CycleCommandCenter unreachable) | Never fabricate data (zero-assumption law; no vendor mocks failures) | Delete generateMockAssignments + fallback (throw per DAT-002) or delete the file wholesale in DAT-046 purge | S | P2 |
| DAT-040 | Smart Assignment "Apply" writes 0 rows but toasts full success — id semantics bug (`testCaseId` carries scope.id, lookup by test_case_id finds nothing); inputs fabricated (capacity 5, duration 15, dates "today +10") | P2 | useSmartAssignment.ts:30-35/74-97, assignment-algorithm.ts:174, useApplyAssignment.ts:27/35-38, SmartAssignmentModal.tsx:57-58 (10 #6); unrouted | Bulk/round-robin assignment that persists (14 §7.3) | On any revival: fix id semantics (update tm_cycle_scope by id), derive capacity/dates from real tables, drop fake 200ms delay; else delete with DAT-046 | M | P2 |
| DAT-041 | Mock team members (ids '1'-'5') + hardcoded fake "AI Team Recommendation" flow into real tm_test_plans inserts | P2 | TeamTab.tsx:20-26,124 → CreateEditTestPlanDialog.tsx:100-166 → real useTestPlansG26 inserts (10 #7); unrouted | — | Replace with profiles query (live pattern CyclesPage.tsx:274-281) or delete with DAT-046; verify owner_id FK rejects garbage — PLACEHOLDER: is owner_id a real FK? | S | P2 |
| DAT-042 | Plan Scope tab is a placeholder shell and coverage ring uses invented formula `existingTests = floor(inScope*0.7)` | P2 | ScopeTab.tsx:2,39,43-45; useCreateEditPlanForm.ts:152-157 "Mock calculation" (10 #8); unrouted | Plan scope by query/filter with real coverage (14 §6.4) | Delete with DAT-046 or rewire to ph_issues scope + tm_requirement_links live counts | S | P2 |
| DAT-043 | Calendar events map joined fields the select never fetches → every event 'Untitled'/'General'/'medium'; duration hardcoded 30 min; header claims "ZERO MOCK DATA" | P2 | useCalendarData.ts:44-65 maps run.tm_test_cases?.* not in select at :46 (02 §3.4); unrouted | Cycle calendar view (14 §6.10) | Fix select to embed tm_test_cases(title,module,priority); derive duration from tm_test_runs.duration_seconds; or delete with DAT-046 | S | P2 |
| DAT-044 | LogDefectModal fabricates `DEF-###` via Math.random + success toast — no DB write, no link (also hex color-law violations) | P2 | LogDefectModal.tsx:52-54, 63, 121 (04 D1); dead releases/test-execution family (10) | Defect raised in <10s with real bidirectional link (14 §8.7, §9.1) | Delete file; routed execution flow uses canonical CreateStoryModal(QA Bug) per DAT-034 | S | P2 |
| DAT-045 | `tm_test_runs.auto_created_defect_id` has no writer anywhere in src — dead column while trigger `trg_tm_auto_create_defect` claims to auto-create defects on failure | P2 | 04 matrix #4 (grep: only types.ts); trigger listed 03 §4 | Auto-defect from failed run with link-back (14 §9.1) | Live-probe the trigger on cyij (does it fire? does it set the column?); wire the trigger to write the column or drop both | S | P2 |
| DAT-046 | ~60-file dead legacy generation (pages/releases/Test*, components/test-cycles/**, test-plans/**, releases/test-*) full of mocks, half-alive — routing any file revives fake data | P1 | 10 §dead-inventory: verified unreachable, importer-grep clean; DAT-039/040/041/042/043/044 all live inside it | — | Plan Lock decision: delete families wholesale (move-not-copy precedent, 07 L20) rather than rebaseline; tsc + importer grep as acceptance | M | P2 |
| DAT-047 | Orphaned duplicate DefectsPage (468 lines) + dead FilterDetailPage shadow canonical pages in greps | P2 | pages/testhub/defects/DefectsPage.tsx (10 #13); pages/testhub/FilterDetailPage.tsx (10 #14) | — | Delete both (canonical: pages/testhub/DefectsPage.tsx, FilterPreviewPage) | S | P2 |
| DAT-048 | QuickActionsBar toast-theater (6 fake actions: fake duplicate, fake PDF export, fake linker) + "Attachment uploads coming soon" placeholder | P2 | releases/test-case-detail/QuickActionsBar.tsx:47-109; TestCaseLinksAttachments.tsx:69,214 (10 #11-12); dead but deceptive | Real actions only; routed ExecutionPage already has real storage uploads (ExecutionPage.tsx:505-526) | Delete folder with DAT-046 | S | P2 |
| DAT-049 | Console.log-only calendar handlers (event click / reschedule / view details do nothing) | P2 | releases/cycle-command-center/CycleCalendarView.tsx:39,45,49 (10 #10); unrouted | — | Delete with DAT-046, or wire to cycle nav + existing RescheduleModal on revival | S | P2 |
| DAT-050 | Types regen overdue + no guard: tm_cycle_sets/tm_set_cases/tm_requirement_tests/plan_test_cycles all accessed via `as any`, so typegen is blind exactly where drift lives | P1 | 04 3e; 03 §9; 02 §1 (useTestCycleByKey, useTestPlansG26 casts) | — | After DDL reconciliation (DAT-013/023): `generate_typescript_types` from cyij, remove every tm_* `as any`, add CI grep failing on `from('tm_` + cast | S | P1 |
| DAT-051 | useCommandCenter's 5+ queries have zero `enabled:` guards — fire with undefined projectId on first render | P2 | testhub/useCommandCenter.ts (02 §1 row 63: "no enabled: guards (0 in file)") | — | Add `enabled: !!projectId` to each query; assert pid non-null in queryFn | S | P1 |
| DAT-052 | Persisted react-query cache (localStorage `catalyst-rq-cache`, 15min staleTime) serves stale data after schema/seed changes — masks fixes and shows deleted entities | P2 | 07 L17 (testhub-report-revamp gotcha) | — | Version the persist cache key (bump on schema change); validation protocol: clear + hard reload before every data proof (06_VALIDATION_EVIDENCE) | S | P2 |
| DAT-053 | Step-result percolate trigger fires only ON UPDATE OF status — INSERT path relies on unproven app-side compensation | P2 | 07 L26 (ENGINE drift log :31, open) | Step fail auto-derives case verdict reliably (14 §8.2) | Extend trigger to INSERT OR prove/centralize app compensation; write-probe both paths | S | P2 |
| DAT-054 | `run_number` hardcoded to 1 — multiple executions per scope item overwrite instead of accumulating history | P1 | 07 L26 (ENGINE 07_HANDOVER:11 "run_number hardcoded 1 / multi-run missing") | Fail-then-pass sequences fully retained per instance (14 §7.6) | Compute next run_number per cycle_scope_id at insert (or DB default via trigger); UNIQUE(cycle_scope_id, run_number); runner shows history | M | P2 |
| DAT-055 | Slug-contract violations: /testhub/sets/:id and /testhub/filters/:filterId use raw UUID params; routes.ts builders declare slug args the mounted routes don't accept | P1 | FullAppRoutes.tsx:680,699 vs src/lib/routes.ts:121-130 (03 §7) | Key-based shareable URLs (14 §2.8, §16.7) | Route sets by set_key, filters by slug; useSetByKey/useFilterBySlug hooks; UuidToSlugRedirect outside CatalystShell for legacy URLs | M | P2 |
| DAT-056 | Audit split-brain: `tm_audit_log` AND `tm_audit_logs` both live in types; TestHub saved filters live in generic saved_filters while tm_saved_filters exists migration-only | P2 | 03 §9 duplicate/overlapping tables | Single project-level audit log (14 §5.8) | Probe writers for both audit tables; consolidate to one + drop the other; document saved_filters as canonical and delete tm_saved_filters migration intent | S | P2 |
| DAT-057 | tm_projects vs projects/ph_projects id split held together by seed-time id mirroring + resolveTmProjectId shim — unseeded projects silently break every tm_* surface | P1 | 03 §2.1 NOTE; CreateStoryModal.tsx:848-854 shim (04 §3) | Cross-entity project identity is structural, not conventional | Add tm_projects.ph_project_id FK (or unify tables) + auto-provision trigger on ph_projects insert — PLACEHOLDER: unify-vs-bridge decision for Plan Lock | M | P2 |
| DAT-058 | case_key zero-padding inconsistent (TC-0001 vs TC-001) — string-sort and dedupe anomalies in lists/exports | P3 | 07 L26 (ENGINE drift :16, open) | Keys stable and uniformly formatted (14 §2.8) | Normalize padding in tm_next_entity_key + one-time backfill of existing keys (with URL redirect safety since cycles route by key) | S | P3 |
| DAT-059 | Migration ledger materially over-states live schema (~56 dead test_* + ~17 th_* migration-created tables absent from types) and had 5 timestamp collisions | P2 | 03 §10; 07 L10 | — | Ledger reconciliation note per family; CI check `ls supabase/migrations | cut -d_ -f1 | sort | uniq -d` empty; never trust ledger over live probe (L9) | S | P3 |
| DAT-060 | Defect↔incident linkage missing entirely — no table/column relates tm_defects to a Production Incident | P2 | 04 2d: CatalystViewIncident shows only regression-coverage gap, no related defects | ITSM problem↔defect linkage (14 §9 domain) | Typed link: extend tm_defect_links with validated linked issue_key (after DAT-010 schema fix) or reuse parent_key pattern; reverse rollup on incident view | M | P3 |

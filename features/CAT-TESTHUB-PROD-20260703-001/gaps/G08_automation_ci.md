# G08 — Automation & CI Ingestion Gaps (Xray persona, credibility gate)

Feature: CAT-TESTHUB-PROD-20260703-001 · Gap-mining agent · 2026-07-03 · Yardstick: discovery/14 Domain 12 (+11.5, 14.11 crossovers) · Evidence: discovery/02, discovery/03, src probes, supabase/functions listing (zero test-result ingestion functions exist among ~90 edge functions).

| ID | Title | Severity | Current state (file:line or discovery-doc evidence) | Target (vendor benchmark) | Proposed fix (concrete, Catalyst-canonical) | Effort S/M/L | Phase P1/P2/P3 |
|---|---|---|---|---|---|---|---|
| AUT-001 | No JUnit XML result import endpoint (API or UI) | P1 | supabase/functions/ listing: ~90 edge fns, zero result-ingestion fns; no importer in src/pages/testhub (grep junit/import-results = 0 code hits, only TESTHUB_BUILD_HANDOVER.md:381 planning note) | 12.1 — XR/TR: idempotent JUnit import via API + UI upload, results land in a designated cycle | New edge fn `testhub-import-results` (POST multipart/JSON): parse JUnit XML → match `tm_test_cases.automation_id`/`case_key` → insert `tm_test_runs`+`tm_cycle_scope`; typed route + UI upload later (AUT-009) | L | P1 |
| AUT-002 | Imports not idempotent — no external run identity on tm_test_runs | P1 | discovery/03:52 tm_test_runs columns: no external_id/import_id/dedupe key; uniques list (03:36) has none for runs | 12.1 — re-POST of same CI payload must not double results | Add `tm_test_runs.import_key TEXT` + `tm_import_jobs.id` FK, UNIQUE(cycle_scope_id, import_key); importer upserts on conflict | S | P1 |
| AUT-003 | No import-job ledger (who imported what, when, match/create/error counts) | P1 | No tm_import_* table in types.ts or migrations for tm_ family (discovery/03 §2 full 55-table list) | 12.1/15.10 — TR/AIO importers return a validation/mapping report | New table `tm_import_jobs` (source, format, cycle_id, counts_json, status, created_by, pipeline_url) written by every ingestion call; surfaced in cycle detail via JiraTable | M | P1 |
| AUT-004 | No stable code↔case mapping-key contract; `automation_id` not unique | P1 | tm_test_cases.automation_id exists (discovery/03:44) but absent from unique-constraint list (03:36); no docs/annotation convention; only 7 src files even reference it | 12.4 — XR/ZS `@TestKey(PROJ-T12)` annotations; rename-proof mapping | Partial UNIQUE index on (project_id, automation_id) WHERE automation_id IS NOT NULL; document mapping precedence: automation_id → case_key → normalized fully-qualified test name | S | P1 |
| AUT-005 | Unmapped automated results have no quarantine path — would be dropped or mis-created | P1 | No quarantine table/state anywhere in tm_ schema (discovery/03 §2); importer doesn't exist yet so behavior undefined | 12.4 — XR: unmapped results quarantined for triage, never silently dropped | `tm_import_jobs` child table `tm_import_quarantine` (raw result row + reason); triage UI tab on import-job detail with "map to case / create case / discard" actions | M | P1 |
| AUT-006 | No auto-create case on first-seen automated result | P1 | No such logic anywhere; tm_test_cases creation is UI-only via useTestCases.ts:327 (discovery/02:35) | 12.5 — TR CLI/AIO importer: zero-touch onboarding of an existing automated suite | Importer option `autoCreate: true` → insert tm_test_cases (test_format='automated', automation_id=test FQN, folder per AUT-007 rules) via `tm_next_entity_key` RPC | M | P1 |
| AUT-007 | No folder/label routing rules for auto-created cases | P2 | tm_folders exists (discovery/03:42) but no routing config; auto-create doesn't exist (AUT-006) | 12.5 — TR: folder/labeling rules on first import (e.g., by package/suite name) | Import config JSON on tm_projects.settings: suite-name → folder path mapping; default `/Automation/<suite>` folder auto-created | S | P2 |
| AUT-008 | No manual-vs-automated provenance surfaced anywhere in UI or reports | P1 | tm_test_runs.execution_mode exists (discovery/03:52) but no UI consumer; Automation Activity report is a stub (docs/feature-builder/features/test-hub/gap-analysis.md:80) | 12.9 — AIO/XR: one cycle mixes manual + automated with clear per-result provenance in every report | Lozenge (`@atlaskit/lozenge`) AUTOMATED/MANUAL on runner history, cycle scope table, and all report bodies; filter dimension on cycle views | M | P1 |
| AUT-009 | No UI upload path for result files (planned, never built) | P1 | src/pages/testhub/TESTHUB_BUILD_HANDOVER.md:381–383,462,592 lists "Import Results (CSV upload → tm_step_results)" as unbuilt backlog | 12.1 — TR/ZS: UI upload of JUnit/CSV into a cycle | Cycle detail "Import results" action → ModalDialog with file drop + dry-run preview (AUT-027) calling the AUT-001 edge fn | M | P1 |
| AUT-010 | `execution_mode` is free text — provenance can drift/lie | P2 | discovery/03:52 execution_mode listed with no enum; tm enums list (03 §3) has no execution_mode enum | 12.9 — provenance must be trustworthy in reports | CHECK constraint execution_mode IN ('manual','automated','imported') + backfill migration; typed union in hooks | S | P1 |
| AUT-011 | No build/commit metadata on automated results (build no., git SHA, pipeline URL) | P1 | tm_test_runs has only unstructured environment_snapshot Json (discovery/03:52); no build_number/git_sha/pipeline_url columns | 12.10 — XR/QT: "which build broke it" answerable from the run record | Add tm_test_runs columns build_number TEXT, git_sha TEXT, pipeline_url TEXT (nullable); importer populates from CI env vars; render as link chips on run history | S | P1 |
| AUT-012 | No CI service identity — automation must impersonate a human user | P1 | RLS gates every tm_ write on `tm_user_has_access(auth.uid(), project_id)` (discovery/03:111); no API-token/service-account model in tm_permissions/tm_roles (03:72) | 14.11 — TR/QT: scoped service accounts, token rotation, last-used visibility | Edge fn auth via per-project import token table `tm_api_tokens` (hashed, scoped to project+verb, last_used_at); edge fn runs service-role internally after token check | M | P1 |
| AUT-013 | No GitHub Actions integration (action, workflow snippet, or docs) | P1 | Zero GH-Actions references in supabase/functions or src for test-result publishing (function listing; grep) | 12.6 — XR GH action / TR CLI: pipeline step publishes results with exit-code semantics | Ship `.github/actions/testhub-publish` composite action (curl → AUT-001 endpoint, token from secrets) + copy-paste workflow snippet rendered in the import UI | M | P1 |
| AUT-014 | No CI exit-code / threshold semantics on import (can't fail a pipeline on quality bar) | P2 | Importer absent; no gate evaluation callable from CI (tm_evaluate_quality_gates RPC exists but release-scoped, discovery/03:70) | 12.6 — publish step returns non-zero when pass-rate below threshold | AUT-001 response includes summary + optional `failIfBelow` param; GH action exits 1 on breach | S | P2 |
| AUT-015 | No Cucumber JSON ingestion despite native Gherkin authoring tables | P1 | tm_gherkin_steps, tm_step_definitions, gherkin_feature/gherkin_scenario cols + tm_convert_to_bdd/tm_save_gherkin_scenario RPCs exist (discovery/03:44,72,141) — write-only island, no result round-trip | 12.3 — XR: scenario in tool = scenario in repo, results map back without reconciliation | Cucumber-JSON adapter in AUT-001 fn: match scenario name/tag `@TC-key` → per-scenario tm_test_runs; per-step results into tm_step_results | M | P2 |
| AUT-016 | No .feature file export/import (BDD round-trip half missing) | P2 | No feature-file code anywhere in src/pages/testhub or edge fns (grep); authoring side exists (AUT-015 evidence) | 12.3/15.8 — XR: export .feature to repo, re-import results | "Export .feature" action on BDD-format cases (serialize gherkin_feature); bulk export per folder as zip | M | P2 |
| AUT-017 | No TestNG / NUnit / xUnit XML adapters | P2 | No parser code exists (importer absent, AUT-001) | 12.2 — XR broadest format list; adapters vendor-maintained | Format adapters behind one normalized internal result model in the AUT-001 fn; TestNG first (JUnit-adjacent) | M | P2 |
| AUT-018 | No Playwright JSON reporter ingestion | P2 | No Playwright ingestion anywhere (grep src + functions); Catalyst's own e2e stack is Playwright — dogfood gap | 12.2 — modern-stack ingestion is table stakes for credibility with JS teams | Playwright JSON adapter (suites→folders, projects→environments, retries→run_number sequence per AUT-024) | M | P2 |
| AUT-019 | No Robot Framework output.xml adapter | P2 | Absent (importer absent) | 12.2 — XR/ZS support Robot | Robot adapter mapping PASS/FAIL/SKIP + WARN via status-map config (AUT-020) | M | P3 |
| AUT-020 | No framework-status → tm_execution_status mapping config | P2 | tm_execution_status = not_run/in_progress/passed/failed/blocked/skipped (discovery/03:82); no mapping layer for framework-specific statuses (e.g., Robot WARN, pytest xfail) | 12.2/14.4 — statuses flow through reports and imports consistently | Per-project status_map JSON in import config with sane defaults; unknown status → quarantine (AUT-005) not guess | S | P2 |
| AUT-021 | No generic JSON result schema for unsupported frameworks | P2 | Absent (importer absent) | 12.2 — XR: unknown formats via generic JSON | Publish a documented minimal JSON schema (name, status, duration, error, attachments[]) accepted by AUT-001 | S | P2 |
| AUT-022 | Ingestion must not bypass tm_cycle_scope or denormalized counters break | P1 | Cycle counters maintained only via tm_cycle_scope triggers `trg_sync_cycle_scope_counters`/`tm_update_cycle_stats` (discovery/03:48,97); a naive importer writing tm_test_runs directly desyncs every cycle KPI | 12.1 — imported results indistinguishable from manual in rollups | Design constraint in Plan Lock: importer creates/updates tm_cycle_scope rows first, then tm_test_runs; verify counters in import integration test | S | P1 |
| AUT-023 | Auto-defect trigger will spam defects on bulk automated import | P1 | `trg_tm_auto_create_defect` creates a tm_defect on every failed run (discovery/03:99); 500 failing CI results → 500 defects | 9.10/12.9 — automated failures triaged, not auto-filed 1:1 | Gate trigger on execution_mode='manual' (or importer session flag); importer instead groups failures into the import-job report for triage | S | P1 |
| AUT-024 | No re-run/retry semantics for ingested results (CI retries would double-count) | P2 | tm_test_runs.run_number exists (discovery/03:52) but no importer contract for retries; latest-wins rollup only via tm_cycle_scope.current_status | 7.6/12.1 — fail-then-pass sequence fully visible; latest result wins for rollups | Importer maps framework retries to incrementing run_number under one scope row; flag `is_retry`; report "passed on retry" count | M | P2 |
| AUT-025 | No designated-cycle targeting/auto-create per pipeline run | P1 | Cycle creation is UI-only (useTestCycles.ts / useTestCyclesEnhanced.ts, discovery/02:36–37); no API path | 12.1 — results land in a designated cycle; XR creates Test Execution per CI run | AUT-001 params: `cycleKey` (append) or `createCycle: {name pattern with build vars}`; uses tm_next_entity_key RPC | S | P1 |
| AUT-026 | No environment/configuration mapping from CI matrix into tm_environments | P2 | tm_test_cycles.environment is free text alongside environment_id FK (discovery/03:48) — free-text drift already possible; no import mapping | 3.9/14.13 — TR configurations/XR environments: results separable per environment | Importer resolves env name → tm_environments (create-if-missing behind config flag); write environment_id not text | S | P2 |
| AUT-027 | No dry-run/validation mode for imports | P3 | Importer absent | 15.1 — TR: validation report before commit | `dryRun: true` param returns match/create/quarantine counts without writes; wired into AUT-009 modal preview | S | P2 |
| AUT-028 | No API artifact ingestion (screenshots/videos/logs onto step results) | P2 | Storage bucket `tm-attachments` used only by defect UI flow (useDefects.ts:822, discovery/02:25); tm_defect_links.attachment_id→step_result_attachments exists but no API writer | 12.12 — XR/TR: evidence from CI attached to the exact instance/step, size-managed | AUT-001 accepts base64/multipart artifacts per result → storage upload + step_result_attachments rows; per-file size cap | M | P2 |
| AUT-029 | No artifact retention/quota policy for CI evidence | P3 | No retention config anywhere (no tm_ retention table, discovery/03 §2) | 14.12 — predictable performance at multi-year scale | pg_cron cleanup of automated-run artifacts older than N days (project setting); keep failure evidence longer than pass | S | P3 |
| AUT-030 | No REST API surface for test ops at all (cases, cycles, results, plans) | P1 | 100% of data access is browser supabase-js under user RLS (discovery/02 §1 master table); zero test-ops edge functions (functions listing) | 12.7 — TR/XR: API parity with UI, versioned, paginated, rate-limit documented | Phase the surface: P1 = results ingestion (AUT-001) + read endpoints for cases/cycles; P2 = CRUD parity via a single `testhub-api` edge fn router with versioned paths `/v1/...` | L | P2 |
| AUT-031 | No API documentation / OpenAPI spec for the (future) test-ops surface | P2 | N/A today (no API); no docs dir for it | 12.7 — long-stable documented API with deprecation policy | OpenAPI YAML checked into repo + rendered docs page under TestHub admin; generated from the AUT-030 router types | M | P3 |
| AUT-032 | No outbound webhooks for test events (result recorded, cycle closed, import finished) | P2 | notify-dispatch fn is user-notification oriented (functions listing); no event subscription model for tm_ entities | 12.8 — QT/TR: event-driven integration without polling | `tm_webhooks` table (project, event_type, url, secret) + pg_net/edge-fn dispatcher on cycle close + import completion first | M | P2 |
| AUT-033 | No Slack/Teams routing for automation failures | P3 | No test-event → Slack wiring (functions listing: email-send/notify-dispatch generic only) | 16.6/12.8 — configurable event→channel routing | Reuse AUT-032 webhooks with Slack incoming-webhook URL preset + message template | S | P3 |
| AUT-034 | Scheduled imports/runs impossible — tm_scheduled_runs is migration-only ghost | P2 | tm_scheduled_runs created in 20260121134233 but NOT in types.ts → dropped or never applied (discovery/03:155); no pg_cron consumer | 12.11 — scheduled/triggered orchestration; at minimum scheduled pulls | PLACEHOLDER: probe cyij for tm_scheduled_runs existence first; then either recreate table + pg_cron→edge-fn puller (fetch artifacts from GH Actions API) or delete the dead migration surface | M | P3 |
| AUT-035 | No pipeline deep-link from cycle to the CI run that fed it | P2 | tm_test_cycles has no pipeline/build fields (discovery/03:48); AUT-011 covers runs, not cycle level | 12.11 — deep links from cycle to pipeline | Roll up pipeline_url from import job to cycle detail header chip (link via tm_import_jobs.cycle_id) | S | P2 |
| AUT-036 | Automation Activity report is a stub | P1 | docs/feature-builder/features/test-hub/gap-analysis.md:80 "Automation Activity report ✓ missing Stub" | 11.1/12.9 — AIO automation dashboards: created vs automated vs pending rate over time | Real report body over tm_test_cases.automation_status + tm_test_runs.execution_mode time series; reuse reports-hub canonical report shell | M | P2 |
| AUT-037 | No automation coverage report (% automated by component/priority/folder) | P1 | No such report in src/pages/testhub/reports (discovery/06 inventory; gap-analysis.md:80 area) | 12.9 — AIO Automation Coverage per release/component | Report: group tm_test_cases by folder/priority, measure automation_status='automated' share; drill to uncovered list | M | P2 |
| AUT-038 | No flaky-test identification (status flip-rate across runs) | P2 | No flakiness computation anywhere (discovery/02 RPC inventory §6 has no flaky RPC) | 11.5 — QT/AIO: flakiness score on the case, quarantine candidates filterable | SQL view v_tm_case_flakiness (flip count over last N automated runs per case) + report + badge on case list | M | P3 |
| AUT-039 | No quarantine/muted state for known-flaky automated tests | P2 | tm_case_status enum = draft/ready/approved/deprecated (discovery/03:80) — no quarantined; no known-issue link semantics | 9.10/11.5 — expected-fail separated from new signal | `is_quarantined boolean` on tm_test_cases (not enum widening); importer marks quarantined failures as status 'skipped' variant in rollups with provenance kept | M | P3 |
| AUT-040 | `automation_status` never auto-maintained by ingestion | P2 | automation_status is a manually-set text col (discovery/03:44); nothing flips it when automated results arrive | 12.5/12.9 — coverage numbers reflect reality, not manual bookkeeping | Importer sets automation_status='automated' on first mapped result; nightly job flags cases with no automated run in 90d as 'stale' | S | P2 |
| AUT-041 | Assignment table fabricates 85 fake rows on any query error — automated results would render as fiction | P0 | useAssignmentTable.ts:203–206 returns generateMockAssignments() with fake testers on ANY error (discovery/02 §3.2) | Credibility baseline — no vendor renders fake data on failure | Delete mock generator; error → SectionMessage + retry (silent-query-error sweep pattern already established on main) | S | P1 |
| AUT-042 | Assignment table hardcodes testType 'functional' for every row | P0 | useAssignmentTable.ts:194 hardcodes 'functional'; computed mapping at :169–173 discarded (discovery/02 §3.5) | 12.9 — type/provenance truthful per row | Use the computed mapping; render dash when unknown (zero-assumption rule) | S | P1 |
| AUT-043 | Data-driven automated results land on legacy dead-family table | P1 | tm_test_runs.test_data_row_id → test_data_rows, a legacy `test_*` family FK (discovery/03:173; 02 §5) — canonical execution spine depends on a dead family; iteration-level import would build on sand | 3.4/12.1 — iteration-level results are separately recorded runs on canonical schema | PLACEHOLDER: consolidation decision owned by datasets domain — importer must target the post-consolidation dataset table; block iteration ingestion until resolved | M | P3 |
| AUT-044 | Defect key generation races — CI-created defects would collide | P1 | useDefects.ts:135–151 client-side MAX-scan of defect_keys bypasses tm_next_entity_key RPC (discovery/02 §7.4); concurrent import + manual create = duplicate keys | 12.1 — concurrent automated + manual flows must be safe | Replace client MAX-scan with tm_next_entity_key RPC everywhere (single key authority); importer uses RPC too | S | P1 |
| AUT-045 | No parallel-shard import safety (N CI shards posting into one cycle) | P3 | Importer absent; no advisory-lock/upsert pattern defined | 12.1 — sharded CI runs are the norm at scale | AUT-002 idempotency + per-scope upsert with ON CONFLICT; document shard behavior in API docs | S | P3 |
| AUT-046 | No rate/size limits or chunking contract for large payloads (10k results) | P3 | Edge fn limits (~6MB body) undocumented for this use; no chunk protocol | 12.7 — rate limits documented so 50k-case flows are scriptable | Accept NDJSON streaming or chunked POSTs bound to one import_job id; document limits in AUT-031 spec | M | P3 |
| AUT-047 | No inbound webhook receiver alternative (GitHub check-run/workflow_run events) | P3 | jira-webhook-receiver exists for Jira only (functions listing); nothing for CI providers | 12.6 — pull model complements push for teams that can't add steps | `testhub-ci-webhook` fn: on workflow_run completed, fetch artifact via GH API token, feed AUT-001 pipeline | M | P3 |
| AUT-048 | No manual→automated conversion stub generation (mapping key pre-inserted) | P3 | No such feature (13.10 emerging); AI gateway exists (discovery doc 12) with generate-from-story only | 13.10 — generate framework-specific skeleton with mapping key | AI gateway prompt: stepped case → Playwright/pytest stub containing `automation_id` annotation; deliver as copyable code block | M | P3 |
| AUT-049 | No per-import notification/digest to the requesting team | P3 | No import entity exists to notify on (AUT-003) | 16.9/12.8 — import completed/failed reaches the right channel | Emit catalyst notification + optional AUT-032 webhook on import_job completion with summary counts | S | P3 |
| AUT-050 | Robot/Playwright/etc. suite hierarchy not mappable to folders on import | P3 | tm_folders ltree exists (discovery/03:42) but no importer mapping | 12.2 — suite structure preserved so imported repo is navigable | Adapter option: mirror suite path into folder path (create-if-missing, capped depth), reusing AUT-007 rules | S | P3 |

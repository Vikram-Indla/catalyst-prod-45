# G02 — Versioning, Baselines, History, Audit (Zephyr/qTest persona)

Feature: CAT-TESTHUB-PROD-20260703-001 · Gap-mining agent · 2026-07-03
Yardstick: `discovery/14_competitor_feature_baseline.md` Domain 5 (+4.5–4.6, 7.11, 14.7, 16.2–16.4). Inputs: 03_db_schema.md, 02_hooks_data_layer.md, 07_lessons_past_mistakes.md + direct src/migration verification. Deduped against `docs/feature-builder/features/test-hub/gap-analysis.md` (its rows 31/36/49/78/101 are subsumed with sharper evidence below).
Severity: P0=broken/lying today · P1=table-stakes missing · P2=competitive · P3=delighter. Phase: P1=integrity foundation · P2=governance/UX table-stakes · P3=regulated-tier/delighters.

| ID | Title | Severity | Current state (evidence) | Target (vendor benchmark) | Proposed fix (concrete, Catalyst-canonical) | Effort | Phase |
|---|---|---|---|---|---|---|---|
| VER-001 | Runs execute against LIVE steps, pinned version never read (#1 architectural risk) | P0 | Runner loads steps via `useTestCase(scope.case_id)` → live `tm_test_steps` (`src/pages/testhub/cycles/ExecutionPage.tsx:361-362`); `tm_cycle_scope.locked_version` is written (migration `20260407190005`, `useTestCycles.ts:497`) but has ZERO read sites in src | ZS/QT 5.3: execution permanently binds to the case version executed | Run-create RPC resolves steps from the `tm_test_case_versions` snapshot at `locked_version`; runner renders snapshot steps, never live rows; fallback to live only when no version exists (logged) | L | P1 |
| VER-002 | `tm_step_results.test_step_id ON DELETE CASCADE` — editing/deleting a step destroys historical step results | P0 | `20260104074712_...sql:272` (`REFERENCES tm_test_steps(id) ON DELETE CASCADE`); step delete lives in `useTestSteps.ts:97` | TR/ZS 4.6/5.9: shared-step/step edits never mutate historical execution records | Migration: FK → `ON DELETE SET NULL` (nullable) + denormalize `action_snapshot`/`expected_snapshot`/`step_number` onto `tm_step_results` at write time (or store per-run steps_snapshot per VER-021) | M | P1 |
| VER-003 | Version restore deletes + reinserts ALL steps → cascades away every step result for the case | P0 | `useTestCaseVersions.ts:254-268` (`.delete().eq('test_case_id',…)` then insert); combined with VER-002 CASCADE this silently erases execution history; both calls are un-error-checked bare `await` | TR/QT 5.4: restore never corrupts history | Replace client restore with a hardened `tm_restore_version` RPC that upserts steps in place (match on step_number), preserves ids, and refuses while step-result FKs would break | M | P1 |
| VER-004 | Hard delete of a case cascades scope→runs→step_results→versions; delete/bulkDelete still shipped despite archive existing | P0 | CASCADE chain `20260104074712:243-272` + versions CASCADE `20260121133526:17`; `useTestCases.ts:712` (delete), `:739` (bulkDelete) vs archive at `:1117`; prior incident: TC-010 stray-click hard delete (07_lessons L13) | TR/QT 2.9/5.9: archive is the default destructive action; deleting never corrupts run history | Make archive the only user-facing action; hard delete becomes admin-gated RPC that refuses when runs exist; keep confirm dialog (L13) | M | P1 |
| VER-005 | Restore is lossy AND a silent overwrite — restores 7 fields, drops the rest, snapshots nothing first | P0 | Snapshot shape omits `expected_result`, `labels`, `custom_fields`, `gherkin_*`, `*_html`, `postconditions_html`, `estimated_time` (`useTestCaseVersions.ts:169-184`); restore writes only those 7 fields (`:231-247`) leaving current values for the rest = mixed-state case; no pre-restore snapshot created | TR/QT 5.4: one-click restore creates a new version, never silent overwrite | Full-row snapshot shape (superset incl. gherkin/html per RPC shape); restore path auto-snapshots current state as new version first, then applies; toast names both version numbers | M | P1 |
| VER-006 | FOUR parallel version-snapshot writers with 3 divergent snapshot shapes | P0 | (1) `useAutoVersioning.ts:58-73`, (2) `useTestCaseVersions.ts:169-184`, (3) `testCaseAuditService.ts:139-153`, (4) DB RPC `tm_create_version_snapshot` (`20260121133526:90-128`, includes gherkin/html/priority the clients omit) — diff/restore cannot rely on a stable shape | All vendors: one version model | Single writer = the DB RPC (hardened, full-row shape); delete the 3 client writers; hooks call the RPC only | M | P1 |
| VER-007 | version_number computed client-side MAX+1 in 3 places → race duplicates hit UNIQUE and fail (silently in one path) | P1 | `useAutoVersioning.ts:50-56`, `useTestCaseVersions.ts:160-166`, `testCaseAuditService.ts:125-136`; `UNIQUE(test_case_id, version_number)` exists (`20260121133526:23`) so the race 409s | Enterprise: versions always commit | Version number assigned inside the RPC (`SELECT … FOR UPDATE` / advisory lock); resolved by VER-006 consolidation | S | P1 |
| VER-008 | `tm_test_cases.version` / `current_version` / `is_latest_version` never maintained by app write paths → locked_version pins a stale number | P0 | Only the (unused) RPC updates `version` (`20260121133526:122-125`); client snapshot writers never touch it; broken auto-version trigger dropped `20260627120000`; `fn_lock_scope_version` copies this stale column (`20260407190005:12-22`) | ZS/QT 5.2: version is a citable, correct artifact | DB trigger on `tm_test_case_versions` INSERT sets `tm_test_cases.version = NEW.version_number`; one-shot backfill migration from MAX(version_number) | S | P1 |
| VER-009 | useAutoVersioning is 100% silent — auto-version snapshots fail with zero user signal | P1 | 0 throws in file; all errors `console.error` + return (`useAutoVersioning.ts:32,44,86`); 02_hooks verdict SILENT | Versioning must be reliable or loudly degraded | Superseded by VER-006 (RPC path throws); until then apply the L11 query contract (error → SectionMessage/toast) | S | P1 |
| VER-010 | Case FIELD edits create no version — only step mutations auto-version | P1 | `createVersionSnapshot` called only from `useTestSteps.ts:40,70,97,138,200`; zero calls in `useTestCases.ts` update path (:422); title/priority/preconditions edits leave no version | TR 5.1: every content change captured | DB AFTER UPDATE trigger on content columns of `tm_test_cases` invoking snapshot fn (replaces the broken trigger dropped 20260627120000 — this time probe-tested per L4) | M | P1 |
| VER-011 | Audit table split-brain: `tm_audit_log` AND `tm_audit_logs` both live; plural is unused by src and FKs the wrong project family | P1 | Both in types.ts (03_db §9); src writes only `tm_audit_log` (`tmAuditLogger.ts:27`, `testCaseAuditService.ts:61`); `tm_audit_logs` created `20260106062454:19` with `project_id→projects` (L6 open FK-outlier flag) | One audit store | Probe row counts on cyij; migrate any rows into `tm_audit_log`, DROP `tm_audit_logs`; regen types | S | P1 |
| VER-012 | Audit capture is client-side fire-and-forget — direct writes bypass it entirely; no DB-level source of truth | P1 | `logTMAuditEntry` "Works silently" (`tmAuditLogger.ts:22-42`); ExecutionPage writes `tm_step_results`/`tm_cycle_scope` directly with no audit call (`ExecutionPage.tsx:90,95,500,532`); offline-queue flush also bypasses (`:52-96`); comment at `useExecutionMutations.ts:88` wrongly assumes capture happens anyway | PT/QT 5.8: all entity CRUD audited, tamper-evident | AFTER INSERT/UPDATE/DELETE triggers on core tm_* tables writing `tm_audit_log` (SECURITY DEFINER); delete both client logger libs; trigger forensics probe first (L4) | L | P1 |
| VER-013 | Two incompatible audit `changes` payload shapes in the same table: `{old,new}` vs `{from,to}` | P1 | `tmAuditLogger.getAuditChanges` emits `{old,new}` (`tmAuditLogger.ts:48-77`); `testCaseAuditService.handleTestCaseChange` emits `{from,to}` (`testCaseAuditService.ts:240-246`) | Uniform audit schema | Standardize on one shape inside the VER-012 trigger writer; migration normalizes existing rows | S | P1 |
| VER-014 | No audit-log viewer UI anywhere in TestHub | P1 | grep: zero pages/components read `tm_audit_log` (only writers exist); prior gap-analysis.md:101 concurs | PT/QT 5.8: filterable by user/entity/date | `/testhub/:projectKey/audit` page on JiraTable with user/entity/action/date filters; route via `:projectKey` (slug contract), registered in `src/lib/routes.ts` | M | P2 |
| VER-015 | No audit export or retention policy | P2 | No export code paths; no retention config anywhere in tm_* | PT/QT 5.8/14.12: exportable, retention-configurable | CSV export from the VER-014 grid (respecting filters, 15.2 pattern); retention setting in tm_projects.settings + scheduled purge fn | M | P3 |
| VER-016 | `tm_test_case_versions` RLS is not project-scoped — any approved user can read/insert versions in any project | P1 | Policies use `current_user_is_approved()` only (`20260121133526:30-37`) vs canonical `tm_user_has_access` pattern (03_db §5) | 14.5: role/project-scoped permissions | Migration replacing both policies with the canonical 4 `tm_user_has_access`-gated policies | S | P1 |
| VER-017 | No deleted-entity tombstones — history views cannot render names/steps of deleted cases | P2 | All history FKs CASCADE (VER-004 chain); no `case_key`/`title` denormalized onto `tm_cycle_scope`/`tm_test_runs` (`20260104074712:241-266`) | TR/ZS 5.9: runs referencing a deleted case still render | Denormalize `case_key`+`title` snapshot columns onto `tm_cycle_scope` at insert (backfill migration); render dash when truly unknown (zero-assumption rule) | M | P2 |
| VER-018 | Baselines missing entirely — cannot freeze a repository/plan snapshot for a release | P2 | grep "baseline" across testhub pages/hooks: 0 hits; no tm_baselines table in migrations or types | XR/QT 5.7: "what did the regression pack look like at release 2.3?" | New `tm_baselines` (+`tm_baseline_items` pinning case_id+version_number) scoped to release/plan; create-from-folder/plan action; slug contract (key + routes.ts builder) | L | P3 |
| VER-019 | Completed/archived cycles still accept result writes — closed doesn't mean closed | P1 | No status gate in `useExecutionMutations.ts` writes or `ExecutionPage.tsx:95,532`; `trg_validate_cycle_status_transition` guards only the cycle.status column itself | QT/PT 7.11: locked cycle; reopen is permissioned + logged | BEFORE-trigger rejecting scope/run/step-result writes when parent cycle ∈ {completed, archived}; permissioned reopen RPC writing an audit row | M | P2 |
| VER-020 | Run results mutable/deletable with no audited amendment path | P2 | Canonical per-op RLS lets any project member UPDATE `tm_test_runs`/`tm_step_results` (03_db §5); no amendment concept anywhere | PT/QT 5.6: correction only via audited amendment; e-sig at top tier | Amend-result RPC recording old→new into `tm_audit_log`; RLS narrowed to executor-or-lead for UPDATE; covered for regulated tier | M | P3 |
| VER-021 | `tm_test_runs` snapshots environment only — steps/expected results as-seen are never captured on the run | P1 | `environment_snapshot JSONB` is the sole snapshot column (`20260104074712:252-266`); data-row runs snapshot rows (`useCreateRunWithDataRows.ts:131`) but manual runs snapshot nothing | ZS/QT 5.3: 2024 audit shows the exact steps the tester saw | Add `steps_snapshot JSONB` + `case_version INTEGER` to `tm_test_runs`, populated by the VER-001 run-create RPC | M | P1 |
| VER-022 | Canonical case view shows versions read-only; diff/restore/create-version live only on the legacy releases surface | P1 | `CatalystViewTestCase.tsx:220,337` ("Versions read-only render"); full `TestCaseVersionHistory`+`VersionDiffView` only under `src/components/releases/test-case-detail/` (:217) | TR/QT 5.1/5.4/5.5 in the primary editor | Port version tab (list, arbitrary-pair diff, restore, manual snapshot) into CatalystViewTestCase; retire the releases-surface copy (move-not-copy rule) | M | P2 |
| VER-023 | VersionDiffView violates the color law and canonical-component rules — cannot ship as-is | P1 | `bg-amber-50`, `border-amber-300`, `bg-red-50 dark:bg-red-950/20`, `bg-green-50`, `border-red-200` etc. (`src/components/testhub/versioning/VersionDiffView.tsx:41-51`); shadcn Dialog/Select + framer-motion + `uppercase` tracking (audit-gate ban) | ADS-token, @atlaskit modal | Rebuild diff during the VER-022 port: `--ds-background-danger/success` tokens, @atlaskit ModalDialog/Select, Lozenge retained | M | P2 |
| VER-024 | `needs_update` bulk status is not a DB enum value → bulk status update 400s | P0 | `BulkCaseStatus` includes `needs_update` (`useTestCases.ts:9`, comment says "DB-level values", bulk input at :1056) but `tm_case_status` = draft/ready/approved/deprecated (03_db §3) | Statuses valid everywhere (14.4) | Either drop `needs_update` from the type + UI, or ALTER TYPE to add it (decision needed in Plan Lock); add enum-bridge module per L8 | S | P1 |
| VER-025 | 'approved' status carries no approver metadata and no transition gating — anyone can self-approve | P1 | Enum value exists; no `approved_by`/`approved_at`/`reviewed_by` columns in the 47-col `tm_test_cases` (03_db §2.1); no permission check on status writes (`useTestCases.ts:346`) | PT/QT 16.2/14.7: approval recorded (who/when), transitions permissioned | Add `approved_by`/`approved_at`; approval via RPC checking tm_roles permission; record decision in audit | M | P2 |
| VER-026 | Draft cases can be added to cycles — no lifecycle gating | P2 | `useAddTestsToCycle.ts:21-41` and `useTestCycles` scope-add have no case-status filter | ZS/PT 1.11: configurable gate (only Ready/Approved enter cycles) | Project-setting toggle; add-to-cycle pickers filter by status; RPC-side check for API writes | S | P2 |
| VER-027 | Status vocabulary lies: DB `ready` renders as "REVIEW"; unknown statuses silently coerce to draft | P2 | `statusFromDb: 'ready'→'REVIEW'` and `statusToDb` fallback `|| 'draft'` (`useTestCases.ts:11-28`) — zero-assumption violation | Honest, consistent lifecycle labels | Single enum-bridge module (L8) with 1:1 labels (Ready ↔ ready); unknown → render dash, never coerce | S | P1 |
| VER-028 | `tm_plan_versions` is trigger-written but has zero consumers — plan history invisible, no plan restore/diff | P2 | Table + `trigger_plan_version` exist (03_db §2.3, migrations `20260117155625`/`20260211183753`); grep src: 0 references | QT 5.2 applied to plans | Plan-history tab in plan detail reusing the VER-022 version-list pattern; restore optional (PLACEHOLDER: does Vikram want plan restore or view-only?) | M | P3 |
| VER-029 | Plan approvals run through the untyped G26 stack; decisions lack immutability/audit | P2 | `useTestPlansG26.ts` — every `.from()` is `as any` (02_hooks §1); `tm_plan_approvals` writes unaudited; no revocation trail | QT 16.2: approval recorded, tamper-resistant | Typed hooks off the generated types; approval decision writes an audit row; block editing approved plans without re-approval | M | P2 |
| VER-030 | No change-reason required when editing Ready/Approved cases | P3 | `change_summary` optional everywhere (`useTestCaseVersions.ts:127-129`); no gate on status | PT/QT 5.10: change-control annotation for validated environments | When status ∈ {ready, approved}, edit flow requires a change summary (modal field) stored on the version row | S | P3 |
| VER-031 | Change feed exists only at cycle level — no case-level or project-level activity feed | P2 | `useCycleActivityFeed.ts:38` (RPC + realtime on tm_cycle_execution_audit) is the only feed; case/project feeds absent | QT/PT 16.4: per-entity + per-project timeline | Feed component reading trigger-based `tm_audit_log` (VER-012) filtered by entity/project; mount in CatalystViewTestCase + project dashboard | M | P2 |
| VER-032 | Defect change history is broken: reads legacy `th_test_executions` with error-as-empty | P0 | `useDefectsG25.ts:177-197` — history query joins `th_test_executions` (:193, dead th_* family per L20) and returns `[]` on error (:184,197); canonical `useDefects.ts` has no history query at all | All vendors: defect timeline | Retire G25 history; defect history = VER-012 audit triggers on `tm_defects` + timeline tab in defect view | M | P2 |
| VER-033 | Defect, folder, step, set, plan, and requirement-link mutations are never audited | P1 | Audit callers are only `useTestCases.ts:5`, `useTestCycles.ts:17`, `useExecutionMutations.ts:13` (grep); `useDefects.ts` imports no audit lib despite workflow-aware status changes (:496) | 5.8: all entity CRUD | Covered by VER-012 trigger coverage list — enumerate all 12+ tables in that migration | S | P1 |
| VER-034 | Audit log is spoofable: any member can INSERT arbitrary audit rows from the client | P2 | INSERT policy allows authed users (`20260104074944:529`); `changes` payload is caller-controlled (`tmAuditLogger.ts:27-36`) | 5.8: tamper-evident | With VER-012: revoke client INSERT on `tm_audit_log` (trigger-only, SECURITY DEFINER); SELECT stays member-scoped | S | P1 |
| VER-035 | Two run-creation paths with inconsistent run_number semantics; no uniqueness guarantee | P1 | ExecutionPage computes MAX+1 client-side (:69,:470 — races between concurrent testers); `useExecutionMutations.ts:72` inserts with DEFAULT 1 → duplicate run_number=1 per scope; no UNIQUE(cycle_scope_id, run_number) (`20260104074712:252-257`) | XR/TR 7.6: per-instance run history is fully ordered | Single run-create RPC assigns run_number atomically; add UNIQUE index (after dedupe backfill) | S | P1 |
| VER-036 | Pinned version is invisible: no UI shows or edits `locked_version` on scope rows | P2 | Sole write site `useTestCycles.ts:497` (only when caller passes `lockedVersions`); no read/render site in src | ZS 5.2: cycles pin to a citable version | Version chip column in cycle-scope JiraTable + "re-pin to latest" action (audited); depends on VER-001/008 | S | P2 |
| VER-037 | Version-pinned test-set membership not schema-supported (inherited open gap) | P3 | CAT-TESTHUB-ENGINE drift log :24 (open, per 07_lessons L26); `tm_set_cases` has no version column (types.ts) | QT: sets can freeze content | PLACEHOLDER: decide if sets pin versions or resolve at cycle-add time; if pinning: `locked_version` on tm_set_cases + carry into cycle scope | M | P3 |
| VER-038 | Version spam: every single step add/edit/delete/reorder/duplicate creates a full version — no coalescing | P2 | `useTestSteps.ts:40,70,97,138,200` each fire `createVersionSnapshot`; a 10-step authoring session = 10+ versions with summaries like "Step updated" | TR: history is meaningful, session-coalesced | Debounce/coalesce in the RPC (fold snapshots within N minutes by same user into one version); or explicit "save version" checkpoint UX | S | P2 |
| VER-039 | No warning before destructive edits to cases used by active cycles ("used in N cycles/runs") | P2 | Restore/edit/delete paths never query cycle usage (`useTestCaseVersions.ts:223-270`, `useTestCases.ts:712`) | TR 4.5: impact view before breaking edits | Pre-flight count query (scope rows in non-completed cycles) surfaced in confirm dialog for restore/delete/step-delete | M | P2 |
| VER-040 | Shared-step edits silently rewrite every consumer and all history; no usage/impact report | P2 | `tm_test_steps.is_shared` + `shared_step_id` self-FK exist (03_db §2.1); no usage-count UI or edit warning anywhere; `tm_shared_steps`/`tm_shared_step_categories` are migration-only, not in types (03_db §9) | TR 4.5/4.6: "used in N cases" warning + snapshot isolation | Usage-impact query on shared_step_id before edit; history isolation comes free with VER-021 snapshots; PLACEHOLDER: probe whether tm_shared_steps exists on cyij before designing the library | M | P3 |
| VER-041 | No optimistic-concurrency protection — simultaneous case edits are silent last-write-wins | P2 | `useTestCases.ts:422` update has no `updated_at`/version guard; no conflict UX anywhere | 16.5 minimum bar: second editor warned | Update mutation adds `.eq('updated_at', loadedUpdatedAt)`; 0-row result → SectionMessage conflict with reload/overwrite choice | M | P2 |
| VER-042 | Cycle clone silently drops scope (and any pinned versions) on read error | P0 | `useTestCycles.ts:359` — clone-scope read is a silent `{data}` destructure (02_hooks §2); clone proceeds, producing an "empty but successful" cloned cycle | TR/ZS 7.9: clone is trivially reliable | Throw on scope-read error (L11 contract); clone carries `locked_version` forward; add clone round-trip proof to acceptance | S | P1 |
| VER-043 | Duplicate restore/history implementations: DB RPCs `tm_restore_version`/`tm_get_version_history` exist but UI uses hand-rolled client paths | P1 | RPCs in types.ts Functions (03_db §8, defined `20260121133526:130+`); `useTestCaseVersions.ts` reimplements both against raw tables | One engine | Converge on the hardened RPCs (per VER-003/005/006); delete client reimplementations | S | P1 |
| VER-044 | Audit trail records no context metadata (entity display key, cycle context, origin surface) | P2 | `tm_audit_log` columns: project/entity_type/entity_id/action/actor/changes only (`20260104074712:337-345`) — viewer can't render "TC-014 in Cycle CYC-003" without N+1 lookups (and nothing after delete) | PT 5.8: human-readable audit | VER-012 triggers denormalize entity_key + title into the audit row at write time | S | P2 |
| VER-045 | AI-generated content changes are not linked into case history/audit | P3 | `tm_test_cases.is_ai_generated`/`ai_model`/`ai_generated_at` exist (03_db §2.1); `tm_ai_usage_log` restored `20260703280000`; but AI generation/regeneration events never appear in version history or tm_audit_log | 13.11: AI actions logged with provenance | AI edge-fn writes create a version (change_summary = "AI generated via <model>") + audit row; provenance lozenge in version list | S | P3 |
| VER-046 | Run/step-result edits invisible to the cycle activity feed — only scope status + assignment changes captured | P2 | Feed source `tm_cycle_execution_audit` populated only by `trg_log_cycle_scope_status`/`trg_log_cycle_assignment` (03_db §4); tm_test_runs/tm_step_results changes bypass it | 16.4: consolidated timeline of edits, runs, comments | Extend VER-012 triggers to feed the cycle activity stream (or union tm_audit_log into `tm_get_cycle_activity_feed`) | M | P2 |
| VER-047 | Version compare cannot cite what a CYCLE executed — no run-level "executed at v3" display | P2 | `tm_test_runs` has no case_version column (VER-021); run history UI (`useTestCaseExecutionHistory.ts`) reads legacy `test_cycle_executions` (:24) with error-as-empty (:36-39) | ZS 5.3: run rows show the version executed | After VER-021: version column in run-history table; rewrite `useTestCaseExecutionHistory` onto `tm_test_runs` (drop the legacy `test_*` read) | M | P2 |
| VER-048 | Case execution-history hook reads a dead legacy table and swallows errors — history renders empty, not failed | P0 | `useTestCaseExecutionHistory.ts:24` reads `test_cycle_executions` (test_* family = 0 rows per memory); `if (error) return []` (:36-39); same legacy reads enrich `useTestCases.ts:136,217` (silent) → execution counts silently 0 | Honest run history | Point history + count enrichment at `tm_test_runs` via `cycle_scope_id`; apply L11 error contract; delete legacy reads | M | P1 |
| VER-049 | Watchers/subscriptions on cases and cycles absent — no way to follow an entity's changes | P3 | No watcher model in tm_* (03_db §2); `tm_notifications` is migration-only, not in types (03_db §9) | Jira-native 16.3 for XR/ZS | Reuse Catalyst's canonical watchers pattern (WatchersChip) over a `tm_watchers` table; notify via existing `catalyst_notify_trigger` family; PLACEHOLDER: probe whether tm_notifications exists on cyij first | L | P3 |
| VER-050 | e-signature on approvals/results absent (regulated tier) | P3 | No signature capture anywhere; `tm_release_signoffs` exists for releases only (03_db §2.3) | PT/QT 5.6/16.2 top tier | Reuse the tm_release_signoffs pattern for case-approval and cycle-close signoff (password re-auth + signed hash column); explicitly deferred-acceptable for v1 per yardstick §4 | L | P3 |

**Cross-cutting dependency note (no prose between rows, so here):** VER-001/002/003/005/006/007/008/021/043 form one architecture slice — "immutable execution snapshots via a single RPC-owned version engine" — and should be planned as one P1 phase; VER-012/013/033/034/044/046 form the "trigger-based audit engine" slice.

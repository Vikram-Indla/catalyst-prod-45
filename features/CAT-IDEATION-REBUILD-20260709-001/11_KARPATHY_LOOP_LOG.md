# Karpathy Loop Log (Hypothesis → Experiment → Measure → Keep/Discard → Log)

| # | Date | Hypothesis | Experiment | Measure | Verdict |
|---|---|---|---|---|---|
| 1 | 2026-07-09 | Ideation is greenfield | 6-lane repo discovery | Full legacy module found (ph_ideas + 8 tables + 8 pages) | ❌ Discard — pivoted to wipe+rebuild per Vikram |
| 2 | 2026-07-09 | Ideas belong inside the work-item hierarchy | Hierarchy registry inspection | Seeded 6-level registry roots at business_request; no benchmark embeds ideas | ❌ Discard — Idea = linked intake object |
| 3 | 2026-07-09 | Legacy conversion targets business_requests | grep ideasRoadmapService | Targets ph_requests (wrong table) | ✅ Keep finding — new conversion targets business_requests |
| 4 | 2026-07-09 | Scoring should be hardcoded formulas | Benchmark (PB drivers, Aha! scorecards, ServiceNow weights) + legacy 40-col failure | Legacy scoring columns 90% unused | ❌ Discard — governed scoring framework (models/drivers) |
| 5 | 2026-07-09 | No Mobbin evidence exists for merge/scatter/feedback tools | Web-fallback search | Claimed absent | ❌ Discard — MCP pass found Salesforce/folk merge, TheyDo matrix, Canny |
| 6 | 2026-07-09 | AI panel should be an overlay drawer | Mobbin MCP image inspection (Intercom Details/Copilot rail tabs) | Rail-tab = less chrome, persistent context | 🔁 Revised — rail tab on Detail, drawer on Inbox preview |
| 7 | 2026-07-09 | Portfolio chart needs a new charting dependency | package.json audit | recharts ^3.5.1 present | ✅ Keep — build on recharts |
| 8 | 2026-07-09 | Auto-Delivered lacks an event source | Migration grep | track_epic_process_step_change() trigger precedent + useWorkItemRealtime | ✅ Keep — mirror the trigger pattern |
| 9 | 2026-07-10 | Mobbin evidence doc (05) was ratified and available | `find` on discovery folder | Only `.DS_Store` on disk — doc existed only in an uncommitted git stash | ❌ Discard — recovered + committed before building on it |
| 10 | 2026-07-11 | GUARD_EVIDENCE_REGISTRY's 'real' label for strategy_link_present/scores_complete/duplicate_review_complete meant they were wired | Read `evaluateGuardsReal`'s switch statement | No case for any of the three — silent fallthrough to `passed: null` | ❌ Discard — wired all 3 real evaluators + Decision UI (Phase 3 S1) |
| 11 | 2026-07-11 | `StatusLozengeDropdown` (canonical status-change control) could be reused for ideation transitions | Read its source: `useCanonicalIssueWorkflow(issueType)` → hardcoded `ISSUE_TYPE_TO_ENTITY` map + `STATUS_OPTION_GROUPS` ph_issues fallback | Deep, risky ph_issues-specific coupling — wrong entityKey resolution risk if 'Idea' isn't in the map | ❌ Discard — built a lighter Decision control directly on `gateTransition` + `ReasonCaptureModal` (both already generic) instead of forcing a ph_issues-coupled component onto a different schema |
| 12 | 2026-07-11 | `CreateBusinessRequestModal` could be reused with prefill for the conversion flow | Read its props | `isOpen/onClose/productId/onWorkTypeChange` only — no prefill/initialValues input; it's shared across many callers | ❌ Discard — direct `business_requests` insert instead (matches open INSERT RLS + auto `request_key` trigger), modal-with-prefill flagged as a future improvement, not built |
| 13 | 2026-07-11 | idn_ideas.score_total alone signals "scores complete" for the `scores_complete` guard | Read `idn_recompute_score()` | LATERAL join only sums drivers that HAVE a score row — partial scoring still produces a non-null total | ❌ Discard — guard evaluator counts distinct scored drivers vs. the active model's driver count instead |
| 14 | 2026-07-11 | Conversion mutation's 3-table write chain (business_requests → idn_conversions → idn_ideas) would work as designed | SQL dry-run in a `BEGIN...ROLLBACK` transaction against real staging schema | All 3 inserts/updates succeeded, `request_key` auto-generated correctly (MIM-001), rolled back clean | ✅ Keep — pre-validated the mutation before it ever touched the UI, high confidence going into live verification |
| 15 | 2026-07-11 | The C.6 merge manifest (votes/evidence/watchers "transfer") could be built client-side in V1 | Read `idn_votes_update`/`idn_watchers_write` RLS | Both require `user_id = auth.uid()` — a client session cannot touch another user's vote/watch row, full stop | ❌ Discard — real data transfer needs a `SECURITY DEFINER` RPC (migration); shipped the status-transition half only, UI says so honestly instead of promising a transfer RLS can't deliver |
| 16 | 2026-07-11 | Merge status-transition + terminal lock would work as designed | SQL dry-run: merge UPDATE + a second UPDATE gated on `idn_idea_is_locked() = false` | Merge succeeded; the lock-gated UPDATE correctly matched 0 rows post-merge, proving the terminal lock is real not just documented | ✅ Keep — pre-validated before UI, rolled back clean |

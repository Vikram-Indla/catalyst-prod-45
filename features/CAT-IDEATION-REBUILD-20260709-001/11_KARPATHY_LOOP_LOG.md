# Karpathy Loop Log (Hypothesis → Experiment → Measure → Keep/Discard → Log)

| # | Date | Hypothesis | Experiment | Measure | Verdict |
|---|---|---|---|---|---|
| 1 | 2026-07-09 | Ideation is greenfield | 6-lane repo discovery | Full legacy module found (ph_ideas + 8 tables + 8 pages) | ❌ Discard — pivoted to wipe+rebuild per Vikram |
| 2 | 2026-07-09 | Ideas belong inside the work-item hierarchy | Hierarchy registry inspection | Seeded 6-level registry roots at business_request; no benchmark embeds ideas | ❌ Discard — Idea = linked intake object |
| 3 | 2026-07-09 | Legacy conversion targets business_requests | grep ideasRoadmapService | Targets ph_requests (wrong table) | ✅ Keep finding — new conversion targets business_requests |
| 4 | 2026-07-09 | Scoring should be hardcoded formulas | Benchmark (PB drivers, Aha! scorecards, ServiceNow weights) + legacy 40-col failure | Legacy scoring columns 90% unused | ❌ Discard — governed scoring framework (models/drivers) |
| 5 | 2026-07-09 | No Mobbin evidence exists for merge/scatter/feedback tools | Web-fallback search | Claimed absent | ❌ Discard — MCP pass found Salesforce/folk merge, TheyDo matrix, Canny |
| 6 | 2026-07-09 | AI panel should be an overlay drawer | Mobbin MCP image inspection (Intercom Details/Copilot rail tabs) | Rail-tab = less chrome, persistent context | 🔁 Revised — rail tab on Detail, drawer on Inbox preview |
| 7 | 2026-07-09 | Portfolio chart needs a new charting dependency | package.json audit | recharts ^3.5.1 present | ✅ Keep — build on recharts |
| 8 | 2026-07-09 | Auto-Delivered lacks an event source | Migration grep | track_epic_process_step_change() trigger precedent + useWorkItemRealtime | ✅ Keep — mirror the trigger pattern |
| 9 | 2026-07-10 | Mobbin evidence doc (05) was ratified and available | `find` on discovery folder | Only `.DS_Store` on disk — doc existed only in an uncommitted git stash | ❌ Discard — recovered + committed before building on it |
| 10 | 2026-07-11 | GUARD_EVIDENCE_REGISTRY's 'real' label for strategy_link_present/scores_complete/duplicate_review_complete meant they were wired | Read `evaluateGuardsReal`'s switch statement | No case for any of the three — silent fallthrough to `passed: null` | ❌ Discard — wired all 3 real evaluators + Decision UI (Phase 3 S1) |
| 11 | 2026-07-11 | `StatusLozengeDropdown` (canonical status-change control) could be reused for ideation transitions | Read its source: `useCanonicalIssueWorkflow(issueType)` → hardcoded `ISSUE_TYPE_TO_ENTITY` map + `STATUS_OPTION_GROUPS` ph_issues fallback | Deep, risky ph_issues-specific coupling — wrong entityKey resolution risk if 'Idea' isn't in the map | ❌ Discard — built a lighter Decision control directly on `gateTransition` + `ReasonCaptureModal` (both already generic) instead of forcing a ph_issues-coupled component onto a different schema |
| 12 | 2026-07-11 | `CreateBusinessRequestModal` could be reused with prefill for the conversion flow | Read its props | `isOpen/onClose/productId/onWorkTypeChange` only — no prefill/initialValues input; it's shared across many callers | ❌ Discard — direct `business_requests` insert instead (matches open INSERT RLS + auto `request_key` trigger), modal-with-prefill flagged as a future improvement, not built |
| 13 | 2026-07-11 | idn_ideas.score_total alone signals "scores complete" for the `scores_complete` guard | Read `idn_recompute_score()` | LATERAL join only sums drivers that HAVE a score row — partial scoring still produces a non-null total | ❌ Discard — guard evaluator counts distinct scored drivers vs. the active model's driver count instead |
| 14 | 2026-07-11 | Conversion mutation's 3-table write chain (business_requests → idn_conversions → idn_ideas) would work as designed | SQL dry-run in a `BEGIN...ROLLBACK` transaction against real staging schema | All 3 inserts/updates succeeded, `request_key` auto-generated correctly (MIM-001), rolled back clean | ✅ Keep — pre-validated the mutation before it ever touched the UI, high confidence going into live verification |
| 15 | 2026-07-11 | The C.6 merge manifest (votes/evidence/watchers "transfer") could be built client-side in V1 | Read `idn_votes_update`/`idn_watchers_write` RLS | Both require `user_id = auth.uid()` — a client session cannot touch another user's vote/watch row, full stop | ❌ Discard — real data transfer needs a `SECURITY DEFINER` RPC (migration); shipped the status-transition half only, UI says so honestly instead of promising a transfer RLS can't deliver |
| 16 | 2026-07-11 | Merge status-transition + terminal lock would work as designed | SQL dry-run: merge UPDATE + a second UPDATE gated on `idn_idea_is_locked() = false` | Merge succeeded; the lock-gated UPDATE correctly matched 0 rows post-merge, proving the terminal lock is real not just documented | ✅ Keep — pre-validated before UI, rolled back clean |
| 17 | 2026-07-11 | Entries 14 & 16's `BEGIN...ROLLBACK` SQL dry-runs meant Merge/Conversion were proven safe before ever touching the UI | Live browser test via Chrome MCP (real signed-in session, real anon/authenticated Supabase client, RLS actually enforced) — NOT the same as the dry-run | Both hit `"new row violates row-level security policy for table \"idn_ideas\""` on the very first real click. **The dry-runs were false confidence**: `execute_sql` via the Supabase MCP connector runs with elevated/service-role-equivalent access that bypasses RLS entirely — it only proved the SQL was schema-valid (columns, types, constraints), never that RLS would allow it | ❌ Discard the assumption that a `BEGIN...ROLLBACK` dry-run via this MCP tool validates RLS-gated writes — it doesn't. Root cause: `idn_ideas_update` has no explicit `WITH CHECK`, so Postgres reuses `USING` evaluated against the NEW row; both `decision IS DISTINCT FROM 'merged'` (Merge) and `converted_business_request_id IS NULL` (Conversion) fail against the exact values their own transitions are trying to set. Pre-existing bug in the original S1 migration, not introduced this session. Needs a migration (split `USING`/`WITH CHECK` properly) — flagged, not silently routed around mid-autonomous-loop |
| 18 | 2026-07-11 | Compensating cleanup (delete the orphaned `business_requests` + `idn_conversions` rows) would work when the final `idn_ideas` update fails | Live test: triggered the failure, checked `business_requests`/`idn_conversions` row counts after | Cleanup silently no-op'd — `idn_conversions` has no DELETE RLS policy at all (only SELECT + INSERT), so the delete matched 0 rows, which then blocked the `business_requests` delete via its `ON DELETE RESTRICT` FK | ❌ Discard the assumption that the compensating delete works. A second, independent RLS gap — kept the attempt in code (harmless, forward-compatible) with an accurate comment instead of removing it; documented as a known consequence needing the same migration pass as #17 |
| 19 | 2026-07-11 | `@atlaskit/popup` (Catalyst wrapper AND raw import, tried both) would position correctly for `VoteControl`'s trigger | DOM inspection via `javascript_tool`: `getComputedStyle` on the popper wrapper across both attempts | Both left the wrapper stuck at `position:fixed; top:0; left:0` with no transform ever applied — Popper's `update()` never ran in this component tree, matching the exact failure `StatusLozengeDropdown.tsx` already documents in its own header comment for the same library | ❌ Discard `@atlaskit/popup` for this component. Rebuilt with the same manual-position + `createPortal` pattern `StatusLozengeDropdown` already uses (trigger `getBoundingClientRect()` on click, portal to `document.body`, `position:fixed` with computed `top`/`left`) — confirmed working live, correctly anchored, vote cast/changed live with real DB writes |
| 20 | 2026-07-11 | `notification_trigger_config`'s `recipients_config` gates who actually gets notified | grepped for `recipients_config`/`trigger_key` usage outside the admin-config hook | Zero hits — that table is read only by `useNotificationTriggers.ts` (admin catalog UI), never by any dispatch code anywhere in the app | ❌ Discard — the real dispatch pattern is a direct `notifications` table insert at the point of the event (found in `useInteractionRecorder.ts`'s `createMentionNotifications`), not a config-driven system. Mirrored that pattern for ideation instead of building a config-reading dispatcher that nothing else in the codebase actually uses |
| 21 | 2026-07-11 | `workItemRepo.ts`'s `softDelete` notification insert (`user_id`/`title`/`message`/`is_read`) was the current `notifications` schema | Queried live `information_schema.columns` for `notifications` on staging | Live schema is `recipient_user_id`/`notification_type`/`entity_title`/`status`/`status_type`/etc. (a later, different migration) — `workItemRepo.ts` targets a superseded shape and would fail at runtime; not this feature's bug to fix, noted for awareness | ❌ Discard `workItemRepo.ts` as the reference; used `useInteractionRecorder.ts`'s `createMentionNotifications` instead, which matches the live schema exactly |
| 22 | 2026-07-11 | `status_type: 'red'`/`'yellow'` were valid values for declined/parked notifications | Live insert attempt via the actual mutation (not a migration read) | `CHECK (status_type = ANY (ARRAY['gray','blue','green','active','done','default']))` — real constraint, caught by the actual failed insert, console-logged the exact reason | ❌ Discard 'red'/'yellow' — remapped declined/parked to 'default' (no red/yellow color exists in this constraint at all), re-tested live, confirmed working (`IDEA-15` decision notification: `status_type='green'`, correct row) |
| 23 | 2026-07-11 | `idn_evidence_insert`'s RLS might have the same self-referential lock bug as `idn_ideas_update` (Merge/Conversion) | Read the policy text before building anything, not after | `WITH CHECK` only tests `added_by = auth.uid()` + the idea's current lock state — no field this INSERT itself sets is checked, so no self-reference is possible | ✅ Keep — confirmed clean before writing code, then verified live (2 evidence rows added, 1 deleted, all correct) rather than assuming clean-on-paper meant clean-in-practice given how wrong that assumption was for Merge/Conversion |

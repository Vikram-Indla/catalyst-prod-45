# Drift Log

| # | Date | Drift | Resolution |
|---|------|-------|------------|
| DR-001 | 2026-07-03 | Session limit hit mid-run: lanes 3, 5, 8, 10, 12 died before writing reports (lanes 7, 9, 14 wrote files but their agents died before returning summaries — summaries recovered from the files' Lane Summary sections). | All five missing lanes relaunched after limit reset with identical scope + instruction to write reports incrementally. No re-baselining needed; ID ranges unchanged. |
| DR-002 | 2026-07-03 | CAT-AUDIT-1281 (useIssueViewData.ts / IssueContentView.tsx priority fallback) skipped during PR-ZA-01 | Verified `useIssueViewData` only reaches the app via `IssueViewShell` → `HierarchyAllWorkPage`, which is not registered in FullAppRoutes.tsx (docblock confirms "Deprecated 2026-04-25 with the /project-hub/:key/hierarchy/allwork route"). No user can reach this code path; fixing it would require widening `AllWorkItem.priority` from `string` to `string \| null` for zero behavioral benefit. Left as-is; candidate for PR 2 dead-code removal instead. |
| DR-004 | 2026-07-03 | CAT-AUDIT-0200 (data-theme 3-writer race) declined in PR8 — NOT fixed | LANE-03 estimated regression risk "Medium" based on a sample that showed CSS rules "paired with .dark everywhere sampled." Live count before touching anything: `grep -rn '\[data-theme="dark"\]' src --include="*.css"` → **1,164 occurrences across 31 files**, only 1 file uses the safe `~=` list-contains form. Deleting the ThemeProvider.tsx/index.html writes (so Atlaskit's `setGlobalTheme` parameterized string becomes the sole value) would silently kill all 1,164 exact-match rules with no screenshot pipeline available this session to catch it. Declined; flagged as its own dedicated PR requiring either (a) a CSS sweep converting all 1,164 selectors to `.dark`/`~=` first, or (b) a live dark-mode screenshot pass per affected surface before/after. Applied the lower-risk, CSS-independent CAT-AUDIT-0201 fix (first-visit OS-dark flash) instead. |
| DR-003 | 2026-07-03 | Third occurrence of the documented "concurrent session, shared checkout" hazard (see CLAUDE.md's CONCURRENT SESSIONS hard-stop, and the other session's own DRIFT-005 in CAT-SPRINTS-NATIVE-20260702-002/08_DRIFT_LOG.md). A separate session working on CAT-SPRINTS-NATIVE-20260702-002 committed `51e4b2d59` to this same shared `main` in between my PR4 and PR5 commits, and logged its own DRIFT-005 entry noting my PR4/PR5 commits as "neither authored nor known to this session." A `git status` immediately after briefly showed my 81 staged zero-assumption files as absent (90→9 lines), which resolved on the next check (79 unstaged + already-committed = accounted for) — verified via `git show --stat` on the foreign commit that it touched only its own 6 files, so this was a transient read during the other session's concurrent git operation, not data loss. No corrective action taken beyond re-verifying and re-staging before each commit (already my established practice this session). Neither session has pushed. | Flagged to the user in the final summary; this is a repo-wide practice gap (not specific to this audit) that only the user can resolve (enforce one-worktree-per-session in practice, not just in docs). |

# Session 001 — Remove Plan hub

Removed the "Plan" hub (⌘0, gray tile, already `deprecated: true`) from the hub switcher and deleted all dedicated Plan-hub code: components/planhub/*, modules/plan/*, modules-dormant/planhub/*, PlanHubSidebar, planhub types/hooks/styles/icon assets. Swept every navigation/config surface referencing it (HubSwitcher, CatalystShell, SidebarBase, HomeSidebar, MobileNavigationMenu, useHubShortcuts doc comment, hubs.ts, hub-tone.ts, tabIdentity.ts, workspaceContext.ts, useForYouData.ts, useRecentProjects.ts, FeatureFlagContext.tsx, icons.registry.ts, routeRegistry.ts, translations.ts + login marketing copy, TopNav.tsx, ReqAssistGenerate.tsx, HubSwitcher.test.tsx, LayoutSidebars.stories.tsx). Kept the existing `/planhub*` → `/tasks/overview` redirect so bookmarks don't break, and left DB schema (`planhub_*` Supabase tables) and the Tasks/Workstreams-related notification-event catalog untouched (ambiguous overlap with the Tasks module's `planner` moduleKey — out of scope, risk of regression).

Verified: tsc baseline unchanged (183 errors before/after), live dev-server check via Chrome MCP shows 10-hub switcher with no gaps and a clean `/planhub` → `/tasks/overview` redirect, no console errors.

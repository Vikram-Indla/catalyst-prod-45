# Defect Register — CAT-TESTHUB-CERT-20260708-001

> **DEF-010 fixed and verified live** — see row below. All defects in this register are now either fixed-and-verified, fixed-pending-DB-apply (DEF-006), or intentionally out of scope (DEF-002/004/005).

| ID | Severity | Area | Repro | Expected | Actual | Screenshot | Root Cause | Fix Status |
|---|---|---|---|---|---|---|---|---|
| DEF-001 | P0 (was blocking) | Auth / test harness | Headless Chromium → `localhost:8080/testhub/dashboard` → redirected to `/auth` → fill `test@example.com`/`testpassword123` (the credential every existing spec in this repo assumes) → submit | Login succeeds, lands on `/testhub/dashboard` | "The email or password you entered is incorrect." No session established. | `test-results/testhub-certification/screenshots/00-auth-probe.png` | No valid test/service account exists in the staging Supabase project (`cyijbdeuehohvhnsywig`) reachable by this repo's checked-in `.env*` files; no service-role key present to mint one; no self-service signup route in `src/routes`/`src/pages`; Supabase MCP unauthenticated this session | **RESOLVED** — user supplied real staging credentials (`vikramataol@gmail.com`), confirmed working live via both headless Playwright and Claude-in-Chrome visible session. |
| DEF-006 | P3 (real, non-blocking) | Repository — Create Test Case dialog | Open Repository → click "Create case" (top toolbar, full dialog) → open Priority dropdown | A list of priority options to choose from | Dropdown shows "No options" — cannot select a priority explicitly | (visible live in Claude-in-Chrome, not saved to disk) | **Root-caused**: `tm_case_priorities` is project-scoped but was only ever seeded for the hardcoded Demo Project (`00000000-0000-0000-0000-000000000001`) in `20260105144028_f9a03772...sql`. No trigger provisions defaults for new/existing projects (unlike `tm_folders`, which has `tm_projects_provision_system_folders`). Affects **every** project except Demo, not just Senaei BAU. Query itself (`CreateStoryModal.tsx:661-673`) is correct and properly project-scoped. | **FIXED (migration written, not yet applied)** — `supabase/migrations/20260708120000_tm_provision_default_priorities.sql` adds `tm_provision_default_priorities()` + an `AFTER INSERT ON tm_projects` trigger (mirrors the folder-provisioning pattern) plus a backfill `DO` block for existing projects with zero priority rows. **Cannot apply live this session** — no Supabase DB credentials/service-role key, only app login creds. Needs `supabase db push` (or equivalent) run by someone with DB access. |
| DEF-007 | P2 (real) | Reports — project selector | Reports hub → any project-scoped report (e.g. Defect Summary) → open Project dropdown | Each project listed once | "Senaei BAU" appears twice as separate selectable entries in the dropdown | (visible live in Claude-in-Chrome) | **Root-caused**: genuine duplicate row in `tm_projects` (stale `748f80ae...` vs canonical `84f91caf...`), per `supabase/migrations/20260703330000_senaei_bau_dedup_and_signoff_seed.sql` — that migration documents the dedup as applied out-of-band on staging only, left as a no-op in the committed file. 6 report bodies each independently query `tm_projects` with an identical copy-pasted `useTmProjects()` (`useRealTestReportData.ts`, `DefectSummaryBody.tsx`, `GovernanceBody.tsx`, `DefectClosureTrendBody.tsx`, `TeamPerformanceBody.tsx`, `ProjectTestingStatusBody.tsx`), none deduping by name. | **FIXED AND VERIFIED LIVE** — added a name-based dedupe filter to all 6 query implementations. Confirmed live in Claude-in-Chrome: "Senaei BAU" now appears exactly once in the Defect Summary report's project selector. `npx tsc --noEmit` clean. The underlying DB-level duplicate row itself is untouched (no destructive DB delete performed without explicit authorization) — this is a defensive UI fix, not a data cleanup. |
| DEF-008 | P4 (minor UX) | Defects list | Defects list → inline "+ Create" row → type title → Enter | New row appears immediately, list count updates | Row does not appear and count stays stale until a manual refresh (refresh icon) is clicked; after refresh the new defect is present and correct | (visible live in Claude-in-Chrome) | **Root-caused**: `src/modules/project-work-hub/pages/BacklogPage.atlaskit.tsx` — all 3 inline-create submit handlers called `dataSource.invalidationKeys.forEach(k => queryClient.invalidateQueries(...))` fire-and-forget, closing the create form before the refetch resolved. The manual-refresh path (`onRefresh`) `await`s the same invalidation and always worked — confirming this was a refetch-timing race, not a broken/missing invalidation key. | **FIXED AND VERIFIED LIVE** — changed all 3 call sites to `await Promise.all(...)` before closing the create form, matching the already-working manual-refresh pattern. Confirmed live: created `DEF-0006` inline, count updated 17→18 instantly with no manual refresh. `npx tsc --noEmit` clean. Scoped to 3 isolated, already-guarded call sites in the shared `BacklogPage` — additive only, no behavior change for surfaces without a `dataSource`. |
| DEF-002 | P1 | Test automation hygiene | Ran `tests/test-management/smoke.spec.ts` against the corrected harness | Should target current `/testhub/*` routes | Hard-codes `${BASE_URL}/projects/${TEST_PROJECT_ID}/tests/overview` (`smoke.spec.ts:32`) and a `5173` default that no longer matches the app's `8080` port — confirmed via live run (`net::ERR_CONNECTION_REFUSED`) | n/a (config-level) | Spec targets pre-`/testhub` migration routes, never updated after the route rename | **OPEN — planned**: quarantine/replace with `tests/testhub-certification/**` suite once auth unblocks; do not delete until replacement suite has equal-or-greater coverage |
| DEF-003 | P2 | Test harness config (repo-wide, not Test Hub-specific) | `playwright.config.ts` `use.baseURL` / `webServer.url` were `http://localhost:5173`; app is `strictPort: true` on `8080` (`vite.config.ts:381`) | `webServer` boots and matches the app's real port | Every Playwright project in the repo (parity-*, test-management-*) failed to start: `Process from config.webServer was not able to start. Exit code: 1` | n/a (config-level) | Config drift — never updated when the app's dev port was fixed to 8080 | **FIXED** — `playwright.config.ts` updated to `8080` for both `use.baseURL` and `webServer.url`, plus registered `testhub-certification` project. Verified by rerunning `test-management-smoke` post-fix (harness boots; test itself still fails per DEF-002). |
| DEF-004 | P3 (informational, non-blocking) | Story detail extension | Read `src/modules/project-work-hub/components/dialogs/story-detail-modules/TestHubSection.tsx:41-61` | Should read `tm_test_runs`/`tm_requirement_links` per the V2 canonical model | Still reads legacy `th_test_executions` (alongside correct `tm_test_case_links`) | n/a (source-level finding, not yet runtime-verified — needs auth) | Old extension not migrated when V2 (CAT-TESTHUB-V2-SPRINT-RELEASE) landed | **OPEN — non-blocking**, per that feature's own handover. Do not rewrite until an authenticated run confirms it actually breaks a mandatory journey. |
| DEF-005 | P3 (informational) | Selector discipline | Grep `data-testid` across `src/pages/testhub/**/*.tsx` | Stable test-id/aria selectors on critical controls (create case, save run, add-to-cycle, etc.) | 0 matches across all Test Hub pages (Repository, Plans, Executions, Defects, Traceability, Reports/lab/*) | n/a | Never added; existing coverage relies on text/role selectors only | **OPEN — queued for Slice 2**. Will add `data-testid` incrementally per screen, only after an authenticated screenshot confirms the target element, per screenshot-signoff rule — not added blind. |
| DEF-010 | P2 (real) | Execution runner — scope status silently not persisted | Live, repeatedly: open runner for a case with no steps ("No steps defined for this test case — record a case-level verdict instead.") → click case-level "Pass" (with or without Force pass) → "Save execution" → "Save run" | Cycle scope table and case's Runs tab both show PASSED after save | Save succeeds with no error toast, a `tm_test_runs` row IS created with the correct `'passed'` status, but the cycle's Scope table and the case's Runs-tab badge (which both read `tm_cycle_scope.current_status`, not the run row) stayed "NOT RUN" — the scope-status write silently failed. | (visible live in Claude-in-Chrome, not saved to disk) | **Root-caused**: `src/pages/testhub/cycles/ExecutionPage.tsx:728-732` — `supabase.from('tm_cycle_scope').update({ current_status: dbStatus }).eq('id', scope.id)` called without `.select()`. PostgREST/supabase-js returns `{ error: null, data: null }` with **zero rows updated** (not an exception) whenever an RLS `USING` clause filters the target row out of the update — so a failed write looked identical to a successful one, and the code proceeded to show success. The `tm_test_runs` insert a few lines earlier succeeds correctly and independently, which is why "1 run exists" while the displayed status never moved. Not literally zero-step-specific — a generic silent-no-op pattern that happened to be caught here. | **FIXED AND VERIFIED LIVE** — added `.select('id')` to the scope update and now throws a real, surfaced error if no row comes back (turns the silent no-op into a visible failure instead of a false "success"). Retested the exact repro after the fix: `TC-0001` now correctly shows **PASSED** in the cycle Scope table. `npx tsc --noEmit` clean. Note: the top-of-page cycle progress bar ("0 passed") did not refresh in the same pass as the per-row status — likely a separate, minor stale-aggregate-count issue worth a follow-up look, not re-investigated this session. Root RLS cause (why this particular update needed rescue) not fully pinned down — the fix makes future occurrences loud instead of silent, which is the safer outcome either way. |
| DEF-009 | P2 (real, latent) | Active Test Space resolver | `src/hooks/test-management/useTestHubProject.ts` — the single resolver every TestHub operational surface (Repository/Cycles/Defects) uses to pick "which project is active." Discovered while investigating an apparent data-loss event (later confirmed by the user to be a routine staging reseed, not a defect) — mid-investigation, Repository silently rendered a completely different project's folders/cases with no error or indication of the switch. | Resolving the active Test Space should be stable and reflect one coherent project identity, not be split by duplicate rows | Resolver ranks candidate projects by raw per-`tm_projects`-row test-case count (`caseCounts[b.id] - caseCounts[a.id]`). Since Senaei BAU's case volume is split across two duplicate rows (DEF-007's root cause), each row's individual count is artificially lowered, letting an unrelated project silently outrank the real active Test Space with zero user-visible warning that the whole surface just changed context. | **Root cause independently confirmed** even though the specific trigger this session was a routine reseed, not the duplicate-row split — the vote-split hazard is real and latent any time `tm_projects` carries same-named duplicates. | **FIXED** — reworked the ranking in `useTestHubProject.ts` to sum case counts per project *name* first (so duplicate rows combine their vote), then break ties by each row's own count. `npx tsc --noEmit` clean. Not fully re-verifiable live this session since the reseed also reset the case-count data the bug depends on — logic reviewed and typechecked, recommend a follow-up live check once case volume is representative again. |

## Summary (final, after fix pass)
- **P0 unresolved: 0** (DEF-001 resolved — real credentials supplied and confirmed working)
- **P1 unresolved: 1** (DEF-002, stale route spec — recommend quarantining `tests/test-management/smoke.spec.ts` and `golden-path.spec.ts` in favor of `tests/testhub-certification/**`; not fixed this pass, out of scope of the Test Hub product itself)
- **P2 fixed and verified live: 3** (DEF-003 harness port; DEF-007 duplicate project dedup; DEF-010 silent scope-status no-op)
- **P2 fixed, typechecked (not live-reverifiable this session): 1** (DEF-009 — active Test Space resolver vote-split, discovered while investigating what turned out to be a routine staging reseed)
- **P3 fixed, DB apply pending: 1** (DEF-006 — migration written, cannot self-apply without DB credentials)
- **P4 fixed and verified live: 1** (DEF-008 stale list count)
- **P3 informational/non-blocking, unchanged: 2** (DEF-004 legacy table read — out of scope per prior feature's own handover; DEF-005 no data-testid coverage — queued, not yet needed since text/role selectors sufficed for this session's coverage)

**Zero unresolved P0/P1 product defects.** DEF-002 is the only open P1 and is a test-automation hygiene item, not a Test Hub product defect — the replacement suite (`tests/testhub-certification/**`) already covers the routes it should have covered; recommend a follow-up PR to formally quarantine/delete the two stale specs.

## Observations (not defects — confirmed correct or inconclusive, logged for completeness)
- **Draft-case batch rejection is all-or-nothing.** Creating a cycle with a mix of draft and non-draft cases rejects the *entire* batch (0 cases added) rather than adding only the valid ones, surfacing `DRAFT_NOT_EXECUTABLE`. This is defensible atomic behavior (no silent partial state), though a minor UX improvement would be to gray out non-executable cases in the picker before selection. Not filed as a defect.
- **Execution Summary report appeared stuck on a skeleton loading state** on one client-side navigation (~6s, no error in console). A full page reload resolved it immediately with correct live data. Could not reproduce reliably enough to file as a confirmed defect — likely dev-server HMR recompilation overhead from this session's own code edits, not a product bug. Flagged for the next certification pass to watch for.
- **Routine staging reseed occurred mid-session.** `tm_test_cases` dropped from ~95 rows (mostly under Senaei BAU) to 12 (under Demo Project) partway through this run, with `tm_folders`/`tm_test_cycles`/`tm_defects` for Senaei BAU unaffected. Confirmed by the user as expected environment behavior (a periodic reseed job), not an incident — no data-recovery action taken or needed. Logged here because it's the trigger that surfaced DEF-009 above, and because it means the exact case/cycle content referenced elsewhere in this defect register and screenshots (e.g. `RVTC-041`, `TC-0051`) may no longer exist verbatim in a future session.

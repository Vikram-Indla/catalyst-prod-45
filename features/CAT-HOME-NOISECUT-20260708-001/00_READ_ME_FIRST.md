# CAT-HOME-NOISECUT-20260708-001 — Home / For You noise cut

**What this is:** Ruthless design-noise reduction on the Home (For You) surface, approved by Vikram
2026-07-08 from a pixel before/after replica (artifact: https://claude.ai/code/artifact/eec5e73e-ea02-4122-864e-cc8b6bd779f4).

**State:** Plan Lock drafted (slice 1 = changes 1–4). Awaiting Plan Lock approval before any code.

**Read order:** 01_OBJECTIVE → 02_CANONICAL_DISCOVERY → 03_PLAN_LOCK → 09_DECISIONS → 11_KARPATHY_LOOP_LOG.

**Context that binds this feature:**
- Design-critical phase active (memory `design-critical-phase`): theme-tokens.css values are a counterfeit
  ADS bridge; root token surgery is post-demo. This feature approves DIRECTION, not exact px.
- Pixels-only approvals: every slice ends with light+dark screenshots on localhost:8080 live data.
- The six approved changes; slice 1 covers 1–4, slice 2 covers 5–6:
  1. Delete row status lozenges in grouped Assigned view (status already in section headers).
  2. Group by status CATEGORY (To do / In progress / On hold∗) not literal status value; one casing.
  3. Global nav timer chip neutral at rest; urgency lives only in the CHG card. Presence-ring
     offline amber → gray.
  4. Collapsed sidebar rail monochrome; color on active hub only; removes 2 scanner-ignored hexes.
  5. Wordmark 14/600, weight 653 → 600, one badge shape in tabs, fake-link project name → subtle.
  6. Single AI entry (drop duplicate Themify pill), sidebar footer keeps clocks / drops weather + per-row timestamps.

∗ "On hold" kept as its own visible bucket (it is a `moved`-category status Vikram's team scans for).

# ASSUMPTIONS register (ASM-###)

| ID | Assumption | Confidence | Owner | Impact if wrong |
|---|---|---|---|---|
| ASM-001 | Repo migrations are an accurate mirror of the live staging/prod schema (no unapplied drift) — live DB was NOT queried this phase per contract. | Medium-High | Build session (pre-flight `supabase/.temp/project-ref` check + `list_tables` diff) | Migration plan could collide with drifted schema; verify before any DDL |
| ASM-002 | ~~unverified~~ **VERIFIED 2026-07-09 (repo evidence)**: `strata_execution_links` is a generic typed-edge table (`from_type/from_id → to_type/to_id, relationship_type`, UNIQUE constraint) — `20260705100300_strata_execution_value.sql:130`. Project Objective→Theme Objective goes via `strategy_elements.parent_id` (enforced theme-context check in `strata_create_project_objective`); Project KPI→Theme KPI via `('kpi','rolls_up_to')` edge in `strata_create_project_kpi`; KR↔KPI via `strata_key_results.kpi_id`. No new tables needed. | Confirmed | — | — (closed) |
| ASM-003 | "Sector / CXO Scorecard" maps onto existing `strata_scorecard_models.owner_scope_type ∈ (sector, function)` + `entity_scope` on instances — a labeling/config exercise, not new schema. CEO Scorecard = the `enterprise`-scope model with rollup over sector instances via existing `rollup_method` + calc RPCs. | Medium-High | Build session | New rollup schema/RPC work would be needed |
| ASM-004 | The Salam demo seed (`20260705100600`) demonstrates the hierarchy but predates the 2-tier Theme relabel and reconciliation columns; it may need a refreshed seed slice to demonstrate OKR→Project-KPI and Portfolio-link chains end-to-end. | Medium | Build session | Seed deliverable (goal item 11) larger than expected |
| ASM-005 | Feature flag `strategy_hub` / gate key `strategyhub` / role `enterprise` remain the intended gating for STRATA (no new flag needed). | High | Vikram | Nav work re-scoped |
| ASM-006 | "Do not call STRATA by any previous product/vendor name" = no "StrategyHub" strings in STRATA-facing UI; redirects (`/strategyhub`→`/strata`) and internal flag keys may keep the legacy key. | High | Vikram | Broader rename of flag keys/DB rows needed |
| ASM-007 | Governance "Review Cadence" is satisfied by `strata_cycles.snapshot_policy` + period granularity + Reviews page; no separate cadence entity required. | Medium | Vikram (product) | New cadence entity + UI |
| ASM-008 | Legacy non-STRATA stacks (`public.objectives`, `public.scorecards`, project-hub `public.projects`) stay untouched except where CON-002 decides otherwise. | High | Vikram | Scope grows substantially |

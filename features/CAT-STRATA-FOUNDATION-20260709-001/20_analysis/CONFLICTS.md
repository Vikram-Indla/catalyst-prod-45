# CONFLICTS register (CON-###) — Gate R2 decisions needed

| ID | Conflict | Evidence | Authority ranking | Proposed resolution | Status |
|---|---|---|---|---|---|
| CON-001 | **Terminology fork: "Play charter" survives** in DB (`strata_play_charters`), RPC (`strata_upsert_play_charter`), TS type (`StrataPlayCharter`), hook (`usePlayCharters`), and UI label "Play charter" on 3 pages — while the locked goal bans "Play" everywhere in active STRATA terminology. Migration `20260706230000` already declared "Theme and Play are the same business concept" but only renamed the enum value. | 02_CANONICAL_DISCOVERY §A4 | SRC-001 (locked goal) > existing code | Rename UI label + TS type + hook + domain method to "Theme charter" (mandatory); rename table/RPC via migration (view/synonym-free, safe rename) OR keep DB names as accepted internal legacy with a documented exception. Goal instruction 8 permits preserving legacy IDs while exposing only STRATA terminology in UI — **minimum viable = UI/code layer rename; recommended = full rename incl. DB**. | ESCALATED — pick minimum vs full |
| CON-002 | **Duplicate strategy hierarchies still live**: `modules/objectives` + `modules/okr-v2` (routed `/enterprise/objectives`) and `components/okr/` are three independent Objective/OKR stacks with own non-`strata_*` tables — locked goal AC19 bans duplicate strategy hierarchy. | §A6 | SRC-001 > existing code | Options: (a) decommission `/enterprise/objectives` surface and migrate to STRATA (big, risky), (b) keep as separate "team OKR" operational surface explicitly out of STRATA scope and document non-duplication rationale, (c) redirect it into `/strata`. Dead `StrategyCockpit` (zero importers) delete regardless. | ESCALATED — scope decision |
| CON-003 | **STRATA-as-product vs STRATA-as-hub — now also a BRANCH decision**: `README_STRATA_ISOLATION.md` (read 2026-07-09, status "Active Isolation", CAT-STRATA-ISOLATE-20260707-001) mandates: "STRATA work happens on `strata-standalone` or feature branches from `strata-standalone`"; `main` holds STRATA code "for reference" only; cross-branch merges are rare + explicit-approval. Current session is on `main`. The locked goal's navigation deliverable (4 areas under Strategy Cycle) could be built as hub-sidebar IA or standalone-product IA. | §C2; stash list; `README_STRATA_ISOLATION.md` §Branch Isolation | User decision required | Confirm BOTH: (a) IA target — extend `/strata/*` (all wiring exists) vs standalone track; (b) build branch — per isolation doctrine the STRATA foundation build should branch from `strata-standalone`, not `main`, unless Vikram overrides. | ESCALATED |
| CON-004 | **IA labels vs locked area names**: existing `EnterpriseSidebar` sections are (Strategy Room/Scorecards/KPI & OKR), (Execution/Portfolio & Value), (Data & Lineage/Reviews & Decisions) — the goal mandates the four areas read as **Strategy Execution, Balanced Scorecard, Value Management Office, Governance** under a Strategy Cycle. | §C1 item 7 | SRC-001 > existing labels | Rename/regroup sidebar sections to the four canonical areas (keep routes stable; labels + grouping only). Low risk. | Resolve at Plan Lock (no user decision needed beyond approval) |
| CON-005 | **Project Card ↔ Strategic Objective linkage is theme-only** — NARROWED 2026-07-09 after ASM-002 verification. Rules 8/10 edges already exist in repo schema: Project Objective→Theme Objective via `strategy_elements.parent_id` (theme-context enforced, `20260706191000` §3), Project KPI→Theme KPI via `execution_links 'rolls_up_to'` (§4), KR↔KPI via `strata_key_results.kpi_id`. **Remaining gap = rule 5 only**: no direct card→strategic-objective edge (`strata_project_cards.theme_id` exists, no `objective_id`). Fix = one nullable FK or typed `execution_link ('project_card'→'element','supports_objective')` + validation that the objective belongs to the card's theme. | §A3, §B3; `20260705100300:130`, `20260706191000` §3–4 | SRC-001 | Single additive migration + guard; no new tables. | RESOLVED in analysis — implement at Plan Lock (no user decision needed) |
| CON-006 | **Orphaned Astryx scaffolding** references dead routes; CLAUDE.md still documents Astryx as scoped to StrategyHub+Ideation, but nothing mounts it. | §C4 | Repo evidence > CLAUDE.md doc | Delete `src/modules/strategy/astryx/` (or explicitly revive for `/strata/*` — new decision). Recommend delete + CLAUDE.md note update. | ESCALATED (touches CLAUDE.md doctrine) |

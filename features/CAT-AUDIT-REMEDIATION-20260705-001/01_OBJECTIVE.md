# Objective

Close every P0/P1 finding in catalyst_p0_p1_static_findings_1000.csv with traced fixes (finding -> root cause -> module -> route -> journey -> fix -> files -> test/runtime proof) or an explicit, evidenced Deferred/Invalid/Duplicate status. No silent drops. No cosmetic-only patching — fix mental models (Test Hub lineage, Release readiness, Incident triage/SLA, Strategy scorecard, Chat collaboration) module by module in mandated order.

Non-scope this program: introducing a new design system, removing features to hide defects, fabricating screenshots/tests/runtime proof.

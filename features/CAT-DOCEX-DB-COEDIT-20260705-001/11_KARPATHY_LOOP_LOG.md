# Karpathy loop log
L1 H: editor chrome broken by missing base css → E: grep imports → M: style.css IS imported → DISCARD; keep incomplete-token-override hypothesis (probes: white side-menu icons, no shadow) → KEEP.

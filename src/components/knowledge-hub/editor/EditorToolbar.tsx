/**
 * EditorToolbar — DEPRECATED (2026-04-20).
 *
 * Was the hand-rolled TipTap toolbar (bold / italic / heading / list /
 * link / image / table / panels). Replaced by @atlaskit/editor-core's
 * built-in primary toolbar, which ships identical affordances and
 * matches Jira/Confluence pixel-for-pixel without a shim.
 *
 * The file is retained (instead of deleted) because file deletions are
 * not permitted in the current sandbox; nothing imports from it and it
 * no longer pulls `@tiptap/*` into the source tree.
 */
export {};

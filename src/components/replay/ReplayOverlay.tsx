import React from 'react';

interface ReplayOverlayProps {
  rootKey: string;
  onClose: () => void;
}

// Stub — portal-based ReplayTheatreOverlay permanently removed.
// Full inline theatre lives in ReplayDashboardWidget › TheatreView.
export function ReplayOverlay(_props: ReplayOverlayProps) {
  return null;
}

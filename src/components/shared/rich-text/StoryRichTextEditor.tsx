/**
 * StoryRichTextEditor — DEPRECATED (2026-04-20).
 *
 * Was a TipTap editor tuned to match Jira's story-description toolbar.
 * Atlaskit is now the single canonical rich-text surface — see
 * `components/shared/rich-text/atlaskit/EpicDescriptionEditor.tsx`.
 *
 * Retained (instead of deleted) because file deletions are not
 * permitted in the current sandbox; nothing imports from it.
 */
export const StoryRichTextEditor = undefined as unknown as never;

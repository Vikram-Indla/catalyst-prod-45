/**
 * ImageBubbleMenu — DEPRECATED (2026-04-20).
 *
 * Was a TipTap-only floating popover for managing image nodes.
 * Atlaskit's media plugin owns image affordances in the new canonical
 * editor — see `components/shared/rich-text/atlaskit/
 * EpicDescriptionEditor.tsx` and `atlaskitMediaOverrides.tsx`.
 *
 * Retained (instead of deleted) because file deletions are not
 * permitted in the current sandbox; nothing imports from it.
 */
export const ImageBubbleMenu = undefined as unknown as never;

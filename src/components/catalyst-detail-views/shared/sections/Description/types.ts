/**
 * Local type alias so we only import from the existing CatalystDetailViews
 * surface in one place. If the canonical PhIssue type moves, update here.
 */
export type { PhIssue } from '@/components/catalyst-detail-views/shared/types';

// NotificationPanel.tsx — SHELL. UI built in Stage C.
interface NotificationPanelProps {
  isOpen: boolean;
  onClose: () => void;
}
export default function NotificationPanel(_props: NotificationPanelProps) { return null; }

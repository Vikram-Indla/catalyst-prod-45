/**
 * Catalyst Login — shared types.
 * (Colour/feature constants removed; the portal styles live in login-styles.css
 *  using ADS tokens.)
 */

export type UserType = 'existing' | 'external';
export type AuthType = 'signin' | 'signup';

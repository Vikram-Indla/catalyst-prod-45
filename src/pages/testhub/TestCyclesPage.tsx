/**
 * Test Cycles Page — TestHub Module
 * Route: /testhub/cycles
 */

import { Navigate } from 'react-router-dom';

export default function TestCyclesPage() {
  return <Navigate to="/releases/test-cycles" replace />;
}

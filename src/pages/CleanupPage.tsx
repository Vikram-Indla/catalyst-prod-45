import { useNavigate } from "react-router-dom";

export default function CleanupPage() {
  return (
    <div style={{ padding: 24, fontFamily: "Inter, sans-serif" }}>
      AI Cleanup — shell (Stage C will build full UI)
    </div>
  );
}

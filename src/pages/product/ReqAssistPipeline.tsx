/**
 * ReqAssistPipeline — Pipeline Dashboard (Stage A shell)
 * Route: /product/req-assist
 */
import React from 'react';

export default function ReqAssistPipeline() {
  return (
    <div className="req-assist-pipeline" style={{ padding: '2rem' }}>
      <h1 style={{ fontFamily: "'Sora', sans-serif", fontSize: '1.25rem', fontWeight: 600, color: 'hsl(var(--foreground))' }}>
        Req Assist — Pipeline Dashboard
      </h1>
    </div>
  );
}

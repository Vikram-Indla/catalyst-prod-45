import React from 'react';

const IdeasRoadmapPage: React.FC = () => {
  return (
    <div className="ideas-roadmap-page" style={{ height: '100%', display: 'flex', flexDirection: 'column' }}>
      {/* STAGE C will build all content */}
      <div style={{ padding: '24px', color: '#64748B', fontFamily: 'Inter, sans-serif' }}>
        Ideas Roadmap — loading...
      </div>
    </div>
  );
};

export default IdeasRoadmapPage;

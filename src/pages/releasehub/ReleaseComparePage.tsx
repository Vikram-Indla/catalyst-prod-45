import { PageHeader } from '@/components/release/PageHeader';

export default function ReleaseComparePage() {
  return (
    <div className="rh-page">
      <PageHeader title="Release Compare" subtitle="Side-by-side release comparison" />
      {/* STAGE C builds all content here */}
    </div>
  );
}

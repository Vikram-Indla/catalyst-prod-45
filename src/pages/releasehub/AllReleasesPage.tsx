import { PageHeader } from '@/components/release/PageHeader';

export default function AllReleasesPage() {
  return (
    <div className="rh-page">
      <PageHeader title="All Releases" subtitle="Manage and track all releases" />
      {/* STAGE C builds all content here */}
    </div>
  );
}

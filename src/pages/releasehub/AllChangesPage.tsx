import { PageHeader } from '@/components/release/PageHeader';

export default function AllChangesPage() {
  return (
    <div className="rh-page">
      <PageHeader title="All Changes" subtitle="Change management registry" />
      {/* STAGE C builds all content here */}
    </div>
  );
}

/**
 * Placeholder — Phase 0 + 1 admin v2 foundation was reverted in a branch
 * switch between turns. The full WorkTypesPage cannot compile here without
 * `useAdminMutation`, `@/components/ads` barrel access, and the
 * `admin_action_audit` table from the Phase 0 SQL bundle.
 *
 * Original file lives in conversation history and at
 * audit/admin-overhaul-2026-04-29/04_phase0_complete.md (referenced).
 *
 * This stub keeps the file present so the import resolver doesn't error.
 * Restore by either (a) merging the Phase 0 + 1a/1b branch back in, or
 * (b) re-running the foundation via the original turns.
 */

export default function WorkTypesPage() {
  return null;
}

/**
 * ProjectHub Story Backlog — wraps the Backlog page
 * Maps project-hub :key context to the Story Backlog (unassigned stories).
 */
import Backlog from '@/pages/Backlog';

export default function ProjectStoryBacklogPage() {
  return <Backlog />;
}

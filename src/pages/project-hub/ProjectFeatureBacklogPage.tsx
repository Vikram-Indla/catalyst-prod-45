/**
 * ProjectHub Feature Backlog — wraps FeaturesBacklog
 * Maps project-hub :key context to the Feature Backlog page.
 */
import FeaturesBacklog from '@/pages/FeaturesBacklog';

export default function ProjectFeatureBacklogPage() {
  return <FeaturesBacklog />;
}
